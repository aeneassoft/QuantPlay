{
  "calculations": [
    {
      "category": "preflop_sizing_ranges",
      "definition": "R_open=clip_0.25(base(position)+stack_adjust(effective_bb)+2*ante_bb,2.0,3.5).",
      "formula_plain": "base={UTG:2.25,MP:2.25,HJ:2.25,CO:2.50,BTN:2.50,SB:3.00}; stack_adjust=-0.25 if bb<15, -0.125 if bb<=40, else 0; round to nearest 0.25bb.",
      "function_name": "preflop_open_raise_size_bb",
      "inputs": "position:str, effective_bb:float, ante_bb:float=0.0",
      "name": "Open-raise size by position and stack depth",
      "python_function": "def preflop_open_raise_size_bb(position: str, effective_bb: float, ante_bb: float = 0.0) -> float:\n    \"\"\"Open size in big blinds, rounded to 0.25bb. Example: BTN,100bb,0 ante -> 2.5.\"\"\"\n    base_by_pos = {\"UTG\": 2.25, \"MP\": 2.25, \"HJ\": 2.25, \"CO\": 2.50, \"BTN\": 2.50, \"SB\": 3.00}\n    p = position.upper()\n    if p not in base_by_pos:\n        raise ValueError(\"position must be one of UTG, MP, HJ, CO, BTN, SB\")\n    stack_adjust = -0.25 if effective_bb < 15.0 else (-0.125 if effective_bb <= 40.0 else 0.0)\n    raw = base_by_pos[p] + stack_adjust + min(0.5, 2.0 * max(0.0, ante_bb))\n    clipped = max(2.0, min(3.5, raw))\n    return round(clipped * 4.0) / 4.0",
      "verify_expr": "preflop_open_raise_size_bb('BTN', 100.0, 0.0)",
      "verify_value": 2.5,
      "worked_example": "preflop_open_raise_size_bb('BTN',100,0)=2.5"
    },
    {
      "category": "preflop_sizing_ranges",
      "definition": "R_3bet=open_size*(3IP+4OOP coefficient + callers), capped by effective_bb if supplied.",
      "formula_plain": "size=min(effective_bb, open_bb*((3 if IP else 4)+callers)); round to 0.25bb.",
      "function_name": "preflop_threebet_size_bb",
      "inputs": "open_size_bb:float, in_position:bool, callers:int=0, effective_bb:float|None=None",
      "name": "3-bet sizing IP/OOP with callers",
      "python_function": "def preflop_threebet_size_bb(open_size_bb: float, in_position: bool, callers: int = 0, effective_bb: float | None = None) -> float:\n    \"\"\"3-bet size in big blinds. Example: 2.5bb open, OOP, 0 callers -> 10.0bb.\"\"\"\n    if open_size_bb <= 0.0 or callers < 0:\n        raise ValueError(\"open_size_bb must be positive and callers nonnegative\")\n    multiplier = (3.0 if in_position else 4.0) + float(callers)\n    raw = open_size_bb * multiplier\n    if effective_bb is not None:\n        raw = min(raw, effective_bb)\n    return round(raw * 4.0) / 4.0",
      "verify_expr": "preflop_threebet_size_bb(2.5, False, 0, None)",
      "verify_value": 10.0,
      "worked_example": "preflop_threebet_size_bb(2.5,False,0)=10.0"
    },
    {
      "category": "preflop_sizing_ranges",
      "definition": "R_4bet=max(threebet+open, m*threebet), m=2.2 IP else 2.5 OOP, capped by effective stack.",
      "formula_plain": "size=min(effective_bb, max(threebet_bb+open_bb, threebet_bb*(2.2 if IP else 2.5))).",
      "function_name": "preflop_fourbet_size_bb",
      "inputs": "threebet_size_bb:float, open_size_bb:float, in_position:bool, effective_bb:float|None=None",
      "name": "4-bet sizing IP/OOP",
      "python_function": "def preflop_fourbet_size_bb(threebet_size_bb: float, open_size_bb: float, in_position: bool, effective_bb: float | None = None) -> float:\n    \"\"\"4-bet size in big blinds. Example: 10bb 3bet, 2.5bb open, IP -> 22.0bb.\"\"\"\n    if threebet_size_bb <= 0.0 or open_size_bb <= 0.0:\n        raise ValueError(\"sizes must be positive\")\n    multiplier = 2.2 if in_position else 2.5\n    raw = max(threebet_size_bb + open_size_bb, threebet_size_bb * multiplier)\n    if effective_bb is not None:\n        raw = min(raw, effective_bb)\n    return round(raw * 4.0) / 4.0",
      "verify_expr": "preflop_fourbet_size_bb(10.0, 2.5, True, 100.0)",
      "verify_value": 22.0,
      "worked_example": "preflop_fourbet_size_bb(10,2.5,True,100)=22.0"
    },
    {
      "category": "preflop_sizing_ranges",
      "definition": "R_squeeze=open_size*(3IP+4OOP coefficient + number_of_callers), capped by effective_bb if supplied.",
      "formula_plain": "squeeze_bb=min(effective_bb, open_bb*((3 if IP else 4)+callers)).",
      "function_name": "preflop_squeeze_size_bb",
      "inputs": "open_size_bb:float, callers:int, in_position:bool, effective_bb:float|None=None",
      "name": "Squeeze sizing by number of callers",
      "python_function": "def preflop_squeeze_size_bb(open_size_bb: float, callers: int, in_position: bool, effective_bb: float | None = None) -> float:\n    \"\"\"Squeeze size in big blinds. Example: 2.5bb open, 2 callers, OOP -> 15.0bb.\"\"\"\n    if open_size_bb <= 0.0 or callers < 1:\n        raise ValueError(\"open_size_bb must be positive and callers >= 1\")\n    raw = open_size_bb * ((3.0 if in_position else 4.0) + float(callers))\n    if effective_bb is not None:\n        raw = min(raw, effective_bb)\n    return round(raw * 4.0) / 4.0",
      "verify_expr": "preflop_squeeze_size_bb(2.5, 2, False, None)",
      "verify_value": 15.0,
      "worked_example": "preflop_squeeze_size_bb(2.5,2,False)=15.0"
    },
    {
      "category": "preflop_sizing_ranges",
      "definition": "Calling fraction from pot odds after equity realization: f_call=clip(1-(call/(pot+call))/R,0,1).",
      "formula_plain": "R=clip(0.55+0.10*position_index+0.0015*min(effective_bb,100),0.45,0.90); f=clip(1-pot_odds/R,0,1).",
      "function_name": "preflop_calling_range_fraction",
      "inputs": "pot_before_call_bb:float, call_bb:float, position_index:float, effective_bb:float",
      "name": "Preflop calling-range fraction from odds, position, stack",
      "python_function": "def preflop_calling_range_fraction(pot_before_call_bb: float, call_bb: float, position_index: float, effective_bb: float) -> float:\n    \"\"\"Top-fraction proxy of hands callable preflop after realization. position_index: 0=OOP, 1=IP.\"\"\"\n    if pot_before_call_bb < 0.0 or call_bb <= 0.0 or effective_bb <= 0.0:\n        raise ValueError(\"pot must be nonnegative; call and stack must be positive\")\n    pos = max(0.0, min(1.0, position_index))\n    realization = max(0.45, min(0.90, 0.55 + 0.10 * pos + 0.0015 * min(effective_bb, 100.0)))\n    pot_odds = call_bb / (pot_before_call_bb + call_bb)\n    return max(0.0, min(1.0, 1.0 - pot_odds / realization))",
      "verify_expr": "preflop_calling_range_fraction(7.5, 2.5, 1.0, 100.0)",
      "verify_value": 0.6875,
      "worked_example": "R=0.80, pot_odds=0.25, f=1-0.25/0.80=0.6875"
    },
    {
      "category": "preflop_sizing_ranges",
      "definition": "Open-shove top-range fraction q=clip(base_pos*sqrt(20/effective_bb)*(1+2*ante_bb_per_player),0,1).",
      "formula_plain": "base={UTG:.12,MP:.15,HJ:.18,CO:.25,BTN:.34,SB:.55}; q=base*sqrt(20/bb)*(1+2*ante).",
      "function_name": "short_stack_open_shove_fraction",
      "inputs": "position:str, effective_bb:float, ante_bb_per_player:float=0.0",
      "name": "Short-stack Nash-ish open-shove range fraction by bb",
      "python_function": "def short_stack_open_shove_fraction(position: str, effective_bb: float, ante_bb_per_player: float = 0.0) -> float:\n    \"\"\"Nash-ish open-shove top fraction. Example: BTN 10bb no ante -> 0.4808326112.\"\"\"\n    import math\n    base_by_pos = {\"UTG\": 0.12, \"MP\": 0.15, \"HJ\": 0.18, \"CO\": 0.25, \"BTN\": 0.34, \"SB\": 0.55}\n    p = position.upper()\n    if p not in base_by_pos or effective_bb <= 0.0:\n        raise ValueError(\"bad position or nonpositive stack\")\n    raw = base_by_pos[p] * math.sqrt(20.0 / effective_bb) * (1.0 + 2.0 * max(0.0, min(0.25, ante_bb_per_player)))\n    return max(0.0, min(1.0, raw))",
      "verify_expr": "short_stack_open_shove_fraction('BTN', 10.0, 0.0)",
      "verify_value": 0.4808326112068523,
      "worked_example": "0.34*sqrt(20/10)=0.4808326112068523"
    },
    {
      "category": "range_construction_balance",
      "definition": "Balanced bluff combos against a call-indifferent opponent: B=V*(bet/(pot+bet))/(1-bluff_equity).",
      "formula_plain": "B/V=alpha/(1-e_b), alpha=bet/(pot+bet).",
      "function_name": "balanced_bluff_combos",
      "inputs": "value_combos:float, bet_size:float, pot_size:float, bluff_equity:float=0.0",
      "name": "Value-to-bluff combos by bet size",
      "python_function": "def balanced_bluff_combos(value_combos: float, bet_size: float, pot_size: float, bluff_equity: float = 0.0) -> float:\n    \"\"\"Bluff combos paired with value combos. Example: V=30, bet=75, pot=100, e=0 -> 12.8571428571.\"\"\"\n    if value_combos < 0.0 or bet_size <= 0.0 or pot_size <= 0.0:\n        raise ValueError(\"value must be nonnegative; bet and pot positive\")\n    e = max(0.0, min(0.999999, bluff_equity))\n    alpha = bet_size / (pot_size + bet_size)\n    return value_combos * alpha / (1.0 - e)",
      "verify_expr": "balanced_bluff_combos(30.0, 75.0, 100.0, 0.0)",
      "verify_value": 12.857142857142858,
      "worked_example": "30*(75/(100+75))/(1-0)=12.857142857142858"
    },
    {
      "category": "range_construction_balance",
      "definition": "Polarization degree π=(nut_combos+air_combos)/(nut+air+medium).",
      "formula_plain": "pi=(N+A)/(N+A+M).",
      "function_name": "polarization_degree",
      "inputs": "nut_combos:float, air_combos:float, medium_combos:float",
      "name": "Polarization degree of a betting range",
      "python_function": "def polarization_degree(nut_combos: float, air_combos: float, medium_combos: float) -> float:\n    \"\"\"Fraction of range lying at nut-or-air tails. Example: 10,15,25 -> 0.5.\"\"\"\n    if nut_combos < 0.0 or air_combos < 0.0 or medium_combos < 0.0:\n        raise ValueError(\"combos must be nonnegative\")\n    total = nut_combos + air_combos + medium_combos\n    if total == 0.0:\n        return 0.0\n    return (nut_combos + air_combos) / total",
      "verify_expr": "polarization_degree(10.0, 15.0, 25.0)",
      "verify_value": 0.5,
      "worked_example": "(10+15)/(10+15+25)=0.5"
    },
    {
      "category": "range_construction_balance",
      "definition": "Capped-range penalty = max(opp_nut_fraction-hero_nut_fraction,0)*SPR/(SPR+4)*bet_pot_fraction.",
      "formula_plain": "penalty=max(n_opp-n_hero,0)*spr/(spr+4)*b_pot.",
      "function_name": "capped_range_penalty",
      "inputs": "hero_nut_fraction:float, opponent_nut_fraction:float, spr:float, bet_pot_fraction:float",
      "name": "Capped-range penalty from nut disadvantage",
      "python_function": "def capped_range_penalty(hero_nut_fraction: float, opponent_nut_fraction: float, spr: float, bet_pot_fraction: float) -> float:\n    \"\"\"Scalar penalty for capped ranges. Example: .05 vs .15 at SPR4 and 75% pot -> .0375.\"\"\"\n    if spr < 0.0 or bet_pot_fraction < 0.0:\n        raise ValueError(\"spr and bet fraction must be nonnegative\")\n    hn = max(0.0, min(1.0, hero_nut_fraction))\n    on = max(0.0, min(1.0, opponent_nut_fraction))\n    return max(0.0, on - hn) * (spr / (spr + 4.0)) * bet_pot_fraction",
      "verify_expr": "capped_range_penalty(0.05, 0.15, 4.0, 0.75)",
      "verify_value": 0.0375,
      "worked_example": "(.15-.05)*(4/(4+4))*0.75=0.0375"
    },
    {
      "category": "range_construction_balance",
      "definition": "Nut fraction n=nut_combos/total_combos.",
      "formula_plain": "n=N/T if T>0 else 0.",
      "function_name": "nut_fraction",
      "inputs": "nut_combos:float, total_combos:float",
      "name": "Nut-fraction of a range",
      "python_function": "def nut_fraction(nut_combos: float, total_combos: float) -> float:\n    \"\"\"Nut-combo density. Example: 12 nut combos out of 120 -> 0.1.\"\"\"\n    if nut_combos < 0.0 or total_combos < 0.0 or nut_combos > total_combos:\n        raise ValueError(\"require 0 <= nut_combos <= total_combos\")\n    if total_combos == 0.0:\n        return 0.0\n    return nut_combos / total_combos",
      "verify_expr": "nut_fraction(12.0, 120.0)",
      "verify_value": 0.1,
      "worked_example": "12/120=0.1"
    },
    {
      "category": "range_construction_balance",
      "definition": "Morphology selector: 0=linear, 1=merged, 2=polar, by SPR, texture wetness, and nut advantage.",
      "formula_plain": "linear if SPR<=2 or wet>=.70; polar if nut_adv>.08 or (SPR>=6 and wet<=.45); otherwise merged.",
      "function_name": "range_morphology_selector",
      "inputs": "spr:float, texture_wetness:float, nut_advantage:float",
      "name": "Range morphology selector by SPR and texture",
      "python_function": "def range_morphology_selector(spr: float, texture_wetness: float, nut_advantage: float) -> int:\n    \"\"\"Return 0 linear, 1 merged, 2 polar. Example: SPR8, dry .2, nut_adv .15 -> 2.\"\"\"\n    if spr < 0.0:\n        raise ValueError(\"spr must be nonnegative\")\n    wet = max(0.0, min(1.0, texture_wetness))\n    if spr <= 2.0 or wet >= 0.70:\n        return 0\n    if nut_advantage > 0.08 or (spr >= 6.0 and wet <= 0.45):\n        return 2\n    return 1",
      "verify_expr": "range_morphology_selector(8.0, 0.2, 0.15)",
      "verify_value": 2,
      "worked_example": "SPR=8, wet=.2, nut_adv=.15 => polar=2"
    },
    {
      "category": "range_construction_balance",
      "definition": "Mergedness μ=1-|top_tail+bottom_tail-middle|/total, clipped to [0,1].",
      "formula_plain": "mu=clip(1-abs((N+A)-M)/(N+A+M),0,1).",
      "function_name": "mergedness_index",
      "inputs": "nut_combos:float, air_combos:float, medium_combos:float",
      "name": "Merged-range index",
      "python_function": "def mergedness_index(nut_combos: float, air_combos: float, medium_combos: float) -> float:\n    \"\"\"High when tails and middle are similarly represented. Example: 10,10,20 -> 1.0.\"\"\"\n    if nut_combos < 0.0 or air_combos < 0.0 or medium_combos < 0.0:\n        raise ValueError(\"combos must be nonnegative\")\n    total = nut_combos + air_combos + medium_combos\n    if total == 0.0:\n        return 0.0\n    return max(0.0, min(1.0, 1.0 - abs((nut_combos + air_combos) - medium_combos) / total))",
      "verify_expr": "mergedness_index(10.0, 10.0, 20.0)",
      "verify_value": 1.0,
      "worked_example": "1-|20-20|/40=1.0"
    },
    {
      "category": "cbet_barrel_theory",
      "definition": "C-bet frequency f=clip(.45+.20*IP+.25*range_adv+.15*nut_adv-.25*wet-.015*min(SPR,20),.05,.95).",
      "formula_plain": "f_cbet=clip(0.45+0.20p+0.25ra+0.15na-0.25w-0.015s,0.05,0.95).",
      "function_name": "cbet_frequency",
      "inputs": "texture_wetness:float, in_position:bool, spr:float, range_advantage:float, nut_advantage:float",
      "name": "C-bet frequency by texture, position, SPR, advantages",
      "python_function": "def cbet_frequency(texture_wetness: float, in_position: bool, spr: float, range_advantage: float, nut_advantage: float) -> float:\n    \"\"\"C-bet frequency scalar. Example: wet=.3, IP, SPR4, ra=.1, na=.05 -> .5475.\"\"\"\n    if spr < 0.0:\n        raise ValueError(\"spr must be nonnegative\")\n    wet = max(0.0, min(1.0, texture_wetness))\n    pos = 1.0 if in_position else 0.0\n    raw = 0.45 + 0.20 * pos + 0.25 * range_advantage + 0.15 * nut_advantage - 0.25 * wet - 0.015 * min(spr, 20.0)\n    return max(0.05, min(0.95, raw))",
      "verify_expr": "cbet_frequency(0.3, True, 4.0, 0.1, 0.05)",
      "verify_value": 0.5475,
      "worked_example": ".45+.20+.025+.0075-.075-.060=.5475"
    },
    {
      "category": "cbet_barrel_theory",
      "definition": "Next-street barrel frequency f_next=clip(f_prev*(.55+.25*eq_adv+.15*scare+.10*IP-.05*(street-2)),0,1).",
      "formula_plain": "f_barrel=clip(f_prev*k,0,1), k=.55+.25e+.15c+.10p-.05(street-2).",
      "function_name": "barrel_continuation_frequency",
      "inputs": "previous_bet_frequency:float, equity_advantage:float, scare_card:float, in_position:bool, street:int",
      "name": "Double/triple-barrel continuation frequency",
      "python_function": "def barrel_continuation_frequency(previous_bet_frequency: float, equity_advantage: float, scare_card: float, in_position: bool, street: int) -> float:\n    \"\"\"Barrel continuation frequency. street: 3=turn after flop, 4=river after turn.\"\"\"\n    if street not in (3, 4):\n        raise ValueError(\"street must be 3 or 4\")\n    prev = max(0.0, min(1.0, previous_bet_frequency))\n    scare = max(0.0, min(1.0, scare_card))\n    pos = 1.0 if in_position else 0.0\n    k = 0.55 + 0.25 * equity_advantage + 0.15 * scare + 0.10 * pos - 0.05 * (street - 2)\n    return max(0.0, min(1.0, prev * k))",
      "verify_expr": "barrel_continuation_frequency(0.55, 0.2, 1.0, True, 3)",
      "verify_value": 0.44,
      "worked_example": "0.55*(.55+.05+.15+.10-.05)=0.44"
    },
    {
      "category": "cbet_barrel_theory",
      "definition": "Give-up frequency g=clip(1-bet_freq-.5*showdown_value_fraction*(1-fold_equity),0,1).",
      "formula_plain": "g=clip(1-f_bet-0.5*sdv*(1-FE),0,1).",
      "function_name": "give_up_frequency",
      "inputs": "street_bet_frequency:float, showdown_value_fraction:float, fold_equity:float",
      "name": "Give-up frequency after betting/checking allocation",
      "python_function": "def give_up_frequency(street_bet_frequency: float, showdown_value_fraction: float, fold_equity: float) -> float:\n    \"\"\"Residual give-up frequency. Example: bet=.5, sdv=.4, FE=.3 -> .36.\"\"\"\n    b = max(0.0, min(1.0, street_bet_frequency))\n    sdv = max(0.0, min(1.0, showdown_value_fraction))\n    fe = max(0.0, min(1.0, fold_equity))\n    return max(0.0, min(1.0, 1.0 - b - 0.5 * sdv * (1.0 - fe)))",
      "verify_expr": "give_up_frequency(0.5, 0.4, 0.3)",
      "verify_value": 0.36,
      "worked_example": "1-.5-.5*.4*(1-.3)=.36"
    },
    {
      "category": "cbet_barrel_theory",
      "definition": "Range-vs-range advantage to bet frequency: f=clip(.5+1.2*equity_adv+.4*nut_adv+.1*IP-.05*(street-2),0,1).",
      "formula_plain": "f=clip(0.5+1.2EA+0.4NA+0.1p-0.05(street-2),0,1).",
      "function_name": "range_advantage_to_bet_frequency",
      "inputs": "equity_advantage:float, nut_advantage:float, in_position:bool, street:int",
      "name": "Range advantage to betting-frequency map",
      "python_function": "def range_advantage_to_bet_frequency(equity_advantage: float, nut_advantage: float, in_position: bool, street: int) -> float:\n    \"\"\"Map range/nut advantages to bet frequency. street: 2 flop, 3 turn, 4 river.\"\"\"\n    if street not in (2, 3, 4):\n        raise ValueError(\"street must be 2, 3, or 4\")\n    pos = 1.0 if in_position else 0.0\n    raw = 0.5 + 1.2 * equity_advantage + 0.4 * nut_advantage + 0.1 * pos - 0.05 * (street - 2)\n    return max(0.0, min(1.0, raw))",
      "verify_expr": "range_advantage_to_bet_frequency(0.05, 0.1, True, 2)",
      "verify_value": 0.7,
      "worked_example": ".5+1.2*.05+.4*.1+.1=0.7"
    },
    {
      "category": "cbet_barrel_theory",
      "definition": "Delayed c-bet EV for a stab after checkback: EV=F*P+(1-F)*(E_real*(P+B)-B).",
      "formula_plain": "EV_delay=FE*pot+(1-FE)*(realized_equity_when_called*(pot+bet)-bet).",
      "function_name": "delayed_cbet_ev",
      "inputs": "pot_size:float, bet_size:float, fold_equity:float, realized_equity_when_called:float",
      "name": "Delayed-cbet stab EV",
      "python_function": "def delayed_cbet_ev(pot_size: float, bet_size: float, fold_equity: float, realized_equity_when_called: float) -> float:\n    \"\"\"EV of delayed c-bet/stab. Example: pot=100, bet=50, FE=.4, realized_e=.3 -> 37.\"\"\"\n    if pot_size < 0.0 or bet_size < 0.0:\n        raise ValueError(\"pot and bet must be nonnegative\")\n    fe = max(0.0, min(1.0, fold_equity))\n    re = max(0.0, min(1.0, realized_equity_when_called))\n    return fe * pot_size + (1.0 - fe) * (re * (pot_size + bet_size) - bet_size)",
      "verify_expr": "delayed_cbet_ev(100.0, 50.0, 0.4, 0.3)",
      "verify_value": 37.0,
      "worked_example": ".4*100+.6*(.3*150-50)=37"
    },
    {
      "category": "bluffing_defense",
      "definition": "River balanced bluff:value ratio α=B/(P+B).",
      "formula_plain": "bluffs/value=bet/(pot+bet).",
      "function_name": "river_bluff_to_value_ratio",
      "inputs": "bet_size:float, pot_size:float",
      "name": "River bluff-to-value ratio by bet size",
      "python_function": "def river_bluff_to_value_ratio(bet_size: float, pot_size: float) -> float:\n    \"\"\"Balanced river bluff-to-value ratio. Example: bet=75 into 100 -> 0.4285714286.\"\"\"\n    if bet_size <= 0.0 or pot_size <= 0.0:\n        raise ValueError(\"bet and pot must be positive\")\n    return bet_size / (pot_size + bet_size)",
      "verify_expr": "river_bluff_to_value_ratio(75.0, 100.0)",
      "verify_value": 0.42857142857142855,
      "worked_example": "75/(100+75)=0.42857142857142855"
    },
    {
      "category": "bluffing_defense",
      "definition": "Bluff-catcher break-even equity q*=call/(pot_before_call+call).",
      "formula_plain": "q*=C/(P+C).",
      "function_name": "bluff_catcher_breakeven_equity",
      "inputs": "call_amount:float, pot_before_call:float",
      "name": "Bluff-catcher break-even equity",
      "python_function": "def bluff_catcher_breakeven_equity(call_amount: float, pot_before_call: float) -> float:\n    \"\"\"Break-even equity for a bluff-catcher. Example: call 50 to win pot 150 -> .25.\"\"\"\n    if call_amount <= 0.0 or pot_before_call <= 0.0:\n        raise ValueError(\"call and pot must be positive\")\n    return call_amount / (pot_before_call + call_amount)",
      "verify_expr": "bluff_catcher_breakeven_equity(50.0, 150.0)",
      "verify_value": 0.25,
      "worked_example": "50/(150+50)=0.25"
    },
    {
      "category": "bluffing_defense",
      "definition": "EV gain from excess folds over equilibrium: ΔEV=(F_actual-F_eq)*(pot+bet).",
      "formula_plain": "delta_ev=(actual_fold-equilibrium_fold)*(P+B).",
      "function_name": "exploit_fold_deviation_ev",
      "inputs": "pot_size:float, bet_size:float, actual_fold_frequency:float, equilibrium_fold_frequency:float",
      "name": "Over-fold exploit EV for bluffing deviation",
      "python_function": "def exploit_fold_deviation_ev(pot_size: float, bet_size: float, actual_fold_frequency: float, equilibrium_fold_frequency: float) -> float:\n    \"\"\"EV gain of bluffing versus an over-fold read. Example: P=100,B=50,F=.6,F_eq=1/3 -> 40.\"\"\"\n    if pot_size < 0.0 or bet_size < 0.0:\n        raise ValueError(\"pot and bet must be nonnegative\")\n    f = max(0.0, min(1.0, actual_fold_frequency))\n    feq = max(0.0, min(1.0, equilibrium_fold_frequency))\n    return (f - feq) * (pot_size + bet_size)",
      "verify_expr": "exploit_fold_deviation_ev(100.0, 50.0, 0.6, 1.0/3.0)",
      "verify_value": 40.0,
      "worked_example": "(.6-1/3)*(100+50)=40"
    },
    {
      "category": "bluffing_defense",
      "definition": "EV gain from excess bluff density when bluff-catching: ΔEV=(q_actual-q_eq)*(pot_before_call+call).",
      "formula_plain": "delta_ev=(actual_bluff_fraction-equilibrium_bluff_fraction)*(P+C).",
      "function_name": "exploit_call_deviation_ev",
      "inputs": "call_amount:float, pot_before_call:float, actual_bluff_fraction:float, equilibrium_bluff_fraction:float",
      "name": "Under-bluff/over-bluff exploit EV for calling deviation",
      "python_function": "def exploit_call_deviation_ev(call_amount: float, pot_before_call: float, actual_bluff_fraction: float, equilibrium_bluff_fraction: float) -> float:\n    \"\"\"EV gain of bluff-catching versus excess bluffs. Example: C=50,P=150,q=.4,qeq=.25 -> 30.\"\"\"\n    if call_amount < 0.0 or pot_before_call < 0.0:\n        raise ValueError(\"call and pot must be nonnegative\")\n    q = max(0.0, min(1.0, actual_bluff_fraction))\n    qeq = max(0.0, min(1.0, equilibrium_bluff_fraction))\n    return (q - qeq) * (pot_before_call + call_amount)",
      "verify_expr": "exploit_call_deviation_ev(50.0, 150.0, 0.4, 0.25)",
      "verify_value": 30.0,
      "worked_example": "(.4-.25)*(150+50)=30"
    },
    {
      "category": "bluffing_defense",
      "definition": "Minimum check-raise combos after accounting for check-call defense: XR=max(0,MDF-check_call_fraction)*check_range_combos.",
      "formula_plain": "MDF=pot/(pot+bet); XR_combos=max(0,MDF-cc_fraction)*N_check.",
      "function_name": "minimum_check_raise_defense_combos",
      "inputs": "check_range_combos:float, pot_size:float, bet_size:float, check_call_fraction_of_range:float",
      "name": "Minimum check-raise defense combos",
      "python_function": "def minimum_check_raise_defense_combos(check_range_combos: float, pot_size: float, bet_size: float, check_call_fraction_of_range: float) -> float:\n    \"\"\"Minimum XR combos to fill MDF after check-call allocation. Example: N=100,P=100,B=50,CC=.5 -> 16.6666667.\"\"\"\n    if check_range_combos < 0.0 or pot_size <= 0.0 or bet_size <= 0.0:\n        raise ValueError(\"combos nonnegative; pot and bet positive\")\n    cc = max(0.0, min(1.0, check_call_fraction_of_range))\n    mdf = pot_size / (pot_size + bet_size)\n    return max(0.0, mdf - cc) * check_range_combos",
      "verify_expr": "minimum_check_raise_defense_combos(100.0, 100.0, 50.0, 0.5)",
      "verify_value": 16.666666666666664,
      "worked_example": "(100/(100+50)-.5)*100=16.666666666666664"
    },
    {
      "category": "bluffing_defense",
      "definition": "Signed bluff EV against defense D: EV=P-D*(P+B); positive means over-fold exploit, negative means under-fold punishment.",
      "formula_plain": "EV_bluff=P-defense*(P+B).",
      "function_name": "fold_bias_bluff_ev",
      "inputs": "pot_size:float, bet_size:float, actual_defense_frequency:float",
      "name": "Over-fold / under-fold signed bluff EV",
      "python_function": "def fold_bias_bluff_ev(pot_size: float, bet_size: float, actual_defense_frequency: float) -> float:\n    \"\"\"Signed bluff EV from defense frequency. Example: P=100,B=50,D=.5 -> 25.\"\"\"\n    if pot_size < 0.0 or bet_size < 0.0:\n        raise ValueError(\"pot and bet must be nonnegative\")\n    d = max(0.0, min(1.0, actual_defense_frequency))\n    return pot_size - d * (pot_size + bet_size)",
      "verify_expr": "fold_bias_bluff_ev(100.0, 50.0, 0.5)",
      "verify_value": 25.0,
      "worked_example": "100-.5*(100+50)=25"
    },
    {
      "category": "stack_depth_odds",
      "definition": "Set-mining margin = effective_stack/call - 12/stack_off_fraction.",
      "formula_plain": "margin=effective_bb/call_bb - 12/stackoff_fraction.",
      "function_name": "set_mining_implied_odds_margin",
      "inputs": "call_bb:float, effective_bb:float, stack_off_fraction:float=1.0",
      "name": "Set-mining implied-odds threshold margin",
      "python_function": "def set_mining_implied_odds_margin(call_bb: float, effective_bb: float, stack_off_fraction: float = 1.0) -> float:\n    \"\"\"Positive iff implied odds meet set-mining threshold. Example: call2, eff30, stackoff .8 -> 0.\"\"\"\n    if call_bb <= 0.0 or effective_bb <= 0.0:\n        raise ValueError(\"call and stack must be positive\")\n    sf = max(0.000001, min(1.0, stack_off_fraction))\n    return effective_bb / call_bb - 12.0 / sf",
      "verify_expr": "set_mining_implied_odds_margin(2.0, 30.0, 0.8)",
      "verify_value": 0.0,
      "worked_example": "30/2 - 12/.8 = 15-15=0"
    },
    {
      "category": "stack_depth_odds",
      "definition": "Suited-connector implied-odds margin = effective_stack/call - (20-3*IP-2*multiway_callers).",
      "formula_plain": "margin=effective_bb/call_bb - max(8,20-3p-2m).",
      "function_name": "suited_connector_implied_odds_margin",
      "inputs": "call_bb:float, effective_bb:float, in_position:bool, multiway_callers:int",
      "name": "Suited-connector implied-odds threshold margin",
      "python_function": "def suited_connector_implied_odds_margin(call_bb: float, effective_bb: float, in_position: bool, multiway_callers: int) -> float:\n    \"\"\"Positive iff suited-connector call has sufficient implied odds. Example: call2, eff40, IP, 2 callers -> 7.\"\"\"\n    if call_bb <= 0.0 or effective_bb <= 0.0 or multiway_callers < 0:\n        raise ValueError(\"call/stack positive; callers nonnegative\")\n    p = 1.0 if in_position else 0.0\n    threshold = max(8.0, 20.0 - 3.0 * p - 2.0 * min(multiway_callers, 3))\n    return effective_bb / call_bb - threshold",
      "verify_expr": "suited_connector_implied_odds_margin(2.0, 40.0, True, 2)",
      "verify_value": 7.0,
      "worked_example": "40/2 - (20-3-4)=20-13=7"
    },
    {
      "category": "stack_depth_odds",
      "definition": "Stack-off commitment margin versus shove: equity - SPR/(1+2*SPR).",
      "formula_plain": "margin=e - spr/(1+2spr).",
      "function_name": "commitment_margin_by_spr",
      "inputs": "spr:float, equity_when_called:float",
      "name": "Commitment threshold by SPR and equity",
      "python_function": "def commitment_margin_by_spr(spr: float, equity_when_called: float) -> float:\n    \"\"\"Positive iff call-off/stack-off is pot-odds committed. Example: SPR2, equity .45 -> .05.\"\"\"\n    if spr < 0.0:\n        raise ValueError(\"spr must be nonnegative\")\n    e = max(0.0, min(1.0, equity_when_called))\n    threshold = spr / (1.0 + 2.0 * spr)\n    return e - threshold",
      "verify_expr": "commitment_margin_by_spr(2.0, 0.45)",
      "verify_value": 0.04999999999999999,
      "worked_example": ".45 - 2/(1+4)=.05"
    },
    {
      "category": "stack_depth_odds",
      "definition": "Reverse-implied-odds discounted equity: E'=E*(1-domination_risk*SPR/(SPR+4)*(1-.2*IP)).",
      "formula_plain": "E_eff=E*(1-r*spr/(spr+4)*(1-0.2p)).",
      "function_name": "reverse_implied_odds_discounted_equity",
      "inputs": "raw_equity:float, domination_risk:float, spr:float, in_position:bool",
      "name": "Reverse-implied-odds equity discount",
      "python_function": "def reverse_implied_odds_discounted_equity(raw_equity: float, domination_risk: float, spr: float, in_position: bool) -> float:\n    \"\"\"Equity after reverse-implied-odds discount. Example: E=.4,r=.5,SPR4,OOP -> .3.\"\"\"\n    if spr < 0.0:\n        raise ValueError(\"spr must be nonnegative\")\n    e = max(0.0, min(1.0, raw_equity))\n    r = max(0.0, min(1.0, domination_risk))\n    p = 1.0 if in_position else 0.0\n    discounted = e * (1.0 - r * (spr / (spr + 4.0)) * (1.0 - 0.2 * p))\n    return max(0.0, min(1.0, discounted))",
      "verify_expr": "reverse_implied_odds_discounted_equity(0.4, 0.5, 4.0, False)",
      "verify_value": 0.3,
      "worked_example": ".4*(1-.5*4/(4+4))=.3"
    },
    {
      "category": "exploitative_deviation",
      "definition": "Bounded deviation EV = clip(confidence*magnitude*sensitivity,-1,1)*(pot+risk).",
      "formula_plain": "EV_dev=clip(c*d*s,-1,1)*(P+R).",
      "function_name": "bounded_read_deviation_ev",
      "inputs": "read_delta:float, confidence:float, pot_size:float, risk_amount:float, sensitivity:float=1.0",
      "name": "EV of bounded exploitative deviation from read magnitude/confidence",
      "python_function": "def bounded_read_deviation_ev(read_delta: float, confidence: float, pot_size: float, risk_amount: float, sensitivity: float = 1.0) -> float:\n    \"\"\"Bounded read-based deviation EV. Example: d=.2,c=.75,P=100,R=50,s=2 -> 45.\"\"\"\n    if pot_size < 0.0 or risk_amount < 0.0:\n        raise ValueError(\"pot and risk must be nonnegative\")\n    c = max(0.0, min(1.0, confidence))\n    edge = max(-1.0, min(1.0, c * read_delta * sensitivity))\n    return edge * (pot_size + risk_amount)",
      "verify_expr": "bounded_read_deviation_ev(0.2, 0.75, 100.0, 50.0, 2.0)",
      "verify_value": 45.0,
      "worked_example": "clip(.75*.2*2,-1,1)*(100+50)=45"
    },
    {
      "category": "exploitative_deviation",
      "definition": "Read strength delta from VPIP/PFR gap, fold-to-cbet excess, and passivity.",
      "formula_plain": "delta=clip(.5*(VPIP-PFR)+.3*(FTC-.45)+.2*((1.5-AF)/1.5),-1,1).",
      "function_name": "stat_read_delta",
      "inputs": "vpip:float, pfr:float, fold_to_cbet:float, aggression_factor:float",
      "name": "VPIP/PFR/fold-to-cbet/aggression read delta",
      "python_function": "def stat_read_delta(vpip: float, pfr: float, fold_to_cbet: float, aggression_factor: float) -> float:\n    \"\"\"Composite exploit read delta. Example: .40,.10,.60,.75 -> .295.\"\"\"\n    v = max(0.0, min(1.0, vpip))\n    p = max(0.0, min(1.0, pfr))\n    ftc = max(0.0, min(1.0, fold_to_cbet))\n    af = max(0.0, aggression_factor)\n    raw = 0.5 * (v - p) + 0.3 * (ftc - 0.45) + 0.2 * ((1.5 - af) / 1.5)\n    return max(-1.0, min(1.0, raw))",
      "verify_expr": "stat_read_delta(0.40, 0.10, 0.60, 0.75)",
      "verify_value": 0.295,
      "worked_example": ".5*(.40-.10)+.3*(.60-.45)+.2*((1.5-.75)/1.5)=.295"
    },
    {
      "category": "position_realization",
      "definition": "Realized equity = raw_equity*clip(.65+.15*IP+.05*initiative+.015*min(SPR,10),.5,1.05).",
      "formula_plain": "E_real=E*R, R=clip(.65+.15p+.05i+.015min(spr,10),.5,1.05).",
      "function_name": "realized_equity_by_position_spr",
      "inputs": "raw_equity:float, in_position:bool, spr:float, has_initiative:bool",
      "name": "Equity realization by position, SPR, initiative",
      "python_function": "def realized_equity_by_position_spr(raw_equity: float, in_position: bool, spr: float, has_initiative: bool) -> float:\n    \"\"\"Realized equity. Example: E=.4, IP, SPR5, initiative -> .37.\"\"\"\n    if spr < 0.0:\n        raise ValueError(\"spr must be nonnegative\")\n    e = max(0.0, min(1.0, raw_equity))\n    p = 1.0 if in_position else 0.0\n    i = 1.0 if has_initiative else 0.0\n    r = max(0.5, min(1.05, 0.65 + 0.15 * p + 0.05 * i + 0.015 * min(spr, 10.0)))\n    return e * r",
      "verify_expr": "realized_equity_by_position_spr(0.4, True, 5.0, True)",
      "verify_value": 0.37,
      "worked_example": "R=.65+.15+.05+.075=.925; .4*.925=.37"
    },
    {
      "category": "position_realization",
      "definition": "In-position EV premium = equity*pot*(R_IP-R_OOP).",
      "formula_plain": "premium=E*P*(R_ip-R_oop).",
      "function_name": "in_position_ev_premium",
      "inputs": "pot_size:float, raw_equity:float, realization_ip:float, realization_oop:float",
      "name": "In-position EV premium",
      "python_function": "def in_position_ev_premium(pot_size: float, raw_equity: float, realization_ip: float, realization_oop: float) -> float:\n    \"\"\"EV premium from position via realization gap. Example: P=100,E=.5,Rip=.9,Roop=.75 -> 7.5.\"\"\"\n    if pot_size < 0.0:\n        raise ValueError(\"pot must be nonnegative\")\n    e = max(0.0, min(1.0, raw_equity))\n    return pot_size * e * (realization_ip - realization_oop)",
      "verify_expr": "in_position_ev_premium(100.0, 0.5, 0.9, 0.75)",
      "verify_value": 7.5,
      "worked_example": "100*.5*(.90-.75)=7.5"
    },
    {
      "category": "position_realization",
      "definition": "Initiative value = pot*(fold_equity_gain + realization_gain*raw_equity).",
      "formula_plain": "IV=P*(ΔFE+ΔR*E).",
      "function_name": "initiative_value",
      "inputs": "pot_size:float, fold_equity_gain:float, realization_gain:float, raw_equity:float",
      "name": "Initiative value from fold equity and realization gain",
      "python_function": "def initiative_value(pot_size: float, fold_equity_gain: float, realization_gain: float, raw_equity: float) -> float:\n    \"\"\"EV value of initiative. Example: P=100, dFE=.08,dR=.10,E=.4 -> 12.\"\"\"\n    if pot_size < 0.0:\n        raise ValueError(\"pot must be nonnegative\")\n    e = max(0.0, min(1.0, raw_equity))\n    return pot_size * (fold_equity_gain + realization_gain * e)",
      "verify_expr": "initiative_value(100.0, 0.08, 0.10, 0.4)",
      "verify_value": 12.0,
      "worked_example": "100*(.08+.10*.4)=12"
    }
  ]
}