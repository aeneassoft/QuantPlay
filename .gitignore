# Secrets (real keys live OUTSIDE the repo in C:\Users\hampe\Desktop\Secret keys\)
*.key
*secret*
.env
.env.*

# Large / transient / regenerable
data/*
!data/preflop_strength.json
tools/
Information/
# retrieved LoRA adapters / nets (large binaries; keep out of git)
models/
*.pdf
*.tgz
*.zip
*.safetensors
# trained nets (advisor/turn/river/deepcfr_hunl) — regenerable, large binaries; never commit
*.pt
*.onnx
*.gguf

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor / OS / Claude-local
.claude/
.idea/
.vscode/
Thumbs.db
.DS_Store

# User-Handhistorien (persoenliche Daten, gross)
hand histories/

# ALLES, WAS NICHT AUSSCHLIESSLICH POKER IST, gehoert nicht in dieses Repository.
# Dieses Repo verankert ausschliesslich den Pokerbot und den Poker-Trainer.
# Nicht-Poker-Arbeiten liegen ausserhalb des Repos (bis 2026-09-10 im geloeschten
# Root-Ordner "#Anderes/"; seither getrennte Ordner ausserhalb von PokerB).
# Ein lokaler pre-commit-Hook (.git/hooks/, selbst nicht versioniert) blockiert
# die Alt-Pfade zusaetzlich -- auch gegen "git add -f".
/_privat/
/raw_*.json

# Handhistorien Dritter (CoinPoker-PLO eines anderen Spielers, personenbezogen): 2026-09-24 nach
# Desktop/PokerB_ausgelagert_2026-09-10/knowledge_base_hand_histories verschoben, nicht mehr versioniert
knowledge_base/hand_histories/players - handhistories/
