# Secrets (real keys live OUTSIDE the repo in C:\Users\hampe\Desktop\Secret keys\)
*.key
*secret*
.env
.env.*

# Large / transient / regenerable
data/
tools/
Information/
# retrieved LoRA adapters / nets (large binaries; keep out of git)
models/
*.pdf
*.tgz
*.zip
*.safetensors
# trained nets (advisor/turn/river/deepcfr_hunl) — regenerable, large binaries; never commit
*.pt
*.onnx
*.gguf

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor / OS / Claude-local
.claude/
.idea/
.vscode/
Thumbs.db
.DS_Store

# User-Handhistorien (persoenliche Daten, gross)
hand histories/

# Persoenliche Exkursionen: liegen sichtbar im Root unter _privat/ und sind
# nicht Teil dieses Repositories. Ein lokaler pre-commit-Hook (.git/hooks/,
# selbst nicht versioniert) blockiert sie zusaetzlich.
/_privat/
/raw_*.json
