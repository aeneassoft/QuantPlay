# CATALOG — the data map ("the gold")

_Generated by `python -m dataset.build_manifest` from `dataset/registry.py` — do NOT hand-edit; re-run to refresh. The training pipeline discovers data by ROLE via the registry (e.g. `registry.sft_gold()`), so this map and the training logic share ONE source of truth._

⚠️ = deprecated / empty / superseded (excluded from active-role queries like `sft_gold()`).

## SFT gold — the shards the Qwen brain trains on

| key | path | size | what it is |
|---|---|---|---|
| `shard.a_contract` | `dataset/shards/a_contract.jsonl` | 1,750 rows · 1.8M | highest-clarity reasoning-loop decisions; ~57% drive the trained advisor (api.solver_freq) |
| `shard.c_decide` | `dataset/shards/c_decide.jsonl` | 3,250 rows · 3.2M | reasoning-loop decisions + opponent-read conditioning |
| `shard.solver` | `dataset/shards/solver.jsonl` | 2,617 rows · 1.7M | TexasSolver GTO mixed-frequency decisions (curated board set) |
| `shard.solver_mass` | `dataset/shards/solver_mass.jsonl` | 25,586 rows · 16.2M | TexasSolver mass-solve GTO mixes (the local CPU mass_solve gold) |
| `shard.hu_blueprint` | `dataset/shards/hu_blueprint.jsonl` | 12,168 rows · 6.6M | HU 200bb near-Nash BLUEPRINT preflop (api.preflop_solve, decide_mix literals) + HU postflop — the flywheel fix for the GTOW HU preflop over-fold leak (the −43.30→? lever) |
| `shard.teacher` | `dataset/shards/teacher.jsonl` | —(missing) | distilled frontier-teacher DSL (gated; present only AFTER a teacher run) |

## Other shards (NOT in the active SFT gold)

| key | path | size | what it is |
|---|---|---|---|
| `shard.decisions` | `dataset/shards/decisions.jsonl` | 200 rows · 116.4K | from_selfplay default sample output (a_contract/c_decide are the staged gold) |
| `shard.local_kb` ⚠️ | `dataset/shards/local_kb.jsonl` | 11,599 rows · 10.7M | OLD blended KB shard (math+exploit+concepts) — the original frac_bad=0.97 cause; NOT the gold — _superseded for SFT by the reasoning-loop shards; kept for reference_ |
| `shard.sample` ⚠️ | `dataset/shards/sample.jsonl` | 253 rows · 256.2K | 253-row verification downsample of local_kb |
| `shard.b_ground` ⚠️ | `dataset/shards/b_ground.jsonl` | 0 rows · 0B | EMPTY placeholder (curriculum stage b) — _0 rows — from_calc/from_math not wired into the stage_ |
| `shard.d_exploit` ⚠️ | `dataset/shards/d_exploit.jsonl` | 0 rows · 0B | EMPTY placeholder (curriculum stage d) — _0 rows_ |

## Trained postflop advisors (.pt — api.solver_freq's gold)

| key | path | size | what it is |
|---|---|---|---|
| `advisor.flop` | `knowledge_base/postflop/advisor.pt` | 23.9K | trained flop GTO bet-frequency MLP (+63% vs strength-only) |
| `advisor.turn` | `knowledge_base/postflop/turn_advisor.pt` | 23.9K | trained turn GTO bet-frequency MLP (+46%) |
| `advisor.river` | `knowledge_base/postflop/river_advisor.pt` | 24.3K | trained river GTO bet-frequency MLP (+18%) |
| `advisor.defense` | `knowledge_base/postflop/defense_advisor.pt` | 25.8K | trained defense-frequency net (facing aggression) — _verify live wiring_ |

## Playbooks (exploit / postflop rules)

| key | path | size | what it is |
|---|---|---|---|
| `kb.playbook` | `knowledge_base/exploit/playbook.jsonl` | 11,520 rows · 7.2M | the exploit playbook — opponent-leak directives (large) |
| `kb.postflop_playbook` | `knowledge_base/postflop/openai_strategy.json` | 30.7K | the 62-rule postflop strategy playbook |

## Preflop ranges / blueprints

| key | path | size | what it is |
|---|---|---|---|
| `kb.preflop_blueprint` | `knowledge_base/ranges/preflop_blueprint.json` | 85.8K | near-Nash 6-max preflop blueprint (RFI / vs-raise) |
| `kb.pushfold` | `knowledge_base/cfr/preflop_pushfold.json` | 168.2K | Nash push-fold blueprint (HU endgame) |

## Math primitives

| key | path | size | what it is |
|---|---|---|---|
| `kb.formulas` | `knowledge_base/math/formulas.py` | 26.8K | engine-verified poker formulas = the DSL math primitives (pot-odds, MDF, SPR, outs) |

## Big training files (data/, gitignored — feed the advisor nets)

| key | path | size | what it is |
|---|---|---|---|
| `data.defense` | `data/defense_data.jsonl` | 806.1M | defense-strategy training sequences (feeds advisor.defense) — _~845MB, gitignored_ |
| `data.advisor` | `data/advisor_data.jsonl` | 84.8M | turn/river advisor training sequences — _gitignored_ |
| `data.river` | `data/river_data.jsonl` | 77.9M | river advisor training sequences — _gitignored_ |
| `data.sd_advisor` | `data/sd_advisor_data.jsonl` | 199,720 rows · 17.0M | short-deck advisor training sequences — _gitignored_ |

## Qwen models (the brain) + shelved nets

| key | path | size | what it is |
|---|---|---|---|
| `net.deepcfr_hunl` ⚠️ | `knowledge_base/postflop/deepcfr_hunl.pt` | 542.7K | Deep-CFR HUNL value net (SHELVED — the -212 fcpa era) — _superseded per CLAUDE.md history_ |
| `model.local_sft` | `models/qwen_local_sft` | 73 files · 2.5G | Qwen3-1.7B SFT adapter (the local frac_bad-fix proof) — _6 checkpoints_ |
| `model.local_grpo` | `models/qwen_local_grpo` | 31 files · 964.6M | Qwen3-1.7B GRPO adapter (local RL proof) |
| `model.poker_ckpt500` ⚠️ | `models/qwen_poker_ckpt500` | 3 files · 666.1M | early 8B LoRA checkpoint (historical) |
| `model.smoke` ⚠️ | `models/qwen_smoke` | 18 files · 554.3M | smoke-test adapter |
| `model.combined_sft` ⚠️ | `models/qwen_combined_sft` | 0 files · 0B | EMPTY placeholder dir — _empty_ |
| `model.sft_aligned` ⚠️ | `models/qwen_local_sft_aligned` | 18 files · 554.3M | EMPTY (aligned re-SFT was killed; the pod run superseded it) — _empty_ |

## Evaluation

| key | path | size | what it is |
|---|---|---|---|
| `kb.scorecard` | `knowledge_base/scorecard.json` | 2.9K | latest grounded scorecard (gto_gap / exploit_edge bb/100) |

## knowledge_base/ subdirs (summary)

| subdir | files | size |
|---|---|---|
| `knowledge_base/cfr/` | 1 | 168.2K |
| `knowledge_base/concepts/` | 2 | 1.5M |
| `knowledge_base/exploit/` | 11 | 7.5M |
| `knowledge_base/hand_histories/` | 43 | 14.2M |
| `knowledge_base/math/` | 17 | 1.7M |
| `knowledge_base/postflop/` | 8 | 671.6K |
| `knowledge_base/ranges/` | 11 | 2.5M |
| `knowledge_base/theory/` | 16 | 284.3K |

## Appendix — on-disk files not yet curated in the registry

- `dataset/shards/streets` (dir) — _uncurated; add to dataset/registry.py_
- `models/_overnight` (dir) — _uncurated; add to dataset/registry.py_
- `models/grpo_metrics.jsonl` (11.9K) — _uncurated; add to dataset/registry.py_
- `models/grpo_slim.tgz` (673.2M) — _uncurated; add to dataset/registry.py_
- `models/qwen_1.7b_aligned` (dir) — _uncurated; add to dataset/registry.py_
- `models/qwen_8b_local_sft` (dir) — _uncurated; add to dataset/registry.py_
- `models/qwen_poker_grpo` (dir) — _uncurated; add to dataset/registry.py_
- `models/qwen_poker_grpo.tgz` (673.3M) — _uncurated; add to dataset/registry.py_
- `models/qwen_poker_grpo_live.tgz` (674.7M) — _uncurated; add to dataset/registry.py_
- `models/qwen_poker_sft.tgz` (2.6G) — _uncurated; add to dataset/registry.py_
- `models/sft_slim.tgz` (673.2M) — _uncurated; add to dataset/registry.py_
- `knowledge_base/postflop/calibrated_priors.json` (43B) — _uncurated; add to dataset/registry.py_
- `knowledge_base/postflop/texture_freqs.json` (262B) — _uncurated; add to dataset/registry.py_
