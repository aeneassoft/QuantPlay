# TRAINER_PLAN.md — One-run implementation plan for the training program
### Generated 2026-07-08 from a 10-agent workflow (5× code recon → 4× design → adversarial verify). Verifier's verdict: **one-run executable = TRUE** (with the binding fixes incorporated below).

**For the executing session (Ultracode):**
- **Binding doctrine:** [../doctrine/TRAINER_DESIGN.md](../doctrine/TRAINER_DESIGN.md) — fairness grade bands (ok/teuer/leak, never grade results), warm tone, uncertainty hierarchy (preflop/river hard, flop/turn = "bot assessment"), simplicity (no accounts/gamification/mobile).
- **Order:** exactly the package order of this document (sorted dependency-correct by the verifier; P2-1 comes BEFORE P1-D so that all UI edits land in training.html).
- **Hard gate at the end of every phase:** the acceptance tests of the phase's autotest package must be green before the next phase begins. six.html stays byte-identical over the ENTIRE run (git diff --quiet -- pokerbot/web/static/six.html).
- **Base bot:** PRINCE v2.2 (POKERB_PRINCE=1). GTO mode = exploit OFF; exploit mode = exploit ON (LCB-gated deviations). Opponents at the table = the arena league (sixmax.py) — the PRINCE bot serves primarily as the GRADING ORACLE.
- **Recon ground truths** (the most important integration points, verified by the recon agents with file:line) are in Appendix A. Design steps cite these APIs; on contradiction the code prevails.

---

## P0-0 · Mode switch GTO/exploit (NEW — identified as a gap by the verifier)
**Files:** pokerbot/web/six_server.py · (UI toggle later in training.html via P2-1/P1-D)
**Steps:**
1. Extend `NewReq` by `mode: str = "gto"` (`"gto"` | `"exploit"`); validation in the handler.
2. `Session.__init__(stack, sb, bb, mode)` stores `self.mode`; in exploit mode the league bots are constructed with active opponent-model use (exploit_gain > 0), in GTO mode neutral.
3. `Session.view()` delivers `mode` to the UI; the session registry (P0-6) logs it.
4. The grader (P0) and the difficulty regulator (P3-D) read the mode from the session — NEVER from env.
**Acceptance:**
- TestClient: `POST /api/new_session {mode:"exploit"}` → view.mode == "exploit"; without mode → "gto".
- Invalid mode → 400.
**Size:** S


## P0-1 · Per-decision capture: record schema + six_server hook
> ★ BINDING FIX (phantom record): build the snapshot BEFORE t.act(), but append to _pending_decisions only AFTER a successful t.act() — t.act() raises the ValueError/RuntimeError that leads to the 400 (six_server.py:159-170); an illegal move must leave NO record.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/__init__.py · C:/Users/hampe/Desktop/PokerB/pokerbot/coach/decision_log.py · C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py
**Steps:**
1. Create pokerbot/coach/decision_log.py with `capture_decision(table, session_id, hand_no, mode, action, amount) -> dict`: build the pre-action snapshot at the recon'd hook — insert the call in Session.human_action (six_server.py:83-89) BETWEEN the street/to_call snapshot (lines 85-86) and `t.act(action, amount)` (line 87), where t.obs_for(HUMAN), t.legal_actions(), t.history, t.board, t.pot() are all pre-mutation (recon integration point #1).
2. Record schema `trainer.decision.v1` (per ../doctrine/TRAINER_DESIGN.md §4): {schema, session_id, hand_id: f'{session_id}-{hand_no}', ts, mode ('gto'|'exploit'), street, spot: dataclasses.asdict(spot_from_table(table, 0)) (pokerbot/brain/format_spot.py:81 — do NOT invent a new spot schema), obs: table.obs_for(0) (table.py:301), legal: table.legal_actions() verbatim (table.py:131-149), history: [h for h in table.history if it is a 'player' entry] (raw copy, needed by the P0-3 HU adapter), human_action: {action, amount}, spot_fp}. Grade fields (checks/oracle/grade/grade_typ/erklaerung_kurz/grade_ms) are filled at hand end by P0-5.
3. spot_fp = zlib.crc32(json.dumps({street, board, hero_hole, pot, to_call, line}, sort_keys=True).encode()) — crc32 of canonical JSON, NEVER Python hash() (PYTHONHASHSEED trap, CLAUDE.md clean-code note). This fp seeds the P0-3 oracle rng.
4. In six_server.Session.__init__ (six_server.py:28-38): add self.session_id (timestamp, same stamp as self.path), self.decisions_path = self.path.with_name('decisions_' + self.path.stem.removeprefix('session_') + '.jsonl') (parallel JSONL next to session_<sid>.jsonl — recon reuse #1), self._pending_decisions: list = []; clear it in start_hand.
5. In Session.human_action: append the captured record to self._pending_decisions (in-memory until hand end; NOT flushed here — single global SESSION dies on 'Neu', so P0-5 flushes every graded record via session_log.append_record (session_log.py:35-38) at hand end).
6. Amount semantics documented in the record: raise `amount` is the commit-TO total in chips (bb=100), matching legal_actions raise_min/raise_max (recon gotcha #5). 'allin' stored with the legal dict so the grader can reconstruct the effective size.
**Acceptance:**
- python -m tests.test_trainer_p0 --unit capture: TestClient(six_server.app) POST /api/new_session then POST /api/action with a legal action -> SESSION._pending_decisions has exactly 1 record; record has spot (asdict shape with street/board/pot/to_call/legal/line keys), obs, legal (with to_call/raise_min/raise_max/is_bet), history, human_action, spot_fp int.
- Capture latency: time.perf_counter around capture_decision < 5 ms (no equity/advisor/oracle call inside the capture path).
- Illegal action still returns 400 (six_server.py:159-170 ValueError path) and appends NO record (capture only runs when t.act will be reached — insert after validation, before act).
**Size:** M

## P0-3 · Oracle-diff: safe PRINCE HU oracle + SixMaxBot multiway oracle
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/oracle.py
**Steps:**
1. PrinceOracle (HU spots only, n_active==2): a DEDICATED instance per Session — oracle = PokerBot(hero_idx=0, seed=<fixed>, exploit=False) (bot.py:134) — the recon-recommended SAFE pattern proven in production by web/server.py:30-31 (separate play-bot + advisor-oracle, advisor called per human action at server.py:76). NEVER share it with a game bot, NEVER call observe_opponent/observe_hand_end on it (bot.py:1248-1276). exploit stays False in BOTH trainer modes — the human is graded vs the GTO baseline, not vs a Dirichlet read (recon gotcha: exploit clobber).
2. record_to_hu_state(rec) adapter modeled 1:1 on gtow_to_state (pokerbot/benchmark/gtowizard.py:85-147, the canonical precedent incl. its _selftest at 222-275): hero forced to index 0, villain hole ['??','??'], players = EXACTLY 2 dicts (hole/stack/committed_street/committed_total/all_in/folded), 'legal' from the stored legal dict, plus street/board/pot/bb/button/current_bet, history = the stored player entries with EVERY 'player' field remapped to 0/1 (_has_initiative bot.py:916 and _villain_barrels bot.py:498-514 parse them), and a STABLE state['hand_id'] = rec['hand_id'] (PRINCE ships LINE_U=1 — omitting hand_id hits the scalar _hand_u fallback bug, bot.py:137-141).
3. Reproducible sample: before decide(), set oracle.rng = random.Random(rec['spot_fp']) (fresh per-spot rng derived from the crc32 fp — the recon-sanctioned repeatable-grading recipe; decide() advances the Mersenne stream otherwise). Then dec = oracle.decide(state) (bot.py:190). oracle.use_resolver stays False in P0 (cold-cache river solve = seconds; also the sparse resolver rationale shape lacks equity/mdf).
4. Store oracle result: {source: 'prince_hu', action, amount, rationale} — keep the FULL rationale dict (bot.py:1242 _mk always adds 'reasoning'; path-dependent keys equity/required_equity/mdf/defense_advisor/advisor_pbet* are free explanation data for P1). Renderer-relevant note in a comment: three rationale families (preflop/postflop/sparse-resolver/deepcfr) exist.
5. SixMaxOracle (multiway, n_active>2 — PokerBot is HU-ONLY: villain hardcoded 1-hero_idx at bot.py:316/1260, players must be exactly 2): use SixMaxBot._decide/decide_6max on the stored obs (arena/sixmax.py:84, accepts an injected rng). Construct one module-level reference bot with the neutral 'tag' Knobs (PROFILES['tag'], sixmax.py:48-58) and call _decide(obs, rng=random.Random(rec['spot_fp'])) — per-spot seeding is deliberate HERE: a grader wants a deterministic reference sample; the sixmax.py:96-97 fake-mixing warning applies to PLAY, cite it in a comment. Import as `from pokerbot.arena.sixmax import OppModel as SeatOppModel` ONLY if needed — the OppModel name collision with strategy/opp_model.py is a known trap (recon gotcha).
6. diff(rec): oracle_action vs human_action — action match (exact verb, with bet/raise normalized via legal['is_bet']) and, on both-aggressive, sizing distance in commit-TO totals (recon gotcha: compare street-commitment targets, not chips-added). Output {match: bool, oracle_action, size_diff_frac}.
7. Validate any synthesized action through api.legalize(spot, action, size_bb) (api.py:469) before storing — cheap legality assert.
**Acceptance:**
- python -m tests.test_trainer_p0 --unit oracle: build a synthetic HU record (SB vs BB, flop, facing 0.5x pot) -> record_to_hu_state passes the same locked asserts style as gtowizard._selftest (players==2, hero idx 0, villain hole hidden, to_call consistent with committed deltas, hand_id present); oracle.decide returns action in the legal set (api.legalize round-trip).
- Reproducibility: two grade passes over the same record yield the identical oracle action+amount (per-spot rng from spot_fp).
- Multiway record (4 active preflop) routes to SixMaxOracle, never constructs a PokerBot state; HU record routes to PrinceOracle.
- Safety: grep-level assert in the test — pokerbot/coach/oracle.py contains no call to observe_opponent/observe_hand_end and the oracle instance is not placed in Session.bots.
**Size:** M

## P0-2 · pokerbot/coach/grader.py — deterministic math checks
> ★ BINDING FIX: `ALL_169_CLASSES` does NOT exist in the repo. Generate the 169 list (13 pairs + 78 suited + 78 offsuit) locally via a mini generator or add it as a helper in ranges.py — do not import.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/grader.py
**Steps:**
1. Module header, BEFORE any strategy import: os.environ.setdefault('POKERB_PRINCE','1'); from pokerbot.strategy import gto_mode; gto_mode.apply() — flags are read at IMPORT time by postflop.py/advisor.py (gto_mode.py:122, recon gotcha). NEVER set the v3+ default-OFF flags (PAIR_DEFENSE/RIVER_DEFENSE/PURIFY/RAISE_NARROW/OVERBET_MENU/TURN_DEF_ADVISOR — gto_mode.py:56-66).
2. check_pot_odds(rec): only when spot['to_call'] > 0 (formulas raise ValueError at C=0 — guard exactly like understanding.py's spot.to_call>0 branch). req = api.required_equity(spot['to_call'], spot['pot']) (api.py:63) — spot.pot ALREADY contains villain's bet, this is the audited convention of understanding.py:76-77. Optimistic equity bound: eq_max = api.equity(hero_hole, ALL_169_CLASSES, board, iters=600, seed=0) (api.py:29 — pass iters EXPLICITLY, never rely on the global modes.current(); seed=0 = deterministic). Guard math.isnan(eq_max) (equity_vs_* return NaN on dead ranges, equity.py:15-120). Hard violation = human CALLED and eq_max + 0.05 < req — calling without the odds even vs any-two is 'pot odds clearly violated' per fairness doctrine §1.2 and respects §1.4 range tolerance (no exact-range assumption). Store {req, eq_max, violated}.
3. check_mdf(rec): pot_pre = spot['pot'] - spot['to_call']; mdf = api.mdf(spot['to_call'], pot_pre) (api.py:75 — argument order (bet, pot); pot argument is the PRE-bet pot, the understanding.py:79-81 reference usage; getting this wrong skews every grade by the bet size — recon gotcha #1). Per-decision hard proxy for 'gross MDF violation': human FOLDED a made hand with api.hand_rank(hole, board)[1] >= 0.62 (STRONG_MADE, understanding.py:26) to size_faced <= 0.5 pot. Always store {mdf, size_faced, strength, violated}.
4. check_sizing(rec): only on human bet/raise. If legal['is_bet']: snapped = snap_to_tree(bet_chips, pot, street) (postflop.py:49, bet_chips = amount - obs['my_committed_street']); else snapped = snap_raise_to_tree(amount, cur_bet, pot, to_call, street) (postflop.py:60). Grade err = |human_frac - snapped_frac| of the relevant pot; hard geometry violation only for err > 0.5 pot-fractions (e.g. min-clicking a river where the tree floor is 0.75x, or >2.5x-pot spew). Store {human_frac, snapped_frac, err, violated}. Pure arithmetic, zero latency.
5. check_advisor(rec): postflop only. role = 'IP'/'OOP' via understanding._in_position(spot) (understanding.py:41 — importable private helper, recon reuse). Facing bet: guard advisor.defense_available() (advisor.py:140) then dist = advisor.p_defense(hole, board, role, size_faced=to_call/pot_pre, street) (advisor.py:144, softmax (P_fold,P_call,P_raise)). Bet/check node: guard advisor.available(street) (advisor.py:74) then pb = advisor.p_bet(hole, board, role, street) (advisor.py:87), dist = {bet: pb, check: 1-pb}. None => store {available: False} and SKIP the frequency check — 'no reference', NEVER treat None as 0 (recon gotcha #2). p_advisor(chosen_action) is the P0-4 mixed-support input. No preflop advisor exists and api.preflop_mix returns None on 6-max (api.py:130) — preflop rec gets {available: False, reason: 'kein 6-max Preflop-Advisor'}.
6. grade_decision(rec) -> rec: runs all four checks + the P0-3 oracle diff, wraps EACH in try/except storing {'error': repr(e)} in the check dict — a grader bug must never break play, but per the recon swallow-trap warning it must be VISIBLE in the record, never silent. Records grade_ms via perf_counter.
**Acceptance:**
- python -m tests.test_trainer_p0 --unit math (check-registry style of tests/test_math_suite.py:23-40, seed 42, exit 1 on FAIL): api.required_equity(50,150)==0.25; api.mdf(b,p) == knowledge_base.math.formulas.minimum_defense_frequency(p,b) for 3 (b,p) pairs; check_pot_odds on a to_call=0 spot returns without calling formulas (no ValueError); NaN range -> check stores available/violated=False, no exception.
- Determinism: grade_decision twice on the same captured record produces byte-identical checks dicts (api.equity seed=0, iters explicit).
- Advisor guard: monkeypatch advisor.available/defense_available to return False -> check_advisor stores {available: False} and grade still computes (falls through to math+oracle).
- Constructed leak spot (river, hero 72o unimproved, to_call=pot) with human_action=call -> check_pot_odds.violated == True.
**Size:** M

## P0-4 · Grade bands + per-street confidence labels (fairness doctrine)
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/grader.py
**Steps:**
1. assemble_grade(checks, oracle_diff, street, n_active) -> {grade, grade_typ, confidence, erklaerung_kurz} implementing ../doctrine/TRAINER_DESIGN.md §1.2 EXACTLY: (a) LEAK (✗) ONLY on hard math violations — check_pot_odds.violated OR check_mdf.violated OR check_sizing.violated ('pot odds clearly violated, gross MDF violation, sizing-geometry error'); (b) OK (✓) if the advisor frequency of the chosen action >= 0.15 (mixed support — from check_advisor's p_defense triple or p_bet split, the recon-mandated direct-mix read, NOT repeated decide() sampling) OR oracle_diff.match OR (both-aggressive and size_diff_frac <= 0.25); when multiple actions carry >=0.15 support, erklaerung_kurz SAYS so ('GTO mischt hier: 60% Bet / 40% Check — beides gut', doctrine §1.2); (c) everything else = TEUER (~). grade_typ names the deciding check ('pot_odds'|'mdf'|'sizing'|'advisor_freq'|'oracle_diff').
2. Confidence labels per doctrine §1.3, HONEST to what P0 actually has (recon: no 6-max preflop blueprint, no resolver in P0): CONFIDENCE map — math-check-decided grades -> 'Mathe (unanfechtbar)'; advisor-frequency-decided -> 'Solver-Frequenz (HU-trainiert, Näherung)' (the advisor is a soft 10-20%-band reference, never hard right/wrong — ADVISOR_ROLE_POS caveat, recon gotcha #3); oracle-diff-decided -> 'Bot-Einschätzung'. Per-street: preflop 6-max carries 'Bot-Einschätzung' (api.preflop_mix returns None on 6-max, api.py:130); river stays 'Bot-Einschätzung' until the resolver lands post-P0 — the trainer that names its limits is more credible (§1.3).
3. Range tolerance (§1.4): no check may compare vs the exact tracker range — the pot-odds bound uses the any-two optimistic range (P0-2) by construction; encode this as a module docstring rule.
4. Advisor unavailable (available: False) => the frequency criterion is simply absent; the band falls back to math + oracle and confidence degrades one step — never a worse grade because a reference was missing.
5. erklaerung_kurz: terse deterministic German one-liner from the deciding numbers (e.g. 'Call brauchte 33% Equity, selbst gegen jede Hand nur 24% — teurer Kauf'), tone per §1.5: a leak is called 'teurer Kauf' (expensive purchase), every criticism carries the alternative (oracle_action) + one-sentence reason. Full template renderer is P1; P0 ships only this one-liner.
6. Write grade/grade_typ/confidence/erklaerung_kurz into the decision record (schema of P0-1).
**Acceptance:**
- python -m tests.test_trainer_p0 --unit bands: constructed fixtures — (1) call with eq_max<req-0.05 -> grade='leak', grade_typ='pot_odds', confidence contains 'Mathe'; (2) check where p_bet=0.60 and human checked (0.40 support) -> grade='ok' and erklaerung_kurz contains both percentages; (3) advisor None + oracle mismatch + no math violation -> grade='teuer', confidence='Bot-Einschätzung'; (4) fold of a 0.70-strength hand vs 0.4x-pot bet -> grade='leak', grade_typ='mdf'.
- Never-leak-without-math invariant: fuzz 200 random records with all violated=False -> zero 'leak' grades (leak requires a hard math check by doctrine).
- Every produced record has all four fields grade/grade_typ/confidence/erklaerung_kurz non-empty.
**Size:** S

## P0-5 · Hand-end grading wiring + latency budget
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py · C:/Users/hampe/Desktop/PokerB/pokerbot/coach/grader.py
**Steps:**
1. Strategy (grading must not stall play): capture is sync+cheap (<5ms, P0-1); ALL scoring runs at hand end inside Session._log_if_done (six_server.py:40-47 — the single call site at the end of every advance(), recon integration point #4): for each rec in self._pending_decisions run grader.grade_decision(rec) then session_log.append_record(self.decisions_path, rec) (session_log.py:35-38); clear the list. The client auto-advances 2400 ms after hand end (six.html:224 advTimer) — the whole-hand grading budget is 800 ms, comfortably inside that window, so the user never waits.
2. Per-check budgets making 800 ms trivially safe: equity iters=600 explicit (~tens of ms flop/turn, river = EXACT enumeration at zero MC cost, equity.py:23/50-60/92); advisor p_bet/p_defense sub-ms after warmup (120k-entry memo, advisor.py:82-98); oracle decide() ~83 ms post-memoization (commit 114e107); snap_to_tree/api.mdf pure arithmetic. use_resolver/solve_node FORBIDDEN in this path (6-30 s, api.py:392 — async deep-analysis only, later phase).
3. Pre-warm at server start, NOT per session: in six_server.main() (six_server.py:188-202) before uvicorn.run, call grader.prewarm() -> advisor.available('flop'), available('turn'), available('river'), defense_available() (one-time ~1-2 s torch load, recon gotcha #4) + one throwaway PokerBot construction (module-level MLP/blueprint caches are shared read-only).
4. Instrument: each record gets grade_ms; _log_if_done logs a WARNING line when a hand's total grading exceeds 800 ms (visible in server output — the advance() bare-except at six_server.py:66-70 must NOT wrap the grading call; grading has its own per-check try/except from P0-2 so a grader bug degrades to an error-annotated record, never a crashed hand).
5. Ordering guarantee: _pending_decisions grades against the PRE-action snapshots taken in human_action — no re-derivation from post-hand table state (the table has mutated).
**Acceptance:**
- python -m tests.test_trainer_p0 --e2e: TestClient drives new_session -> scripted actions (policy reads view.legal: call when can_call else check else fold; every 5th action a raise to raise_min) across 20 hands via POST /api/hand (CLAUDE.md: verify via TestClient, never screenshots) -> decisions_*.jsonl exists, one graded line per human decision, every line json.loads-able with grade in {ok,teuer,leak}.
- Latency: max per-hand grading wall time over the 20 hands < 800 ms; median per-decision grade_ms < 250 ms (measured from the recorded grade_ms fields).
- Robustness: monkeypatch check_advisor to raise -> hands still complete (200 responses), records carry {'error': ...} in the advisor check, grade still assigned from remaining checks.
- Grade-distribution sanity (autotest doctrine §7-lite): a call-everything policy over 30 hands produces >=1 'leak' with grade_typ='pot_odds'; a fold-unless-free policy produces zero pot_odds leaks.
**Size:** S

## P0-6 · Session registry (append-only)
> ★ BINDING: the registry logs the MODE per session from the request (see P0-0), not from an env variable.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/registry.py · C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py
**Steps:**
1. pokerbot/coach/registry.py: REGISTRY_PATH = config.DATA_DIR/'trainer'/'registry.jsonl'; register_start(session) and register_end(session) both implemented as session_log.append_record(REGISTRY_PATH, row) (session_log.py:35-38 — reuse verbatim, recon reuse #1; append-only per ../doctrine/TRAINER_DESIGN.md §4 'Append-only + Session-Registry'). A reader `sessions()` folds start/end rows by session_id (last-writer-wins).
2. Start row (written in Session.__init__, six_server.py:28-38): {event:'start', session_id, ts, mode (os.environ.get('POKERB_TRAINER_MODE','gto') — the exploit toggle itself is P3), stack_bb, session_log_path: str(self.path), decisions_path: str(self.decisions_path), profile_assign: {1:'tag',2:'lag',3:'nit',4:'station',5:'maniac'} (the hardcoded dict at six_server.py:36-38 — recorded so the P2 difficulty controller has provenance), fingerprint: gto_mode.fingerprint() (gto_mode.py:135 — QA doctrine: every graded session logs the exact flag set, needed for the GTOW-Analyzer second opinion of §4)}.
3. End row (written in /api/analyze at six_server.py:180-185 and in Session.__del__-free fashion: also when a NEW Session replaces the global — the 'Neu' button wholesale-replaces SESSION (six_server.py:125, recon gotcha #1), so new_session() (six_server.py:143-148) first calls register_end(old SESSION) if one exists): {event:'end', session_id, ts, hands_done, human_net} (both already tracked on Session, six_server.py:34-35).
4. In-memory trainer state persists NOTHING beyond these JSONLs — Session death is lossless (registry + decisions + session log are the truth).
**Acceptance:**
- python -m tests.test_trainer_p0 --unit registry: new_session -> registry.jsonl gains a start row with a fingerprint dict containing the PRINCE flags; play 3 hands, POST /api/new_session again -> an end row for the first session_id with hands_done==3 exists BEFORE the second start row.
- registry.sessions() returns both sessions, first marked ended with correct decisions_path pointing at an existing file.
- Registry write failure (monkeypatch append_record to raise) does not break /api/new_session (200 returned, warning logged).
**Size:** S

## P0-7 · P0 autotest harness (mandatory gate before human use)
**Files:** C:/Users/hampe/Desktop/PokerB/tests/test_trainer_p0.py
**Steps:**
1. Structure per the repo's most disciplined harness, tests/test_math_suite.py:23-40 (recon reuse): @check(name) decorator -> CHECKS registry, deterministic seed 42, fast default, --json PATH output, exit code 1 on any FAIL; invoked as python -m tests.test_trainer_p0 (NO pytest anywhere in this repo — recon gotcha). Flags --unit capture|math|oracle|bands|registry and --e2e select subsets; no flag runs everything.
2. Unit checks: all acceptance items of P0-1..P0-4 and P0-6 registered as @check functions (they are enumerated in those packages).
3. E2E check: fastapi.testclient.TestClient(six_server.app) (CLAUDE.md convention) with a tmp DATA_DIR redirect (monkeypatch config.DATA_DIR or the Session paths) — drives the full P0-5 e2e including latency and grade-distribution sanity (call-station policy graded worse than fold-discipline policy = the §7 'station worse than tag' sanity in P0-reachable form).
4. Environment check: assert gto_mode.fingerprint() shows PRINCE profile active and POKERB_EXPLOIT resolves to '0' inside the grader process, and assert none of the excluded v3+ flags (POKERB_PAIR_DEFENSE etc.) are set — pins the v2.2 anchor.
5. Print a one-line PASS summary with counts + max grade_ms, so future phases can gate on this harness in CI-style runs.
**Acceptance:**
- python -m tests.test_trainer_p0 exits 0 with all checks PASS on a clean checkout with POKERB_PRINCE unset beforehand (the grader module sets it itself).
- python -m tests.test_trainer_p0 --json out.json writes machine-readable per-check results.
- Deliberately breaking the MDF convention (swap to api.mdf(to_call, spot.pot)) makes the math unit check FAIL (guards the #1 pot-convention footgun).
**Size:** M

## P1-A · Glossary registry + auto-markup (pokerbot/coach/glossar_de.py)
> ★ BINDING (glossary unification): glossar_de.py is the SINGLE source of truth (GLOSSAR dict + term()/markup(), schema {begriff, synonyme, erklaerung, formel?, quelle?}). P2-2 CONSUMES it (GET /api/glossary serves glossar_de.GLOSSAR as JSON). NO separate static/glossar.json, no second registry, no second markup mechanism.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/glossar_de.py · C:/Users/hampe/Desktop/PokerB/tests/test_glossar.py
**Steps:**
1. Create pokerbot/coach/glossar_de.py with module-level GLOSSAR: dict[str, dict]. Entry schema: {begriff: str, synonyme: list[str] (German+English surface forms incl. plurals, e.g. 'MDF' -> ['MDF','Minimum Defense Frequency','Mindest-Verteidigungsfrequenz']), erklaerung: str (ONE line plain German, <=220 chars), formel: str|None, quelle: str|None}. formel/quelle for math terms come from the audited docstrings in knowledge_base/math/formulas.py (compute_pot_odds:7, minimum_defense_frequency:150, breakeven_bluff_percentage:594, bluff_to_value_and_frequencies:183, required_future_winnings_for_implied_odds:259, required_fold_equity:322, outs_to_equity_rule_2_and_4:374, compute_spr:439, equity_realization:624, count_hand_combos_with_blockers:473).
2. Populate >=45 term ids covering EVERYTHING the P1 templates and report can emit (derived from the recon rationale-key enumeration at bot.py:1242 and understanding.py concepts): MATH: equity, pot_odds, required_equity, mdf, spr, outs, implied_odds, fold_equity, alpha_breakeven_bluff, blocker, equity_realization, ev, varianz. ACTIONS/LINES: cbet, donk_bet, barrel, probe_bet, check_raise, threebet, fourbet, open_raise, limp, overbet, thin_value, value_bet, bluff, bluffcatcher, slowplay, allin_jam, pot_control. CONCEPTS: range, position_ip_oop, initiative, board_textur, draw, made_hand, kicker, mixing (mixed strategy), gto, exploit, sizing, hand_class_percentile, showdown, wtsd, vpip, pfr, aggression_factor, advisor_frequenz, resolver, blueprint, effective_stack. Each erklaerung in warm plain German (fairness doctrine tone, ../doctrine/TRAINER_DESIGN.md par.1.5).
3. def get(term_id: str) -> dict — KeyError on unknown id. def all_terms() -> list[dict] sorted by begriff, each entry + its id (for the UI panel / GET /api/glossar).
4. def term(term_id: str, text: str|None = None) -> str — returns '<span class="term" data-term="<id>">' + html-escaped (text or begriff) + '</span>'; raises KeyError for unregistered ids. This is THE completeness enforcement: templates only emit terms through term(), so an unregistered term crashes the coverage test instead of shipping.
5. def markup(text: str) -> str — auto-markup for free prose (session report): (1) html.escape the input FIRST, (2) one compiled alternation regex over ALL synonyms sorted longest-first with word boundaries (\b, case-preserving match), (3) wrap only the FIRST occurrence of each term per text (noise control), (4) never touch text already inside a produced span (single left-to-right pass guarantees this). Must be idempotent: markup(markup(x)) == markup(x) — achieved by skipping any region already matching the span pattern.
6. Create tests/test_glossar.py as a __main__ module (repo convention: python -m tests.<name>, NO pytest — pattern tests/test_math_suite.py:23-40 @check registry, exit 1 on FAIL): checks = (a) every entry has non-empty begriff+erklaerung, erklaerung single-line <=220 chars; (b) no synonym string is claimed by two different term ids; (c) markup('Deine Pot Odds und die MDF') yields exactly 2 term spans with ids pot_odds and mdf; (d) idempotency markup(markup(x))==markup(x) on 5 mixed samples; (e) markup('<script>alert(1)</script> Equity') html-escapes the script tag AND marks Equity; (f) term('nonexistent_xyz') raises KeyError.
**Acceptance:**
- python -m tests.test_glossar exits 0 with all checks PASS
- GLOSSAR has >=45 entries; python -c "from pokerbot.coach import glossar_de; assert len(glossar_de.GLOSSAR)>=45" passes
- markup() is idempotent and html-escapes its input (checked by tests c/d/e)
**Size:** M

## P1-B · Deterministic German templates (pokerbot/coach/templates_de.py)
> ★ BINDING: exports the symbol TERMS_USED (exactly this name — P2-2 checks against it) and render_hand_feedback/render_decision_feedback returning {text, html, terms}.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/templates_de.py · C:/Users/hampe/Desktop/PokerB/tests/test_templates_de.py · C:/Users/hampe/Desktop/PokerB/tests/fixtures_p1_rationale.py
**Steps:**
1. Create pokerbot/coach/templates_de.py. Import the SHARED thresholds from pokerbot.brain.understanding (RIVER_BET_MEDIAN_X, SPR_COMMITTED, SPR_DEEP, STRONG_MADE, MEDIUM_MADE — understanding.py:21-26) and the helpers understanding._in_position/_effective_stack_bb so grader math and UI text agree (recon reuse mandate). Import pokerbot.coach.glossar_de as G. Do NOT import pokerbot.strategy.advisor (keeps torch out of the render path).
2. Numbers->comparisons helpers: freq_vergleich(p: float) -> str ('praktisch nie', 'selten (etwa 1 von 10)', 'etwa jede vierte', 'etwa jede dritte', 'etwa jede zweite', 'meistens', 'fast immer' — banded, deterministic); pot_frac_de(frac: float) -> str ('ein Viertel Pot', 'ein Drittel Pot', 'halber Pot', 'zwei Drittel Pot', 'Pot-Groesse', f'Overbet ({frac:.1f}x Pot)'); equity_satz(eq, req) -> str ('du gewinnst etwa 40 von 100 Laeufen — du brauchst nur 25'). Percentages appear ONCE as a number, then as comparison (doctrine: numbers -> comparisons).
3. INPUT CONTRACT (pin it in the module docstring): a decision record per ../doctrine/TRAINER_DESIGN.md par.4 (BINDING): {hand_id, ts, street, spot_fp, state_kompakt, human_action, oracle_action, advisor_dist, equity, pot_odds, mdf, grade, grade_typ, erklaerung_kurz, mode, rationale}. grade vocabulary PINNED: {'ok','teuer','leak'} (icons ✓/～/✗); unknown grade -> ValueError. Every field access via .get() with graceful German fallback (key-tolerance = P0-drift armor).
4. Per-rationale-family renderers covering ALL FOUR shape families from the recon enumeration (bot.py:1242): (1) preflop base + blueprint/push-fold/bb_vs_open/vs_3bet (keys hand_class, percentile, blueprint.dist, equity_vs_shove, required_equity, realization); (2) postflop facing-bet (equity, required_equity, mdf, call_threshold, defense_advisor=[pf,pc,pr], made_hand, texture) and bet-side (advisor_pbet/advisor_pbet_turn/advisor_pbet_river, slowplay, turn_probe); (3) SPARSE resolver shape {phase, street, hand, made_hand, board, range_conf, resolver: True} — NO equity/mdf keys exist, render 'River-Urteil vom Resolver (harte Referenz)' from made_hand alone; (4) deepcfr {phase:'deepcfr', hand, street, probs}. Unknown phase -> generic fallback line + stderr warning (never crash, never empty).
5. render_decision_feedback(rec: dict) -> {'text': str, 'html': str, 'terms': list[str]} — ONE decision, one to two sentences: played vs recommended action + one-sentence reason keyed by grade_typ (pot_odds -> equity_satz; mdf -> 'MDF sagt: mindestens X% verteidigen'; sizing -> pot_frac_de comparison played vs snap). Used later by the P2 replay walkthrough — build it now, it is the atom of render_hand_feedback.
6. render_hand_feedback(decisions: list[dict], hand_result: dict|None, mode: str) -> {'html','text','terms'} — 3 TO 5 LINES enforced by assert: L1 neutral hand frame (street reached, pot in bb; FORBIDDEN: result words like 'leider verloren'/'gut gewonnen' — anti-results-orientation, doctrine par.1.1); L2 best decision EXPLICITLY celebrated if an 'ok' exists (warm tone, doctrine par.1.5); L3 most important non-ok decision as 'teurer Kauf' + alternative (oracle_action) + one-sentence reason via render_decision_feedback; L4 optional mixing hint if advisor_dist has >=2 actions with >=0.15 support ('GTO mischt hier: 60% Bet / 40% Check — beides gut', doctrine par.1.2); L5 optional takeaway sentence keyed by grade_typ. Uncertainty label per doctrine par.1.3: preflop/river verdicts 'hart', flop/turn advisor-based ones labeled 'Bot-Einschaetzung'. If decisions is empty: one warm line ('Keine Hero-Entscheidung zu bewerten — Fold preflop ist oft der beste Kauf.').
7. EVERY poker/math term in any template string goes through G.term(id, anzeige_text) — never raw. Module-bottom: TERMS_USED: frozenset[str] = the literal set of every term id any template can emit (grep-able single source; the static completeness check reads it).
8. GERMAN-ONLY rule: strategic_read (understanding.py:32) output is English and is NEVER surfaced verbatim (recon gotcha); geometry sentences are re-rendered German from the record's numeric fields using the audited pot conventions of understanding.py:76-81 (required_equity uses pot AS-FACED; MDF uses pot_pre = pot - to_call).
9. Create tests/fixtures_p1_rationale.py: hand-built deterministic fixture records for EVERY family/grade combination — preflop blueprint, preflop bb_vs_open, push/fold, postflop facing-bet with defense_advisor, postflop bet-side with advisor_pbet, sparse resolver, deepcfr; each grade ok/teuer/leak at least once; plus one record with advisor_dist mixing-support and one empty-decisions hand. Field values follow the recon key enumeration exactly (this doubles as the executable spec of the P0 record).
10. Create tests/test_templates_de.py (__main__, @check registry pattern, exit 1 on FAIL): (a) TEMPLATE-COVERAGE: every fixture renders non-empty German text (all 4 rationale families — the P4-autotest criterion 'every rationale path renders' pre-satisfied); (b) 3<=lines<=5 on every hand render; (c) DETERMINISM: two renders byte-identical; (d) GLOSSAR-COMPLETENESS dynamic: parse all data-term ids out of every rendered html -> each resolves via glossar_de.get() (KeyError = FAIL); (e) static: TERMS_USED <= set(GLOSSAR); (f) no English leak markers ('STRATEGIC READ', 'in position', 'equity to call') in any output; (g) forbidden result-orientation words absent from L1; (h) unknown grade raises ValueError; (i) sparse-resolver fixture renders WITHOUT KeyError despite missing equity/mdf.
**Acceptance:**
- python -m tests.test_templates_de exits 0 — includes the mandated TEMPLATE-COVERAGE test (all 4 rationale shape families render) and the GLOSSARY-COMPLETENESS test (every emitted data-term id resolves in GLOSSAR, both dynamically parsed and statically via TERMS_USED)
- Rendered hand feedback is always 3-5 lines, deterministic, German-only, with warm-tone L2 celebration when an 'ok' decision exists
- python -c "from pokerbot.coach import templates_de" imports without loading torch (no pokerbot.strategy.advisor import)
**Size:** L

## P1-C · Per-hand feedback API + glossary endpoint (six_server)
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py · C:/Users/hampe/Desktop/PokerB/tests/test_feedback_api.py
**Steps:**
1. Session gains self.last_feedback: dict|None = None (init near six_server.py:34-35 where human_net/hands_done live). In _log_if_done (six_server.py:40-47), directly after append_record(build_hand_record(t)): collect this hand's P0 decision records — primary source: the in-memory per-hand list the P0 logger keeps on Session (P0 hook sits in human_action between lines 86 and 87); fallback: read the tail of the P0 decisions JSONL (same-dir sibling of SESSION.path) filtered by hand_id — then lazy-import pokerbot.coach.templates_de and set self.last_feedback = {'hand_no': t.hand_no, 'mode': <'gto'|'exploit' from trainer config>, **templates_de.render_hand_feedback(decisions, t.result, mode)}. Wrap in try/except -> on any render error store a one-line fallback feedback and log to stderr (never break the deal loop).
2. view() (six_server.py:91-122): add key 'coach': self.last_feedback if (hand over and feedback exists) else None — the frontend gets feedback in the SAME hand-ending POST response, zero extra roundtrip (recon: extend THIS dict, no second channel).
3. New route POST /api/feedback (pattern of analyze(), six_server.py:180-185, lazy imports): 400 {'error':'no session'} if SESSION None; 400 {'error':'no completed hand'} if last_feedback None; else JSONResponse(SESSION.last_feedback). PINNED response shape: {hand_no:int, mode:str, html:str, text:str, grades:[{street, human_action, oracle_action, grade, grade_typ}], terms:[str]} — grades list built from the same decision records in step 1.
4. New route GET /api/glossar: returns {'terms': glossar_de.all_terms()} (each entry {id, begriff, erklaerung, formel, quelle}); JSONResponse with ensure_ascii=False semantics (FastAPI default JSONResponse handles unicode — verify umlauts survive in the test).
5. Create tests/test_feedback_api.py (__main__, fastapi.testclient.TestClient per CLAUDE.md — NOT the screenshot tool): (a) POST /api/feedback before new_session -> 400; (b) new_session -> loop: read view.legal, POST /api/action with fold/check/call until hand_over (bounded 50 iterations guard); (c) the hand-ending action response contains 'coach' with html+text; (d) POST /api/feedback returns 200, html contains class="term" spans, EVERY data-term id appears in GET /api/glossar terms; (e) grades list entries have the 5 pinned keys; (f) an umlaut ('ä'/'ö'/'ü') round-trips intact through /api/glossar.
**Acceptance:**
- python -m tests.test_feedback_api exits 0 (TestClient end-to-end: new_session -> actions -> hand end -> feedback with resolvable glossary terms)
- POST /api/feedback response matches the pinned shape {hand_no, mode, html, text, grades[], terms[]}; error cases return 400 with error text, never 500
- Existing routes unchanged: /api/new_session, /api/action, /api/analyze still pass their current behavior in the same test run (regression asserts in test_feedback_api)
**Size:** S

## P1-E · Session report on our own logs (pokerbot/coach/trainer_report.py)
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/trainer_report.py · C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py · C:/Users/hampe/Desktop/PokerB/tests/test_trainer_report.py · C:/Users/hampe/Desktop/PokerB/tests/fixtures_p1_session.jsonl
**Steps:**
1. report(session_path) -> dict: (1) stats = pokerbot.analysis.session_analysis.compute_stats(recs) (existing, session_analysis.py:21 — the zero-work baseline); (2) stats_reich = the richer stat block (per-metric n) computed over the same hand records; (3) report_html: deterministic German sections (Ueberblick / Preflop / Postflop-Aggression / Sizing-Verteilung+Entropie / Blind-Defense / Bluff-Anatomie) — prose through glossar_de.markup(), numbers in small HTML tables; per-metric n ALWAYS shown, n<20 gets '(noch wenig Daten)' instead of hiding (fairness: the trainer names its limits); NO loss-word bans (the trainer states EV losses plainly).
2. Extend POST /api/analyze (six_server.py:180-185): keep the existing {stats, narrative} keys byte-compatible, ADD {'stats_reich': ..., 'bluffs': ..., 'report_html': ...} via lazy import of trainer_report; on adapter failure return the old payload plus {'report_error': str} — never 500.
3. Create tests/fixtures_p1_session.jsonl: 5 hand-built records in exact build_hand_record schema — one showdown win, one hero preflop fold, one hero allin, one multi-street hand with a raise (for the to-total delta math), one hand where a bot wins uncontested.
4. Create tests/test_trainer_report.py (__main__, exit 1 on FAIL): (a) stats_reich hand-count/vpip/pfr match the fixture by manual count; (b) report_html non-empty, every data-term id from markup resolves in GLOSSAR; (c) report builds OFFLINE (no ANTHROPIC key -> session_analysis.narrative fallback '' — graceful); (d) TestClient regression: POST /api/analyze on a fresh played session still returns stats+narrative AND the new keys.
**Acceptance:**
- python -m tests.test_trainer_report exits 0 — incl. the glossary-completeness check on report_html
- POST /api/analyze is backward compatible (old keys unchanged) and now carries stats_reich + report_html; works offline without an Anthropic key
**Size:** M

## P2-1 · training.html scaffold + GET /training route + 2560x1440 Snowie layout (table ~60% / coaching panel ~40%)
> ★ BINDING: training.html is created as a copy of the UNCHANGED six.html and then rebuilt; six.html stays byte-identical (P2-7 gate: git diff --quiet). An empty <details id="gloss"> placeholder already belongs in this package so that its own acceptance passes.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/web/static/training.html · C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py
**Steps:**
1. DECISION (per recon caching gotchas): do NOT rework six.html — create NEW pokerbot/web/static/training.html and serve it from six_server.py, because six_server already uses the read-fresh-per-request pattern (six_server.py:137-140 index() re-reads the file every GET; the module-level _INDEX at line 24 is dead code — do not use it). This gives restart-free UI iteration for the whole P2 build, keeps the plain game at GET / untouched, and keeps the trainer in the SAME process where P0's decision-logger hook lives (Session.human_action, six_server.py:83-89). Only ONE server restart is needed when the new Python routes land; all later HTML/JS edits are refresh-only. Do NOT create a separate trainer server module.
2. Add route in six_server.py after index() (line ~140): @app.get('/training', response_class=HTMLResponse) def training() -> str: return (Path(__file__).parent / 'static' / 'training.html').read_text(encoding='utf-8')  # same read-fresh pattern, same comment.
3. Create training.html by copying six.html (264 lines) as the base — reuse verbatim: card()/setCards (six.html:142-147), chipStack/chipCol (152-155), SC seat coords (line 131, %-of-felt so they survive resizing), RANK_DE (133-135), api()/send()/start()/nextHand() fetch layer (249-255), render(v) single-redraw architecture (157-201). The backend view() contract (six_server.py:91-122) is unchanged.
4. New stage geometry (replaces #stage 1200x680, six.html:16): #stage width 2400px height 1330px. fit() becomes const s=Math.min(innerWidth/2400, innerHeight/1330) (was /1200,/680 at six.html:138-140) — uniform downscale still works on smaller monitors, native 1:1 at 2560x1440.
5. Table zone LEFT ~60%: .table left:20px top:48px width:1400px height:830px border-radius:415px/248px, .felt radius 390px/230px, .felt::after inset 40px (scale of six.html:21-27). .bar (action bar, six.html:86) moves to left:20px top:900px width:1400px height:120px. .hdr width:1400px.
6. Coaching panel RIGHT ~40%: .panel (six.html:100) becomes left:1450px top:48px width:930px height:1240px. New DOM order inside .panel: (a) .net session-netto row + #hph (hands/hour, P2-3) + .mini buttons row [Neu | Analyse | Replay]; (b) <h3>Feedback</h3><div id='coach' class='out'></div> — renders the per-decision/after-hand coach payload (the 'coach' key P1 adds to view(), recon integration point at Session.view six_server.py:91-122); until P1 lands, render a placeholder from view.legal/street so the panel is never empty; (c) <h3>Verlauf</h3><div id='log'> (existing); (d) the collapsible glossary panel (P2-2) pinned at the panel bottom.
7. Enlarge cards for the bigger felt: .card 52x74px font 24px, .card.hero 62x86px, .card.sm 40x56px, board cards 56x78px (scale six.html:38-47 proportionally).
8. Keep #stage centered translate(-50%,-50%) scale(s) exactly as six.html:138-139; keep overflow:hidden on body.
**Acceptance:**
- TestClient: from fastapi.testclient import TestClient; c=TestClient(app); r=c.get('/training'); assert r.status_code==200 and 'id="coach"' in r.text and 'id="gloss"' in r.text and '2400' in r.text — and c.get('/').text still contains '1200px' (six.html untouched).
- TestClient: POST /api/new_session then GET /api/state returns the unchanged view contract (keys hand_no, seats, legal, bot_events) — training.html consumes the same dict, no backend view change in this WP.
- Read-fresh check: edit a marker string in training.html while the server runs, re-GET /training via urllib, marker appears WITHOUT restart (mirrors tests/test_six_client.py wait_up pattern).
- Manual at 2560x1440: table occupies the left ~60% (felt right edge ≈ x=1420 of 2400 stage), panel the right ~40%; no scrollbars; seats/dealer/bets positioned correctly (SC % coords intact).
- Test command: python -m tests.test_trainer_ui (built in P2-7; this WP's assertions are its first checks).
**Size:** M

## P1-D · UI: coach feedback + collapsible glossary panel (six.html)
> ★ BINDING (retarget + autodeal): All UI edits land in training.html (NOT six.html — P2-1 runs before and creates it). Auto-deal: T.AUTODEAL=3000ms + 'Weiter' button + Space (the 12000ms variant + its assert are dropped). Cancel points of the auto-deal: glossary open, replay active, filter focused.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/web/static/six.html
**Steps:**
1. Right panel (.panel CSS six.html:100-109, DOM 121-126): insert <div id="coach"></div> above #out. In render(v) (six.html:157-201): if v.coach -> coach.innerHTML = grade-icon header (✓/～/✗ from the worst grade in v.coach.grades) + v.coach.html (server-rendered, already escaped + term-marked — the client NEVER re-escapes it); else clear.
2. COLLAPSIBLE PANEL (collapsible glossary) at panel bottom: <div id="glossar"><div id="glossarHead">Glossar ▸</div><div id="glossarBody" style="display:none"></div></div>. BEHAVIOR SPEC (pinned): collapsed by DEFAULT; glossarHead click toggles (▸/▾); body lists FIRST the terms of the current feedback (from v.coach.terms, section 'In dieser Hand'), THEN 'Alle Begriffe' as a second collapsed sub-section; full registry fetched ONCE via GET /api/glossar on first expand and cached in a JS variable (no polling); each entry renders begriff bold + erklaerung + optional formel in <code>. State is in-session JS only (no localStorage, no accounts — simplicity rule).
3. Term click-to-open: ONE delegated document-level click listener: e.target.closest('.term') -> expand #glossarBody, scroll the matching entry (id 'gl-'+dataset.term) into view, add CSS class .hl (2s flash-highlight animation). CSS: .term { border-bottom: 1px dotted; cursor: help; } .hl { animation: glflash 2s; }.
4. Auto-advance gate (recon gotcha: advTimer at six.html:224 nukes the hand after 2400ms): in renderEnd (209-225), when v.coach exists set the timeout to 12000ms instead of 2400 AND render a 'Weiter'-button in #coach that clears advTimer and calls nextHand() immediately (= 'auto-deal after feedback dismiss or timeout', TRAINER_DESIGN par.6). send() already clears advTimer (line 252) — unchanged.
5. NO stage/geometry rework (that is P2): panel-content changes only; keep #out and #log intact (Analyse flow untouched). six_server re-reads six.html per request (six_server.py:139) — no restart needed while iterating.
**Acceptance:**
- Extend tests/test_feedback_api.py: GET / body contains id="coach", id="glossar", id="glossarHead", the .term delegated-click handler code, the 12000 timeout branch, AND still contains id="out" and id="log" (no regression)
- python -m tests.test_feedback_api exits 0 after the extension
- Manual smoke (documented, not gating): python -m pokerbot.web.six_server --open -> play one hand -> feedback appears right, terms dotted-underlined, click opens the collapsible panel at the entry, 'Weiter' deals the next hand
**Size:** M

## P2-2 · Collapsible GLOSSARY panel: glossar.json + GET /api/glossary + clickable terms in all coaching text
> ★ BINDING: the coverage test checks against templates_de.TERMS_USED (not 'USED_TERMS' — otherwise the test skips forever and the glossary gate is ineffective). Data source is /api/glossary from glossar_de (see P1-A) — no separate JSON.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/web/static/glossar.json · C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py · C:/Users/hampe/Desktop/PokerB/pokerbot/web/static/training.html
**Steps:**
1. Create glossar.json using the German glossary item schema — a JSON array of {"begriff": str, "erklaerung": str, "syn": [str,...]} (syn = recognition synonyms, e.g. begriff 'Pot Odds' syn ['Pott-Odds','pot odds']). Seed >=45 entries in plain German covering EVERY term the coaching layer can emit: the rationale keys recon enumerated (bot.py:560+ — Equity, Required Equity, MDF, Pot Odds, Realization, Blocker, Texture, Initiative), understanding.py vocabulary (SPR, Position/IP/OOP, Effektiver Stack, Made Hand, Draw, Outs), formulas.py concepts (knowledge_base/math/formulas.py — Alpha/Breakeven-Bluff%, Bluff-to-Value-Ratio, Implied Odds, Fold Equity, Regel von 2 und 4, Combos/Blocker), action/line terms (C-Bet, Donk Bet, Probe Bet, Barrel, Check-Raise, Overbet, Value Bet, Bluffcatcher, Slowplay, 3-Bet, 4-Bet, Squeeze, Limp, Open Raise, All-in, Side Pot), grading/doctrine terms (GTO, Exploit, Mischen/Mixed Strategy, EV, Varianz, Ergebnis-Orientierung, Grade-Band OK/Teuer/Leak), stats terms from /api/analyze (VPIP, PFR, Aggression Factor, WTSD, bb/100). Each erklaerung 1-3 sentences of plain German, no formula-only entries.
2. Add route in six_server.py: @app.get('/api/glossary') def glossary() -> JSONResponse: return JSONResponse(json.loads((Path(__file__).parent/'static'/'glossar.json').read_text(encoding='utf-8')))  # read fresh per request, same pattern as index(); import json at top.
3. Collapsible-panel DOM in training.html panel bottom: <details id='gloss'><summary>📖 Glossar</summary><input id='gfilter' placeholder='Begriff suchen…'><div id='glist'></div></details>. CSS: #gloss{background:#070d18;border-radius:8px} summary{cursor:pointer;color:var(--gold);font-weight:700;padding:8px} #glist{max-height:420px;overflow:auto;font-size:13px;line-height:1.5;padding:0 8px 8px} .gentry b{color:var(--cyan)} .gentry.hl{background:#f0c86022;border-radius:6px}. Native <details> = the collapsible, zero framework. NOTE: input fields need user-select — override the global *{user-select:none} (six.html:13) with #gfilter{user-select:text;-webkit-user-select:text}.
4. JS loader: at start(), fetch('/api/glossary') → GLOSSARY = list; build TERM_RE once: all begriff+syn strings sorted longest-first, escaped, joined into one case-insensitive word-boundary RegExp; render #glist entries <div class='gentry' id='g-<slug>'><b>begriff</b> erklaerung</div>; #gfilter oninput hides non-matching entries (substring on begriff+erklaerung).
5. markTerms(text): the SINGLE function every coaching string passes through before hitting innerHTML — (1) HTML-escape text, (2) replace TERM_RE matches with <span class='term' data-term='<canonical begriff>'>match</span> (map syn→begriff via a lookup dict). CSS .term{color:var(--cyan);text-decoration:underline dotted;cursor:help}. Apply markTerms in: the #coach feedback renderer (P1 payload), the replay strategy one-liners (P2-6), and the /api/analyze narrative output (analyze() in training.html, port of six.html:256-259).
6. Term-click wiring via ONE delegated listener on .panel: document.querySelector('.panel').addEventListener('click', e=>{const t=e.target.closest('.term'); if(!t) return; openGloss(t.dataset.term);}). openGloss(begriff): $('gloss').open=true; clear previous .hl; the entry #g-<slug> gets class 'hl' and scrollIntoView({block:'nearest'}). Esc / clicking summary collapses (native).
7. Browsable full list requirement: with #gfilter empty, #glist shows ALL entries alphabetically — the collapsible panel doubles as the browse UI; no separate page.
**Acceptance:**
- TestClient: r=c.get('/api/glossary'); data=r.json(); assert r.status_code==200 and len(data)>=45; begriffe=[d['begriff'] for d in data]; assert len(set(begriffe))==len(begriffe); assert all(d['erklaerung'].strip() for d in data); assert all no syn collides with another begriff.
- Coverage check (in tests/test_trainer_ui.py): for the fixed core-term list ['Pot Odds','MDF','Equity','EV','SPR','C-Bet','3-Bet','Blocker','Fold Equity','VPIP','PFR','GTO','Exploit','Bluffcatcher','Outs'] assert each is a begriff or syn in glossar.json. Additionally, if pokerbot.coach templates module (P1) is importable and exports USED_TERMS, assert USED_TERMS ⊆ begriffe∪syns; else print SKIP.
- Static check: training.html contains markTerms(, data-term, <details id="gloss">, and the delegated .panel click handler (regex asserts in the test module).
- Manual: play a hand, click a dotted-underlined term in the feedback text → the collapsible panel opens, entry highlighted and scrolled into view; open the collapsible panel manually and filter 'odds' → only matching entries remain; every term in visible feedback text is either plain German or clickable.
- Test command: python -m tests.test_trainer_ui
**Size:** M

## P2-3 · Speed pack: <300ms animation constants, hotkeys (F/C/R/A/1-4), gated auto-deal, hands/hour display
> ★ BINDING: T.AUTODEAL=3000 is canonical (conflict with P1-D resolved, see there).
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/web/static/training.html
**Steps:**
1. Centralize ALL timings in one named constant object at the top of the training.html script (CLAUDE.md 'constants not magic numbers'): const T={FLY:260, DEALER:260, BETPOP:180, BANNER:250, POP:900, AUTODEAL:3000}. Rework the six.html sources: flyToWinner 520ms→T.FLY (six.html:203-207 uses 520 twice), dealer transition .34s→260ms (CSS six.html:70 + JS animate 340→T.DEALER at 194-195), banner .3s→.25s (CSS 35-36), betpop stays 180ms (74). .pop 'rise' (chip win/loss floater, six.html:84) is NON-blocking overlay — shorten 1.6s→T.POP 900ms, exempt from the 300ms budget (it never gates input). CSS durations must literally match T (the autotest greps both).
2. Auto-deal flow (replaces the blind 2400ms timer at six.html:224): renderEnd() shows the result banner AND the after-hand feedback in #coach first, THEN arms advTimer=setTimeout(nextHand, T.AUTODEAL). The 'Naechste Hand' state in the bar shows a countdown hint '(Leertaste)'. Cancel rules: send() already clears advTimer (six.html:252 — keep); ADD clears in openGloss(), on #gfilter focus, and on entering replay (P2-6 enterReplay()). Space/N at hand_over = deal now (clearTimeout + nextHand()).
3. Hotkey layer: one document.addEventListener('keydown', onKey). Guards first: if(e.target.tagName==='INPUT'&&e.target.id==='gfilter')return; if(replayMode){handled in P2-6 branch; return;}. Action keys ONLY when view && !view.hand_over && view.legal.to_act===view.human_seat (the legal dict is {to_act:null} otherwise, six_server.py:120): F→send('fold') if legal.can_fold; C→can_check?send('check'):can_call?send('call'):noop; R→if can_raise: first press reveals/focuses the sizer, second press (or Enter) sends send('raise',parseInt($('sl').value)); A→send('allin') if can_raise; 1/2/3/4→set slider to Min/half/three-quarter/Pot via the same formulas as the qz buttons (six.html:245: cur+0.5*(pot+call) etc.); ArrowUp/ArrowDown→slider.value±slider.step then upd(). Hand-over keys: Space or N→next hand; G→toggle the collapsible panel; Esc→close the collapsible panel / exit replay. preventDefault on Space (page scroll).
4. Show <kbd> hints on the buttons: 'Fold (F)', 'Check (C)' / 'Call (C)', 'Bet/Raise (R)', 'All-in (A)', quick sizes '1..4' — tiny .kbd CSS (opacity .6, font-size 10px).
5. Hands/hour: client-side sessionStart=Date.now() set in start(); render() updates #hph: const hph=v.hands_done>1?Math.round(v.hands_done/((Date.now()-sessionStart)/3.6e6)):'—'; display next to the session-net ('X Haende · Y H./Std'). Pure display — no backend change.
6. Bot decisions are already instant (engine, no LLM — six_server advance()); no backend latency work needed.
**Acceptance:**
- Timing autotest (tests/test_trainer_ui.py): read training.html; parse the const T object via regex; assert every value except T.POP and T.AUTODEAL is <=300; assert the literals '520' and '2400' (the old slow timings) do NOT appear in the script; assert CSS contains '.25s' for banner and no '1.6s'.
- Static hotkey check: training.html contains keydown listener, cases for 'f','c','r','a','1','2','3','4',' ','n','g','Escape' (regex), and the guard view.legal.to_act===view.human_seat.
- TestClient flow: new_session → drive to human turn → POST /api/action check/call loop to hand end → the view at hand_over has legal.to_act null (guard basis verified server-side).
- Manual: play 10 hands using ONLY the keyboard (no mouse) in <=5 minutes (>=120 hands/hour pace); auto-deal fires ~3s after feedback appears; pressing Space deals immediately; opening the glossary stops the auto-deal.
- Test command: python -m tests.test_trainer_ui
**Size:** M

## P2-4 · Visibility pack: stronger actor highlight, big bet amounts at chips, permanent pot, one-line action ticker
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/web/static/training.html
**Steps:**
1. Actor highlight: keep .seat.turn turnpulse (six.html:52-54, already good) and ADD a name emphasis — .seat.turn .nm{color:var(--cyan);font-size:15px} plus a small '▶' marker before the acting player's name in the pod; the human's turn additionally flashes the .bar border (box-shadow pulse) so the eye lands on the action bar.
2. Big bet amounts at chips: .camt (six.html:77) 13px→20px font, padding 2px 10px; chipStack() unchanged otherwise (chip colour coding six.html:151-155 already distinguishes sizes — keep verbatim, it is a deliberate design note in the source).
3. Permanent pot: .pot (six.html:30) 17px→28px font, always rendered (it already is — render() line 197 sets it every redraw); add a persistent second line under it '#potdetail' showing current_bet when >0: 'Bet: Xbb' (from view.current_bet), font 15px muted.
4. One-line action ticker: new <div id='ticker'> positioned between table and bar (left:20px top:882px width:1400px height:22px, ellipsis overflow, font 16px). Build in render(): maintain tickerParts=[] reset on street change or new hand (track lastStreet/lastHandNo); for each e of v.bot_events push `${e.pos} ${verbDe(e.action)}${e.amount?' '+bbs(e.amount)+'bb':''}`; on human send() push 'Du '+verb; ticker.textContent=parts.join(' → ') (design-doc example: 'CO bets 12 → BTN calls'). verbDe maps fold/check/call/bet/raise/allin → setzt/callt/foldet/checkt/erhoeht/All-in (short German). The existing #log keeps the full history; the ticker is the at-a-glance line.
5. Board cards: on street change animate the newly revealed cards only with a <=250ms fade/pop (CSS class .new on the appended cards, keyframe 250ms) — visibility of what just changed, inside the T budget.
**Acceptance:**
- Static: training.html contains id="ticker", verbDe(, '#potdetail', and .camt font-size >=20px (regex asserts in tests/test_trainer_ui.py).
- TestClient: after new_session, the view contains bot_events (list, possibly empty) and current_bet — the ticker inputs exist in the contract (assert keys present).
- Manual: during a live hand the ticker reads e.g. 'CO erhoeht 3bb → BTN callt → Du ?' and resets each street; pot readable at a glance from 1m distance at 2560x1440; the acting seat is unambiguous within 1s of looking at the screen.
- Test command: python -m tests.test_trainer_ui
**Size:** S

## P2-5 · Replay backend: pure step compiler from the logs + GET /api/replay/last
> ★ WARNING: The table.py line anchors 115-116/170-172/189-190 are NOT recon-verified (semantics yes, lines no) — re-check before using as edit anchors. OPTIONAL (~10 lines): a ?hand_no= parameter on the same route allows replay of EVERY session hand, not only the last.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/web/replay.py · C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py
**Steps:**
1. New module pokerbot/web/replay.py with ONE pure function build_replay_steps(hand_rec: dict, decisions: list[dict]) -> dict. Input hand_rec = a build_hand_record dict EXACTLY as logged (session_log.py:8-32): {hand_no, button, sb, bb, human_seat, positions{seat→label}, hole{seat→[c1,c2]} (ALL seats incl. bots), board (full runout), actions[{street, seat, pos, action, amount, is_human}], result{winners[{seat,amount,rank?}], reveal?, shown?}, net{seat→chips}}. Input decisions = the P0 per-decision records for this hand (../doctrine/TRAINER_DESIGN.md §4 fields: hand_id, ts, street, spot_fp, state_kompakt, human_action, oracle_action, advisor_dist, equity, pot_odds, mdf, grade, grade_typ, erklaerung_kurz, mode).
2. Reconstruction rules (blinds+deal events are DROPPED by build_hand_record — table.py:119/237 vs session_log.py:14 'player' filter — so re-derive): (1) BLINDS step: sb_seat=(button+1)%6, bb_seat=(button+2)%6 (table.py:115-116); seed committed={sb_seat:sb, bb_seat:bb}, pot=sb+bb. (2) DEAL steps: board order is deterministic — board[0:3]=Flop, board[3]=Turn, board[4]=River; insert a {type:'deal', street, cards} step before the FIRST action whose street tag changes; if the runout has streets with NO actions (all-in earlier), append the remaining deal steps at the end (the silent-runout case, table.py:236 silent=True — the recon-flagged trap). (3) ACTION steps with pot math per VERIFIED history semantics: 'call' → committed[seat]+=amount (chips ADDED, table.py:170-172); 'bet'/'raise' → committed[seat]=amount (street-commit-TO total, table.py:189-190 logs 'to'; allin is normalized to bet/raise there, so no separate case); 'fold'/'check' → no chips; on each deal step fold committed into pot and reset street committed. Each step carries {type:'action', street, seat, pos, action, amount, is_human, pot_after, committed_after{seat→chips}}. (4) JOIN: for the k-th is_human action of each street, attach decision=the k-th decision record of that street (match tolerant: prefer explicit hand_no/hand_id equality if the field exists, else positional by street+order). (5) HOLE EXPOSURE (deliberate, anti-results-orientation per TRAINER_DESIGN §1.1 + recon privacy gotcha): steps expose hero hole + board; bot holes only for seats in result['shown'] when result['reveal'] is truthy (mirrors seat_view, six_server.py:98-105); the raw hole{} of unrevealed bots is STRIPPED from the response. Return {hand_no, button, sb, bb, human_seat, positions, hero_hole, board, steps[...], result, net, final_pot}.
3. final_pot = sum of all committed = must equal table.pot() at hand end (t.pot() is the sum of committed — the acceptance invariant).
4. Route in six_server.py: @app.get('/api/replay/last') def replay_last() -> JSONResponse: 400 {'error':'no session'} if SESSION is None; read the LAST line of SESSION.path (the hand-record JSONL, path set at six_server.py:32), 400 {'error':'no hand yet'} if the file is missing/empty; load decisions from the P0 decisions file — PINNED CONTRACT: SESSION.path.with_name(SESSION.path.name.replace('session_','decisions_')) i.e. data/sessions/decisions_<sid>.jsonl; if that file is absent, pass decisions=[] (replay then shows played actions without recommendations — graceful, not an error); filter decision lines to the last hand (hand_no field, fallback hand_id), call build_replay_steps, return JSONResponse. Follow the analyze() lazy-read pattern (six_server.py:180-185).
5. Keep replay.py free of FastAPI/engine imports (pure dict→dict) so it is unit-testable without a server.
**Acceptance:**
- Unit (in tests/test_trainer_ui.py): feed a handcrafted 6-max hand_rec (open→call→flop cbet→fold, plus an all-in-preflop runout case) → assert step[0].type=='blinds'; deal steps appear in flop→turn→river order; the runout case appends deal steps after the last action; every action step's pot_after is monotone non-decreasing; final_pot equals the hand-computed expectation; unrevealed bot holes absent from the response; the human action steps carry the joined decision when decisions given and decision==None when [].
- TestClient end-to-end: new_session → check/call the human through one full hand (loop on view.legal until hand_over) capturing last_view.pot at hand_over → GET /api/replay/last → 200; assert reply.final_pot == captured last_view.pot (reconstruction invariant vs the live engine); assert n action steps == len(actions) of the logged record; GET /api/replay/last before any session → 400 'no session'.
- Semantics regression guard: unit case with a raise-then-call sequence asserts call adds chips while raise sets the TO-total (the session_log.py:20 'to' or 'amount' merge trap).
- Test command: python -m tests.test_trainer_ui
**Size:** M

## P2-6 · Replay UI: after-hand button → animated step-through with played-vs-recommended at every hero spot
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/web/static/training.html
**Steps:**
1. Entry: 'Replay' button in the panel .mini row (P2-1) + hotkey P at hand_over. enterReplay(): clearTimeout(advTimer) FIRST (the recon-flagged trap — otherwise the hand auto-advances under the walkthrough, six.html:224), set replayMode=true, fetch('/api/replay/last'), on error log it and abort; stash the current live view for exit.
2. Replay renderer reuses the existing table DOM/renderers (card(), chipStack, SC seats, #board, #pot) — renderReplayState(i): from steps[0..i] derive per-seat committed/folded/stack-delta and board slice; hero hole from hero_hole; bot holes render '??' backs except result-shown seats at the final step; a REPLAY badge overlays the .hdr ('REPLAY — Hand #N · Schritt i/M'); the .bar is replaced by replay controls: ⟨ Zurueck · Weiter ⟩ · Verlassen (plus keys: ArrowRight/Space=next, ArrowLeft=back, Esc=exit — wired into the P2-3 onKey replayMode branch).
3. Step animation: advancing one step animates only the delta (bet chips via existing .bet/betpop 180ms, deal via the 250ms card pop from P2-4, fold via .folded opacity) — all within the T budget; holding ArrowRight steps fast (no artificial delays between steps).
4. HERO-SPOT PAUSE (the design-doc core, TRAINER_DESIGN §6 Replay): when steps[i].is_human, auto-stop and render into #coach: line 1 'Gespielt: <verbDe(action)> Xbb' vs 'Empfohlen: <verbDe(decision.oracle_action)>' + the grade icon (✓/～/✗ from decision.grade — the grade-band symbols of TRAINER_DESIGN §1.2); line 2 = decision.erklaerung_kurz (the one-line strategy); optional line 3 = compact numbers 'Equity X% · benoetigt Y% · MDF Z%' from decision.equity/pot_odds/mdf when present. ALL through markTerms() (P2-2) so every term is glossary-clickable. When decision is null (no decisions file): show 'Gespielt: …' + 'keine Empfehlung geloggt' — never crash.
5. Also pause on deal steps with a one-word street label flash ('FLOP'), so the walkthrough reads street by street; non-hero action steps play through without pause when 'Weiter' is held, one per keypress otherwise.
6. exitReplay(): replayMode=false, re-render the stashed live view via render(), restore the bar, do NOT re-arm auto-deal (the user decides with Space/N — deliberate: post-replay reflection); 'Neu'/next-hand buttons stay functional.
7. Guard: send() and nextHand() early-return while replayMode (no live actions from inside a replay).
**Acceptance:**
- Static: training.html contains enterReplay, exitReplay, replayMode guards in send/nextHand/onKey, renderReplayState, 'Empfohlen', and the null-decision fallback string (regex asserts).
- TestClient (data path): play a scripted hand, GET /api/replay/last, assert every is_human step has the fields the renderer reads (action, amount, street, pot_after) and — when the P0 decisions file exists — decision.oracle_action + decision.grade + decision.erklaerung_kurz are present (else the test asserts the endpoint still returns steps with decision null).
- Manual: after a lost hand press P → step through with ArrowRight; at each of your decisions the panel shows Gespielt vs Empfohlen + one-liner; a term in the one-liner opens the collapsible panel; Esc returns to the live table with the hand-end state intact and NO auto-deal having fired during the replay; bot holes stay hidden except showdown-revealed seats.
- Latency: stepping is instant (<50ms per step, pure client render of pre-fetched steps).
- Test command: python -m tests.test_trainer_ui
**Size:** L

## P2-7 · P2 autotest module: tests/test_trainer_ui.py (TestClient end-to-end + static UI contract checks)
**Files:** C:/Users/hampe/Desktop/PokerB/tests/test_trainer_ui.py
**Steps:**
1. Structure per the repo's disciplined harness pattern (tests/test_math_suite.py:23-40, recon): module-level CHECKS registry via @check(name) decorator, each check returns None (pass) or a failure-detail string; main() runs all, prints PASS/FAIL per check, exits 1 on any failure, supports --json PATH; NO pytest (repo convention: python -m tests.test_trainer_ui from project root). Deterministic: seed 42 where randomness is touched.
2. Backend checks (fastapi.testclient.TestClient(app) — the CLAUDE.md-mandated UI verification path): (1) routes_exist: GET /training 200 + GET / still serves six.html; (2) glossary_valid: the P2-2 assertions (>=45 unique entries, non-empty erklaerung, core-term coverage list); (3) full_hand_flow: POST /api/new_session → loop: while not view.hand_over: if view.legal.to_act==view.human_seat: POST /api/action check-else-call; capture pot at hand_over; (4) replay_contract: GET /api/replay/last → 200, blinds-first, deal order, final_pot==captured pot, action-step count==logged record's len(actions), no unrevealed bot holes in the payload; (5) replay_errors: fresh-process simulation via SESSION=None monkeypatch → 400.
3. Pure-unit checks: build_replay_steps handcrafted cases from P2-5 (raise-TO vs call-added semantics, all-in silent runout, empty-decisions grace).
4. Static UI-contract checks (read training.html as text, regex): const T values <=300 except POP/AUTODEAL; old literals 520/2400/1.6s absent; hotkey cases f/c/r/a/1-4/Space/n/g/Escape present; ids coach/gloss/glist/gfilter/ticker present; markTerms + data-term + replayMode guards present; stage 2400x1330 + fit() divisors updated; six.html byte-unchanged vs git (subprocess git diff --quiet -- pokerbot/web/static/six.html).
5. Wire into the P4 autotest doctrine: expose run_all() so the later full-stack harness (TRAINER_DESIGN §7) can import and include these checks; note in the module docstring that hotkey/visual feel remains the manual checklist (listed in each WP's acceptance).
**Acceptance:**
- python -m tests.test_trainer_ui exits 0 with all checks PASS on a clean checkout after P2-1..P2-6; exits 1 with a named failing check when any single P2 artifact is broken (verify by temporarily renaming glossar.json → glossary_valid FAIL).
- python -m tests.test_trainer_ui --json out.json writes machine-readable results (name→pass/fail detail).
- The module needs NO running server (TestClient in-process) and NO network; runtime <30s.
- Existing suites still green: python -m tests.test_table and python -m tests.test_game unchanged-pass (no engine edits anywhere in P2).
**Size:** M

## P3-A · Language layer: JSON contract (coach.v1) + pluggable backend interface (templates = default)
> ★ BINDING (enum pin): coach.v1 adopts EXACTLY the P0-4 vocabulary: grade ∈ {ok, teuer, leak}, grade_typ ∈ {pot_odds, mdf, sizing, advisor_freq, oracle_diff}. NO grade 'none' (P0 never produces it; P3-D would otherwise silently exclude it). Adapter: P1-B delivers {text, html, terms} — LanguageBackend.render_short returns str; map explicitly onto templates_de.render_hand_feedback/render_decision_feedback.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/language.py · C:/Users/hampe/Desktop/PokerB/tests/test_language_contract.py
**Steps:**
1. Create pokerbot/coach/language.py defining the frozen payload contract `coach.v1` as a documented dict schema + a `make_payload(...)` builder. Fields (exact): {schema:'coach.v1', mode:'gto'|'exploit', hand_id, street, hero_pos, grade:'ok'|'teuer'|'leak'|'none', grade_typ:'mix'|'ev'|'pot_odds'|'mdf'|'sizing'|'none', confidence:'hart'|'bot_einschaetzung' (the design doc's uncertainty hierarchy: preflop/river/math=hart, flop/turn strategy=bot_einschaetzung), human_action:{action,amount_bb}, oracle_action:{action,amount_bb}|null, numbers:{...}, strategic:{spr, position:'IP'|'OOP', made_hand, texture:[...], eff_stack_bb}, mixed:{is_mixed:bool, dist:{...}}|null, glossar_terms:[str]}. RULE (docstring, binding): every numeral the rendered text may contain MUST come from `numbers` — the language layer decides nothing and computes nothing (../doctrine/TRAINER_DESIGN.md §3).
2. `numbers` sub-dict keys map 1:1 onto the P0 decision record / decide()-rationale fields from recon: equity_pct, required_equity_pct, mdf_pct, pot_bb, to_call_bb, bet_frac_pot, snapped_frac_pot, advisor_bet_pct (from pf_advisor.p_bet), advisor_fold_pct/advisor_call_pct/advisor_raise_pct (from pf_advisor.p_defense 3-tuple), spr, ev_diff_bb — all pre-rounded to 1 decimal at payload-build time so validator matching is exact. Absent values are OMITTED, never 0 (recon: advisor None must not become 0).
3. Define `class LanguageBackend(Protocol): name: str; def render_short(self, payload: dict) -> str` and `class TemplateBackend` that delegates to the P1 template renderer (P1's deterministic German template library; whatever module name P1 shipped — the executing agent wires TemplateBackend as a thin adapter around it, the payload schema defined HERE is what P1's renderer consumes). `get_backend() -> LanguageBackend` reads env `POKERB_COACH_BACKEND` ('template' default; 'ollama' only if P3-B passed its gate) — templates remain the DEFAULT, LLM strictly optional.
4. Implement `validate_numbers(text: str, payload: dict) -> list[str]`: regex-extract all numeric tokens from the rendered German text (handle '68 %', '12,5' comma-decimal, '3bb'), normalize, and check each against the set of allowed renderings of payload['numbers'] values (exact, int-rounded, and 1-decimal forms) plus a tiny structural whitelist {street card counts 3/4/5 only if board terms used — keep whitelist explicit and short}. Return the list of hallucinated tokens (empty = pass). This is the frac_bad-style validity gate carried over from the GLM lessons (../doctrine/TRAINER_DESIGN.md §3).
5. tests/test_language_contract.py (repo convention: python -m tests.test_language_contract, main() + asserts, NO pytest): build payloads for every rationale family from recon — preflop blueprint, postflop facing-bet (with defense_advisor), bet-side (advisor_pbet), SPARSE resolver shape (no equity/mdf), deepcfr shape — assert TemplateBackend.render_short returns non-empty German text for each, validate_numbers == [] for each, and a deliberately corrupted text (inject '73 %' not in payload) is caught.
**Acceptance:**
- python -m tests.test_language_contract exits 0: all 5 rationale-family payloads render non-empty text via TemplateBackend, validate_numbers()==[] on each, and the injected-number negative case is flagged.
- get_backend() with unset env returns TemplateBackend (name=='template'); POKERB_COACH_BACKEND=ollama with no Ollama configured falls back to TemplateBackend with a logged warning, never crashes.
- TemplateBackend.render_short is deterministic: two calls on the same payload return byte-identical text (asserted in the test).
**Size:** M

## P3-C · Exploit-mode panel: "What the bot has learned about you" (sixmax.OppModel → plain text, glossary-linked)
> ★ BINDING: UI parts in training.html (never six.html).
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/bot_read.py · C:/Users/hampe/Desktop/PokerB/pokerbot/arena/sixmax.py · C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py · C:/Users/hampe/Desktop/PokerB/pokerbot/web/static/six.html · C:/Users/hampe/Desktop/PokerB/tests/test_bot_read.py
**Steps:**
1. sixmax.py refactor (behavior-identical, byte-identity-gated per clean-code discipline): hoist the _read trigger literals (sixmax.py:296-317) into module constants — READ_THREEBET_HAPPY=0.11, READ_AGGRO_HI=0.55, READ_AGGRO_LO=0.30, READ_OVERFOLD=0.55, READ_STATION=0.30, READ_CONT_BONUS_CAP=0.15, READ_CALL_DELTA=0.07, READ_BLUFF_MULT_MAX=2.2, READ_BLUFF_MULT_STATION=0.4 — used by _read AND imported by bot_read.py so the panel can never drift from the live exploit logic.
2. pokerbot/coach/bot_read.py: `read_of_human(bots: dict) -> dict`. Source (recon-exact): any bot's model of seat 0 — `bots[1].opp[0]` (from pokerbot.arena.sixmax import OppModel as SeatOppModel — NAME-COLLISION gotcha: NOT strategy/opp_model.OppModel) — identical across bots because six_server._observe_all feeds all bots the same public actions; the panel presents ONE read (INTEGRITY: convergent observation). Output fields (exact): {hands, stats:{fold_to_bet:{value|null, n:faced_bet, min:8}, threebet:{value|null, n:tb_opp, min:6}, aggression:{value|null, n:agg_opp, min:8}, vpip_raw:vpip, pfr_raw:pfr}, live_exploits:[{key:'overfold'|'station'|'aggro_bluffcatch'|'passive_valueonly'|'threebet_happy', stat, threshold, effekt_de, text_de, glossar_terms:[...]}]} where live_exploits is computed by evaluating the derived rates against the hoisted READ_* constants exactly as _read does.
3. German rendering in bot_read.py via the P3-A contract (numbers only from the dict): per trigger a fixed template, e.g. overfold → 'Er hat bemerkt: du foldest {fold_to_bet_pct} % auf Bets (n={n}) → er blufft dich öfter (Bluff-Frequenz bis {mult}×).' Below sample gates render the honest empty state 'Noch nicht genug Daten ({n}/{min})' from the raw counters (recon gotcha: derived methods return None until faced_bet>=8 / tb_opp>=6 / agg_opp>=8, typically 10-25 hands). Attach glossar_terms (begriff keys, e.g. 'Fold-to-Bet', '3-Bet', 'Aggression', 'Bluff-Frequenz') so the P2 collapsible glossary panel makes them clickable.
4. six_server.py: add `GET /api/bot_read` following the analyze() template (six_server.py:180-185): 400 {error:'no session'} if SESSION is None; if the session mode is 'gto' return {mode:'gto', stats:..., live_exploits:[], hinweis:'Im GTO-Modus nutzt der Bot diese Daten nicht.'} (stats still shown — pedagogically useful); in exploit mode return the full read_of_human(SESSION.bots) payload.
5. six.html: add a collapsible '🧠 Was der Bot über dich gelernt hat' section in the right coaching panel (the P2 panel area, .panel DOM six.html:121-126): a toggle button fetches /api/bot_read on expand and after each hand-end while expanded (hook into renderEnd, six.html:209-225 — do NOT add polling; the frontend stays request-driven). Live exploits rendered red-flagged with the German text; gated stats as 'n/min' progress; terms wrapped in the P2 glossary-click markup.
6. tests/test_bot_read.py (python -m convention): construct a Session-like bots dict with a hand-crafted SeatOppModel (e.g. faced_bet=12, fold_bet=9 → fold_to_bet 0.75 > 0.55) → assert live_exploits contains key 'overfold' with threshold 0.55 and the rendered text passes language.validate_numbers; below-gate model (faced_bet=3) → assert stats.fold_to_bet.value is null and the n/min empty state renders; TestClient drive: new_session → /api/bot_read returns 200 with hands==0 empty state, no-session returns 400.
**Acceptance:**
- python -m tests.test_bot_read exits 0 (over-threshold trigger detected, below-gate empty state, TestClient 200/400 paths).
- Byte-identity gate on the sixmax.py constant-hoist: a scripted 200-hand seeded league run (existing harness pattern) produces identical action sequences before/after the refactor (constants only, no behavior change).
- Manual smoke via TestClient: after simulating >=30 hands with an over-folding scripted human policy, /api/bot_read live_exploits is non-empty and every German string's numerals pass validate_numbers against the payload.
**Size:** M

## P3-D · Adaptive difficulty regulator: error rate from grades → 10-20% band → league composition + exploit_gain
> ★ BINDING: the mode comes from the session (P0-0), not from env; UI parts in training.html.
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/difficulty.py · C:/Users/hampe/Desktop/PokerB/pokerbot/arena/sixmax.py · C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py · C:/Users/hampe/Desktop/PokerB/tests/test_difficulty.py
**Steps:**
1. sixmax.py: add field `exploit_gain: float = 1.0` to Knobs (sixmax.py:34-45; default 1.0 keeps every existing caller byte-identical). In _read (sixmax.py:290-318): `g = self.k.exploit_gain; if g <= 0: return {}` at the top, then scale the deltas — cont_bonus *= g, call_delta *= g, bluff_mult = 1.0 + (bluff_mult - 1.0) * g — the recon-designated cleanest per-bot exploit dial. This ALSO implements the two modes cleanly: GTO mode = all seats exploit_gain=0.0 (bots ignore tendencies), exploit mode = 1.0 (../doctrine/TRAINER_DESIGN.md §2), applied via dataclasses.replace at Session construction.
2. pokerbot/coach/difficulty.py: (1) `error_rate(decision_records) -> (rate, n)` — share of grade in {'teuer','leak'} among records with grade != 'none' from the P0 decisions JSONL (only graded decisions count; math-check-only 'none' spots excluded). (2) `TIERS` list easiest→hardest using the recon ladder built purely from existing PROFILES (incl. held-out): T0 {1:'whale',2:'station',3:'rock',4:'nit',5:'whale'}, T1 {1:'station',2:'whale',3:'nit',4:'rock',5:'maniac'}, T2 {1:'tag',2:'lag',3:'nit',4:'station',5:'maniac'} (= today's default, six_server.py:36), T3 {1:'tag',2:'shark',3:'lag',4:'tag',5:'shark'}, T4 {1:'shark',2:'tag',3:'shark',4:'tag',5:'shark'}. (3) `update(state, rate, n) -> state`: band controller — if n >= 30 and rate > 0.20: tier -= 1; if n >= 30 and rate < 0.10: tier += 1; clamp [0,4]; at T4 with rate still < 0.10, raise exploit_gain fine-dial 1.0 → 1.5 (deltas stay hard-capped by the READ_* caps, so this only reaches the caps faster); hysteresis: at most ONE step per completed session, never mid-session. (4) Persist data/trainer/difficulty.json {tier, exploit_gain, history:[{ts, rate, n, tier}]} via json read/write (append-style history).
3. six_server.py Session.__init__ hook (the single composition seam, six_server.py:36-38): replace the hardcoded _assign dict with `difficulty.current_assignment()` (reads difficulty.json, falls back to T2 when absent), and apply mode: knobs = dataclasses.replace(PROFILES[name], exploit_gain=0.0 if mode=='gto' else state['exploit_gain']). At session end (the existing /api/analyze call or an explicit session-close), compute error_rate over that session's decisions file and call difficulty.update — persisting for the NEXT session (recon: Session state dies with the process; the JSON file is the survivor).
4. Expose the state in the UI info line (six.html header area): 'Liga-Stufe {tier_name} · Fehlerrate letzte Session {rate} % (Ziel 10-20 %)' — honest framing per design doc §5 (85% rule = hypothesis, as an adaptive band).
5. tests/test_difficulty.py: deterministic unit checks — rate 0.25/n=50 steps tier down; rate 0.05/n=50 steps up; rate 0.15 holds; n=10 NEVER moves regardless of rate; tier clamps at 0 and 4; T4+low-rate raises exploit_gain to 1.5; exploit_gain=0 makes SixMaxBot._read return {} on a crafted over-threshold OppModel; round-trip persistence (write→read→same state).
**Acceptance:**
- python -m tests.test_difficulty exits 0 (all band/hysteresis/clamp/persistence/gain-zero cases).
- Byte-identity: with exploit_gain default 1.0, a seeded 200-hand league run matches pre-change action sequences exactly (the Knobs field addition is behaviorally invisible).
- TestClient end-to-end: seed difficulty.json to tier 0, new_session → SESSION.bots profile names match TIERS[0]; mode='gto' session → every bot's knobs.exploit_gain == 0.0.
**Size:** M

## P4-A · Autotest harness pokerbot/coach/autotest.py — league bots through the complete stack, binding before human use
> ★ BINDING (action mapping): SixMaxBot.decide can deliver 'bet', POST /api/action accepts only fold|check|call|raise|allin — the driver normalizes bet→raise and clamps amount into [raise_min, raise_max] from view.legal, otherwise the harness 400s and zero_crash fails spuriously. (SESSION.table is real: six_server.py:30.)
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/autotest.py · C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py
**Steps:**
1. Structure autotest.py EXACTLY on the tests/test_math_suite.py pattern (recon: the repo's most disciplined harness): module-level CHECKS registry via @check(name) decorator, functions return None (pass) or a failure-detail string, seed 42, argparse --hands (default 60) / --deep (300 hands) / --json PATH, exit 1 on any FAIL, run as python -m pokerbot.coach.autotest. FIRST lines of main(): os.environ.setdefault('POKERB_PRINCE','1'); gto_mode.apply() BEFORE any pokerbot.strategy import (import-time flag gotcha).
2. six_server.py instrumentation (tiny): add module counter BOT_FALLBACKS=0, incremented in the advance() bare-except fallback (six_server.py:66-70) — the recon'd silent-swallow trap becomes measurable; the harness asserts it stays 0 (a broken bot integration must FAIL the autotest, not quietly play fit-or-fold).
3. Full-stack driver (in-process): TestClient(six_server.app) per CLAUDE.md; human seat driven by SixMaxBot(0, PROFILES[profile]) with ONE persistent injected rng (bot.rng = random.Random(seed) — solver_policy.py:32-33 pattern; per-spot reseeding = the fake-mixing trap, sixmax.py:96-97). Loop per hand: obs = six_server.SESSION.table.obs_for(0) (TestClient shares module state in-process) → dec = driver.decide(obs) → POST /api/action via HTTP (exercises the real route incl. grader/logger/renderer) → on hand_over POST /api/hand; run --hands hands in BOTH modes (gto + exploit).
4. @check('zero_crash'): every HTTP response across the full run is 200 (400 only for the deliberate no-session probe); BOT_FALLBACKS == 0; the run completes --hands hands in both modes. @check('latency_budgets'): from the decision records' timing fields (P0 logs ts; add a grade_ms field if absent), assert p95 grade+math < 500 ms and p95 template render < 50 ms (LLM path measured only when POKERB_COACH_BACKEND=ollama, budget p95 < 2000 ms per the P3-B criterion). @check('grade_distribution_sanity'): drive one full run with profile 'station' and one with 'tag' (same --hands, seeds 42/43), compute difficulty.error_rate over each run's decisions; require >= 200 graded decisions per arm (auto-bump hands if fewer) and assert rate(station) > rate(tag) — the design doc's explicit sanity criterion. @check('template_coverage'): render via TemplateBackend a fixture set covering ALL rationale families (preflop blueprint/push-fold/bb_vs_open, postflop facing-bet + bet-side, SPARSE resolver, deepcfr) x grade bands — every payload yields non-empty text, validate_numbers()==[], and every emitted glossar_term exists as a begriff key in the glossary JSON (the collapsible-panel hard requirement). @check('report_builds'): POST /api/analyze returns stats dict with hands>0 offline (narrative '' fallback allowed, no exception). @check('replay_reconstructs'): the P2 replay endpoint returns a walkthrough for every logged hand whose step count equals the hand's action count. @check('bot_read_renders'): after the exploit-mode run (>= 30 hands), /api/bot_read returns either live_exploits or gated n/min states, all texts validate_numbers-clean. @check('decisions_schema'): every line of the run's decisions JSONL parses and contains the P0 required keys {hand_id, ts, street, spot_fp, human_action, grade, mode}.
5. --json PATH writes {checks:{name: 'PASS'|detail}, hands, decisions_n, error_rates:{station, tag}, latency:{grade_p95_ms, render_p95_ms}, ts} — the artifact the executing agent cites as DONE evidence; wire the invocation into the launcher docs ('before human use: python -m pokerbot.coach.autotest').
**Acceptance:**
- python -m pokerbot.coach.autotest --hands 60 --json data/trainer/autotest.json exits 0 with ALL checks PASS on the completed P0-P3 stack; the JSON artifact exists and shows decisions_n > 0, BOT_FALLBACKS-backed zero_crash PASS, grade_p95 < 500 ms, render_p95 < 50 ms.
- grade_distribution_sanity PASS with >= 200 graded decisions per arm and error_rate(station) > error_rate(tag) recorded numerically in the JSON.
- Deliberate-failure probe (dev-only, documented in the module docstring): monkeypatching a template to emit a foreign number makes template_coverage FAIL and the process exit 1 — the harness detects, not just runs.
- python -m pokerbot.coach.autotest --deep completes 300 hands x 2 modes x 2 driver profiles without crash (the pre-release bar).
**Size:** L

## P3-B · Ollama backend + measured Ollama-vs-templates test protocol (<2s, 0 number hallucination)
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/llm_backend.py · C:/Users/hampe/Desktop/PokerB/research/language_backend_test.py
**Steps:**
1. pokerbot/coach/llm_backend.py: `class OllamaBackend(LanguageBackend)` — POST http://localhost:11434/api/generate (urllib, timeout=3s), model from env POKERB_OLLAMA_MODEL (default 'qwen3:8b'), stream=False. System prompt (German, hard-coded): render 3-5 lines of feedback FROM the JSON; take numbers EXCLUSIVELY from payload['numbers'], NEVER compute, NEVER new numbers; tone warm+concrete per fairness doctrine §1.5. Post-generation: run language.validate_numbers(text, payload); on ANY hallucinated token or timeout or empty output → return TemplateBackend fallback text and increment a module counter FALLBACKS (the GLM validity-gate lesson: LLM output is gated, never trusted).
2. research/language_backend_test.py — THE measured A/B protocol (../doctrine/TRAINER_DESIGN.md §3 'ONE measured test'): (1) Corpus: load >=100 real decision payloads from the newest data/sessions/decisions_*.jsonl (generated by the P4-A autotest run if no human session exists — document `python -m pokerbot.coach.autotest --hands 60` as the corpus generator), stratified to include every rationale family + every grade band; fail fast with a clear message if <100 or a family is missing. (2) For backend in [TemplateBackend, OllamaBackend]: render each payload, record wall-latency, hallucinated-token count (validate_numbers), empty-output count, fallback count. (3) Verdict rules (hard, printed + JSON): OllamaBackend is ELIGIBLE iff p95 latency < 2000 ms AND total hallucinated tokens == 0 AND empties == 0; templates win every tie ('the simpler system wins'). Default stays 'template' REGARDLESS — eligibility only unlocks the user manually setting POKERB_COACH_BACKEND=ollama.
3. Graceful offline: if localhost:11434 is unreachable, print 'Ollama nicht installiert/gestartet — Test übersprungen, Templates bleiben Default', write verdict 'skipped', exit 0 (the trainer must work offline; recon narrative() fallback pattern).
4. Write the machine-readable report to data/trainer/language_backend_report.json: {corpus_n, per_backend:{p50_ms, p95_ms, hallucinated, empties, fallbacks}, verdict, ts} — the executing agent cites this file as the measured artifact (Verification-grounding gate).
**Acceptance:**
- python -m research.language_backend_test with Ollama NOT running: exits 0, prints the skip message, report JSON has verdict='skipped', and templates remain default (get_backend() unchanged).
- python -m research.language_backend_test with the corpus present: report JSON exists with per-backend p50/p95/hallucinated fields; TemplateBackend rows show hallucinated==0 and p95 < 50 ms.
- Unit check inside the script (--selftest flag): a mocked LLM response containing a number not in the payload is rejected and the returned text equals the TemplateBackend fallback (FALLBACKS counter incremented).
**Size:** M

## P4-B · GTOW Analyzer export validation path: trainer hands → PokerStars HH (sixmax_export) as external second opinion
**Files:** C:/Users/hampe/Desktop/PokerB/pokerbot/coach/export_gtow.py · C:/Users/hampe/Desktop/PokerB/pokerbot/web/session_log.py · C:/Users/hampe/Desktop/PokerB/tests/test_export_gtow.py
**Steps:**
1. session_log.py: extend build_hand_record (session_log.py:8-32) with one additional key 'export': {names, button, holes, history, result} — the EXACT dict research/sixmax_export.play_hand returns and format_hand(hand_id, dt, h) consumes (recon: 'serialize exactly this per hand → zero replay needed'). names from the Session seat names, history=table.history verbatim (keeps the 'blinds'/'deal' events build_hand_record otherwise drops — needed for correct HH street sections), result=table.result. Single call site _log_if_done (six_server.py:40-47) unchanged.
2. pokerbot/coach/export_gtow.py CLI: python -m pokerbot.coach.export_gtow --session <path|latest> [--out FILE]. Reads the session JSONL, takes rec['export'] per hand, calls research.sixmax_export.format_hand. GEOMETRY GUARD: refuse (clear German error) any hand with bb != 100 or reset stack != 10000 — the exporter constants (SB/BB/STACK = 50/100/10000) must match or the HH stack lines lie (recon gotcha).
3. Hand-ID dedup law (recon, hard): idbase auto-derived fresh per export — idbase = 50000 + (int(time.time()) % 10**7) * 2000 collision-checked against data/trainer/hh_ledger.jsonl; append {idbase, n_hands, file, session, ts} to the ledger BEFORE writing (IDs burn on first contact, even failed uploads — never reuse); refuse export if the derived idbase range overlaps any ledger entry.
4. Write with open(out, 'w', encoding='utf-8', newline='\r\n') exactly as pokerstars_export.py:246 (CRLF mandatory — LF files silently fail to analyze). All-in run-out board sections are handled inside the reused format_hand (post-2026-07-05 fix) — do NOT write a new formatter.
5. Off-tree sizing DECISION (documented in the module docstring, resolving the recon 'document the choice' item): export the human's RAW sizes — post-hoc snapping via _snap_hero would desync pot accounting of all later actions. Instead print a warning summary listing hero bets whose pot-fraction deviates > 10% from postflop.snap_to_tree's nearest tree size, with the note 'diese Spots graded der Analyzer degradiert/nicht'. Print the upload workflow (GTO Wizard → Analyze → Upload hands) + the CRLF/dedup reminders at the end of every run.
6. tests/test_export_gtow.py: build a fixture session via a short seeded TestClient run (incl. one forced all-in hand for the run-out case) → export → assert: file bytes contain b'\r\n' line endings; hand-block count == logged hand count; all hand IDs unique and >= 50000; the all-in hand's HH contains its '*** FLOP ***'/'*** TURN ***'/'*** RIVER ***' sections; every block has a '*** SUMMARY ***'; ledger gained one entry; a second export refuses the same idbase and derives a fresh one; a crafted bb=200 record is refused with the geometry error.
**Acceptance:**
- python -m tests.test_export_gtow exits 0 (CRLF, counts, unique fresh IDs >= 50000, all-in board sections, SUMMARY, ledger append, re-export gets new idbase, geometry guard).
- python -m pokerbot.coach.export_gtow --session latest on an autotest-generated session produces a .txt whose first hand block visually matches the PokerStars-HH shape of research/sixmax_export.py output (spot-checked in the test by asserting the 'PokerStars Hand #' header and 'Hold'em No Limit' line).
- The off-tree warning fires on a session containing a non-tree human bet size and is silent on a snapped-only session (both cases in the test via crafted export dicts).
**Size:** M

---

## Risks (aggregated from the design agents)
- HU-state adapter fidelity: record_to_hu_state must remap EVERY history 'player' field to 0/1 or _has_initiative (bot.py:916) / _villain_barrels (bot.py:498-514) silently misread the line — mitigated by porting gtowizard._selftest's locked asserts (P0-3 acceptance), but multi-raise preflop lines in 6-max collapsed to HU are an approximation; grades from that path carry the 'Bot-Einschätzung' label, never 'Mathe'.
- Pot-convention drift: three different pot conventions exist (api.required_equity as-faced, api.mdf pre-bet, F.compute_pot_odds P-before-bet); P0-7's deliberate-break test guards it, but any later refactor touching grader.py must keep understanding.py:76-81 as the reference.
- advance() bare-except (six_server.py:66-70) swallows bot errors into silent check/call — if grading is ever accidentally moved inside that loop, grader crashes become invisible; the design keeps grading in _log_if_done with its own visible error records.
- torch/advisor availability on the target machine: if the MLP files or torch are missing, every advisor check degrades to {available: False} — grades still compute (math+oracle) but the >=15%-support OK band disappears; autotest must pass in BOTH configurations (the None-guard unit check covers the degraded one).
- Latency assumption '~83 ms decide()' is post-memoization on this PC; first-call cold caches (blueprint/texture load) can spike the first graded hand — prewarm() in main() covers it, but if a future session skips main() (e.g. uvicorn import string) the first hand may exceed 800 ms once (harmless: it still lands inside the 2400 ms auto-advance window).
- Single global SESSION: two browser tabs share one session; P0 accepts this (no accounts by design) — registry rows make any resulting log interleaving diagnosable.
- P0 coupling: P1-C wires feedback to the P0 decision log. The renderer itself (P1-B) is fixture-driven and key-tolerant (.get everywhere, schema pinned to ../doctrine/TRAINER_DESIGN.md par.4 which is BINDING for P0 too), so P1-A/B/E build and pass standalone; but if P0 shipped only a JSONL and no in-memory per-hand list, P1-C's specified fallback (read decisions-JSONL tail by hand_id) must carry the load — the executing agent should verify which P0 variant exists before step 1 of P1-C.
- Rationale-shape drift: the four families (preflop/postflop/sparse-resolver/deepcfr) are covered by fixtures, but a FUTURE new rationale key renders only the generic fallback silently. Mitigation is built in (stderr warning + P4 autotest criterion 'every path renders'), but coverage is only as good as the fixture set.
- Encoding on Windows: German umlauts in .py/.jsonl/JSON responses — all files must be UTF-8; test prints should avoid raw umlauts to stdout (cp1252 console) or reconfigure sys.stdout; append_record already writes ensure_ascii=False.
- Auto-markup false positives in report prose (e.g. 'Bet' inside longer words) — mitigated by word-boundary regex + first-occurrence-only + templates using explicit G.term() instead of markup(); only the report free text uses markup().
- Feedback lifetime: last_feedback lives on the module-global SESSION and dies on 'Neu' (recon gotcha) — acceptable by design because the decision JSONL persists; do not store trainer state in-memory only anywhere else.
- Latency/imports: templates_de must not import pokerbot.strategy.advisor (torch load ~1-2s) — the render path is pure string work over already-computed P0 numbers; the acceptance check in P1-B guards this.
- P0/P1 contract drift: P2 pins the decisions file to data/sessions/decisions_<sid>.jsonl with per-record fields from ../doctrine/TRAINER_DESIGN.md §4 (human_action, oracle_action, grade, erklaerung_kurz, equity, pot_odds, mdf) and a hand_no (or hand_id) join key, plus a 'coach' key in view(). If the P0/P1 packages name these differently, ONLY replay.py's join + the #coach renderer need a one-line rename — build_replay_steps already joins tolerantly (explicit key preferred, positional street-order fallback) and treats a missing decisions file as decisions=[] (graceful degrade), so P2 cannot hard-block on P0/P1.
- Replay pot-math edge: an uncalled final bet stays in committed (and thus in t.pot()) until resolution — final_pot compares against the live view.pot captured at hand_over (same semantics, sum of committed), so the invariant holds; but the DISPLAYED final pot includes the refunded uncalled portion. Cosmetic only; the P2-5 unit test documents it.
- Hotkeys racing the request cycle: send() is async; a second keypress before the response could double-fire. Mitigation is in P2-3: a busy flag around api() (set before fetch, cleared after render) — cheap, but must not be forgotten or fast keyboard play produces 400 'not your turn' log spam.
- The global SESSION is one-per-process (six_server.py:125): opening /training and / simultaneously in two tabs shares the same session — acceptable for a single-user trainer, but the plan deliberately does not add multi-session plumbing (design doc: no accounts).
- Glossary term-marking over innerHTML: markTerms must escape BEFORE wrapping spans or a coaching string containing '<' breaks the DOM; the P2-7 static check asserts the escape call order (escape then wrap) by regex on the function body.
- advTimer leak paths: every new cancel point (glossary open, replay enter, filter focus) must clear the timer; missing one silently swallows a hand under the user mid-reading — the manual acceptance in P2-6 explicitly tests the replay path, the worst offender.
- training.html is a fork of six.html (~60% shared code): future fixes to the shared renderers must be applied to both files. Accepted trade-off: forking preserves the untouched, known-good play app and keeps both as zero-build single-file SPAs; the P2-7 git-diff check enforces that P2 never touches six.html.
- Cross-phase interface drift: P3-A assumes the P1 template renderer and the P0 decisions-JSONL schema ({hand_id, ts, street, spot_fp, human_action, grade, mode, timing}) exist under whatever names P0/P1 shipped — the executing agent must wire TemplateBackend and the corpus loader to the ACTUAL P0/P1 artifacts; the coach.v1 payload schema defined in P3-A is the binding contract both sides meet.
- grade_distribution_sanity (P4-A) is statistical: station>tag error-rate could flip on a small sample — mitigated by requiring >=200 graded decisions per arm and fixed seeds (42/43); if it still flips, raise --hands before declaring the grader broken (investigate protocol: compete the hypotheses).
- sixmax.py edits (READ_* constant hoist in P3-C, Knobs.exploit_gain in P3-D) touch the RL-league code shared with research harnesses — both are designed behavior-identical at defaults and MUST pass the seeded byte-identity run before merge (clean-code discipline).
- Ollama results are machine-dependent (no GPU guarantee on the user's PC); the <2s p95 gate may fail on this hardware — that is a valid measured outcome, templates simply remain default (the test is the deliverable, not Ollama passing).
- Storing table.history verbatim in the 'export' record (P4-B) grows session JSONL size and logs all bot hole cards (already true of build_hand_record) — post-hand-only exposure is the existing deliberate choice; the export file additionally reveals bot holes to GTOW, which is intended (validation, not live play).
- advance()'s bare-except fallback counter (BOT_FALLBACKS) only detects exceptions, not silently-wrong bot decisions — latency/grade checks partially cover this; deeper bot-correctness is out of the autotest's scope.

## Open questions of the designers (checked by the verifier — NONE blocks the one-run execution)

---

## Appendix V — Verifier protocol (already incorporated into the packages; here for traceability)
**API violations (corrected):**
- P0-2: `ALL_169_CLASSES` is cited as if it exists — verified absent (grep of pokerbot/strategy/ranges.py shows only named range helpers, no 169-class constant anywhere). api.equity DOES accept 169-class strings (recon), but the executing agent must construct the full 169-class list itself (13 pairs + 78 suited + 78 offsuit, trivial generator) or add it to ranges.py; do not import a nonexistent constant.
- P2-5: table.py line anchors 115-116 (blind seats), 170-172 (call adds chips), 189-190 (raise logs 'to') are NOT in recon (recon confirms the SEMANTICS via session_log.py:20 and the 'blinds'/'deal' drop at table.py:119/237, but not these line numbers). Semantics are safe to build on; the line cites must be re-verified before being used as edit anchors.
- P3-A: TemplateBackend 'delegates to the P1 template renderer (whatever module name P1 shipped)' — the design self-acknowledges it does not know the concrete symbol. P1-B defines it: pokerbot/coach/templates_de.render_hand_feedback / render_decision_feedback. Wire to those exact names, and note P1-B returns a dict {text, html, terms} while LanguageBackend.render_short returns str — the adapter must extract ['text'].
- P2-2 acceptance: 'if pokerbot.coach templates module exports USED_TERMS, assert coverage; else SKIP' — P1-B actually exports TERMS_USED (name mismatch). As written the P2-2 coverage check would silently SKIP forever, gutting the glossary-completeness gate. Not an invented API in the repo, but an invented symbol of a sibling package — align on TERMS_USED.
- P4-A: 'six_server.SESSION.table' — was unconfirmed by recon (recon only shows local `t`), but I verified it IS real: six_server.py:30 `self.table = Table(...)`. No change needed; recorded here because recon did not cover it.
- P2-3: `$('sl')` slider id was unconfirmed by recon — verified real at six.html:234/238/240. No change needed.

**Gaps (closed by P0-0 or marked optional):**
- No user-facing GTO/Exploit mode switch exists in any package. Verified: NewReq has only stack_bb (six_server.py:129); P0-6 reads mode from env POKERB_TRAINER_MODE at server start and P3-D applies exploit_gain from it — so switching modes requires restarting the server with a different env. The user requirement is 'GTO+exploit modes' as operable trainer modes. Fix (small): add mode:'gto'|'exploit' to NewReq + a toggle in training.html's Neu flow; P0-6's registry row and P3-D's Session-construction read the request value, env as fallback.
- Replay is last-hand-only (/api/replay/last). The design doc's replay-walkthrough requirement is arguably met, but there is no way to replay any earlier hand of the session even though the JSONL contains them all — flagging as a conscious scope cut the executing agent should confirm is intended (a ?hand_no= param on the same route would close it for ~10 lines).
- The P.S. glossary requirement ('every term in ANY coaching text explained, clickable') is covered by P1-A/P1-D/P2-2 + completeness tests, BUT only if the two parallel glossary systems are unified (see must_fix) — as designed, feedback text terms resolve against glossar_de.GLOSSAR while the collapsible panel renders static glossar.json, and nothing forces the two term sets to match. The requirement is only truly met after unification.
- All other requirements are covered: fairness grade bands (P0-4), per-decision logging (P0-1), session reports (P1-E), replay walkthrough (P2-5/6), hotkeys+speed+visibility (P2-3/4), 2560x1440 Snowie layout (P2-1), 'what the bot has learned' panel (P3-C), language-layer contract with templates default (P3-A/B), adaptive 10-20% band (P3-D), autotest with sanity criteria (P0-7/P2-7/P4-A), GTOW export validation (P4-B), simplicity (no accounts/gamification/mobile anywhere).

**Must-fix list (source of the ★ BINDING directives):**
- CROSS-PHASE FILE CONFLICT (the big one): P1-D, P3-C and P3-D all edit six.html, while P2-1 mandates six.html untouched, P2-1's acceptance asserts GET / still contains '1200px', and P2-7 hard-gates 'six.html byte-unchanged vs git' (git diff --quiet). In one run these packages cannot all pass as written. Resolution (encoded in the ordering above): run P2-1 (create training.html from the pristine six.html) BEFORE P1-D; re-target P1-D's coach div + collapsible panel + auto-advance gate, P3-C's bot-read panel, and P3-D's league-tier info line to training.html; P1-D's acceptance asserts on GET /training instead of GET /; keep P2-7's six.html gate.
- DUPLICATE GLOSSARY SYSTEMS: P1-A builds glossar_de.py (GLOSSAR dict, term()/markup(), GET /api/glossar via P1-C, schema {begriff, synonyme, erklaerung, formel, quelle}) while P2-2 independently builds static/glossar.json + GET /api/glossary + client-side markTerms with schema {begriff, erklaerung, syn}. Two registries, two endpoints, two term-marking mechanisms = guaranteed divergence and a broken completeness guarantee. Fix: glossar_de.GLOSSAR is the single source of truth; ONE endpoint (keep /api/glossar serving all_terms()); delete static/glossar.json (or generate it from GLOSSAR at startup); P2-2's collapsible panel consumes the unified endpoint; client markTerms is used ONLY for client-composed strings (ticker, replay one-liners) with the synonym list fetched from the same endpoint; server-rendered feedback html keeps its server-side term() spans and is never re-marked client-side. Also align TERMS_USED (P1-B) vs USED_TERMS (P2-2 check) so the coverage check cannot silently SKIP.
- GRADE/GRADE_TYP VOCABULARY DRIFT: P0-4 emits grade_typ in {pot_odds, mdf, sizing, advisor_freq, oracle_diff} and grade in {ok, teuer, leak}; P3-A pins coach.v1 grade_typ as {mix, ev, pot_odds, mdf, sizing, none} and adds grade 'none' which P0 never produces (P0-4 acceptance: every record has a non-empty grade); P3-D's error_rate excludes grade=='none'. Pin ONE enum — P0-4's — across coach.v1, templates_de, P2-6's replay renderer, and difficulty.error_rate; either delete 'none' or define exactly when it is emitted (e.g. ungradeable spots) and make P0-4 emit it.
- P0-1 PHANTOM-RECORD BUG: the design appends the decision record to _pending_decisions BEFORE t.act, but t.act itself raises the ValueError/RuntimeError that produces the 400 (recon: six_server.py:159-170) — an illegal amount would leave a captured record for an action that never happened, contradicting P0-1's own acceptance ('appends NO record'). Fix: build the snapshot before t.act, append to _pending_decisions only after t.act returns successfully.
- AUTO-DEAL SPEC CONFLICT: P1-D sets the hand-end timeout to 12000ms with a 'Weiter' button; P2-3 specifies T.AUTODEAL=3000 with Space-to-deal. After re-targeting P1-D onto training.html both edits land in the same file. Pick one: T.AUTODEAL=3000 + 'Weiter' button + Space, with the P2-3 cancel points (glossary open, replay enter, filter focus) — and update P1-D's static asserts (the '12000' check) accordingly.
- P4-A DRIVER ACTION MAPPING: SixMaxBot.decide can emit 'bet', but POST /api/action accepts only fold|check|call|raise|allin (recon, six_server.py:159-170). The autotest driver must normalize bet→raise and clamp amounts into [raise_min, raise_max] from view.legal, else the harness 400s and zero_crash fails spuriously.
- P0-2 must generate the any-two 169-class list itself (ALL_169_CLASSES does not exist — see api_violations) — add a tiny module-level generator or a ranges.py helper.
- P2-1 acceptance asserts 'id="gloss"' in /training but the collapsible panel is built in P2-2 — P2-1 must ship an empty <details id='gloss'> placeholder (consistent with its own DOM-order step d) or move that assert to P2-2.
- MODE TOGGLE (from gaps): add mode to NewReq + minimal UI toggle; P0-6 registry and P3-D bot construction read it from the request. Without this the two required modes exist only as a restart-time env var.

---

## Appendix A — Recon ground truths (verified APIs + integration points)

### WEB/SERVER
**Core APIs:**
- `GET /` — `index() -> str (HTMLResponse)` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:137-140). Re-reads static/six.html from disk on EVERY request ('read fresh each load so UI tweaks show on refresh') — UI edits need NO server restart. NOTE: module-level _INDEX at line 24 also reads six.html at import but is DEAD/unused; don't be misled by it.
- `POST /api/new_session` — `new_session(req: NewReq{stack_bb:int=100}) -> JSONResponse(view)` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:143-148). Sets the single module-global SESSION (line 125: `SESSION: Session | None = None`) — exactly ONE concurrent session per process, no accounts. Constructs Session(stack=stack_bb*100, sb=50, bb=100), calls start_hand() (deals + auto-plays bots to the human's turn), returns full view.
- `POST /api/hand` — `next_hand() -> JSONResponse(view)` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:151-156). Starts the next hand on the existing SESSION. 400 {error:'no session'} if SESSION is None. Client auto-calls this 2400ms after hand end (six.html:224 advTimer=setTimeout(nextHand,2400)).
- `POST /api/action` — `action(req: ActionReq{action:str, amount:int|None}) -> JSONResponse(view)` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:159-170). THE HUMAN ACTION ENTRY POINT. Validates hand not over + to_act==HUMAN (seat 0), then SESSION.human_action(action, amount). ValueError/RuntimeError from illegal actions -> 400 with error text. action strings: 'fold'|'check'|'call'|'raise'|'allin'; amount = raise-TO total in chips (bb=100).
- `GET /api/state` — `state() -> JSONResponse(view)` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:173-177). Read-only current view. The UI does NOT poll it — the frontend is purely request-driven: each POST returns the full new view and render(v) redraws everything. /api/state exists but is unused by six.html (useful for TestClient verification).
- `POST /api/analyze` — `analyze() -> JSONResponse(analyze_session(SESSION.path))` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:180-185). Lazy-imports pokerbot.analysis.session_analysis.analyze_session and runs it over the session JSONL. Returns {stats:{hands, vpip_pct, pfr_pct, threebet_pct, postflop_aggression_factor, went_to_showdown_pct, net_bb, bb_per_100}, narrative} (consumed in six.html:256-259). This is the existing end-of-session coaching seam.
- `Session.human_action` — `human_action(self, action: str, amount) -> list[dict] (bot events)` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:83-89). ★ THE DECISION-LOGGER HOOK. Line 85-86 snapshots street/preflop_raises/to_call BEFORE the act; line 87 `t.act(action, amount)` applies it; line 88 broadcasts to bot opponent-models; line 89 advances bots. Insert the trainer's per-decision capture BETWEEN line 86 and 87: at that point t.obs_for(HUMAN), t.legal_actions(), t.board, t.pot(), range-tracker state etc. are all pre-action — exactly what a grader needs to score the human's choice against PRINCE/GTO.
- `Session.advance` — `advance(self) -> list[dict] events` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:55-75). HOW BOTS ACT: while not hand_over and to_act is a bot -> obs=t.obs_for(seat) -> dec=self.bots[seat].decide(obs) -> t.act(dec['action'], dec['amount']). Bare-except fallback to check/call/fold so a bot bug never crashes the table (a swallowing trap for trainer-bot errors — errors become silent check/calls). Each bot action appended as {seat,name,pos,action,amount,street} -> returned as bot_events for UI log/animation. Ends with _log_if_done().
- `Session.__init__ / bots` — `Session(stack=10000, sb=50, bb=100); self.bots = {s: SixMaxBot(s, PROFILES[{1:'tag',2:'lag',3:'nit',4:'station',5:'maniac'}])}` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:28-38). ★ Opponents are the arena LEAGUE (pokerbot/arena/sixmax.py: SixMaxBot.decide line 320, .observe 253, .new_hand 247, decide_6max 201) — NOT the PRINCE engine (pokerbot/strategy/bot.py). The trainer's 'PRINCE v2.2 as basis' means either swapping/augmenting these decide() agents or (more likely) using the PRINCE bot purely as the GRADER of the human's decision. Session log path: config.DATA_DIR/sessions/session_<YYYYMMDD_HHMMSS>.jsonl; human_net/hands_done tracked here.
- `Session._log_if_done + Session.view` — `_log_if_done(self); view(self, events=None) -> dict` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:40-47 and 91-122). _log_if_done (called at the end of every advance()): when hand_over & result & not yet logged -> append_record(build_hand_record(t)), updates human_net (winners minus committed_total). view() = THE full UI state contract: {hand_no,button,sb,bb,street,board,pot,current_bet,to_act,hand_over,result,human_seat,session_net,hands_done,seats[{seat,name,stack,hole,folded,all_in,committed_street,is_human,is_button,is_turn,pos,net,won}],legal,bot_events}. Hole-card hygiene lives in seat_view (98-111): opponents show ['??','??'] unless folded ([]) or showdown-revealed. A trainer 'coach' payload extends THIS dict.
- `session_log.build_hand_record` — `build_hand_record(table) -> dict` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/session_log.py:8-32). TODAY'S RECORD (the replay substrate): {hand_no, button, sb, bb, human_seat, positions{seat->label}, hole{seat->cards, ALL seats incl. bots}, board (full runout), actions[{street,seat,pos,action,amount(to-total or amount),is_human}], result (table.result incl. winners/rank/reveal), net{seat->chips}}. It FILTERS table.history to 'player' entries only — the 'blinds' and per-street 'deal' events (table.py:119,237) are dropped; street timing is still recoverable because each action carries its street and board order is deterministic (3 flop/1 turn/1 river). MISSING for the trainer: per-decision pot/to_call/stack snapshots, legal-action menu, any grading fields — the trainer's data layer must add these (either richer records or a parallel decisions.jsonl).
- `session_log.append_record` — `append_record(path, rec: dict) -> None` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/session_log.py:35-38). mkdir -p + append one JSON line (ensure_ascii=False). Reuse as-is for the trainer's decision log.
- `Table.legal_actions` — `legal_actions() -> {to_act,to_call,can_fold,can_check,can_call,call_amount,can_raise,is_bet,raise_min,raise_max,pot}` (C:/Users/hampe/Desktop/PokerB/pokerbot/engine/table.py:131-149). raise_min/raise_max are commit-TO totals; is_bet distinguishes bet vs raise. This dict is what six.html renders the action bar from (renderBar, six.html:227-247) and what the decision logger should store verbatim per decision.
- `Table.obs_for` — `obs_for(seat) -> {hole,board,to_call,pot,my_stack,bb,n_active,position,preflop_raises,cur_bet,my_committed_street,street,can_check,can_call,can_raise,raise_min,raise_max}` (C:/Users/hampe/Desktop/PokerB/pokerbot/engine/table.py:301-313). The canonical bot observation. Note it contains NO action history — SixMaxBot gets history via observe() callbacks; a PRINCE-based grader needing line/range context must be fed table.history (or the range tracker) separately.
- `six.html frontend` — `static SPA, no framework, no websockets; fetch POST -> render(view)` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/static/six.html (264 lines)). Fixed 1200x680 #stage scaled to the window via fit() (lines 138-140) -> 2560x1440 works by upscaling but a Snowie-style layout means enlarging the stage/panel geometry. Layout: table at left 18-898px, action .bar below it (top:578), RIGHT PANEL .panel at left:916px width:266px (lines 100-109, DOM 121-126) containing session-net, Neu/Analyse buttons, #out (analysis text), #log (action history) — ★ this is where the coaching panel + collapsible glossary attach (widen it or replace its content sections). Actions sent via send(a,amt) line 252-255 -> POST /api/action; bot actions animated from view.bot_events; ALL state comes as the POST response (no polling loop). Hand-end: renderEnd (209-225) shows banner + auto-advances via advTimer=setTimeout(nextHand,2400) at line 224 — replay/coaching walkthrough MUST cancel or gate this timer (send() already clears it, line 252). German UI strings throughout; RANK_DE dict (133-135) is a mini precedent for the German glossary.
**Integration points:**
- Human decision logger: pokerbot/web/six_server.py:83-89 Session.human_action — insert pre-action capture between the snapshot (lines 85-86) and t.act (line 87); everything needed (t.obs_for(HUMAN), t.legal_actions(), t.history, pot, board) is available pre-mutation. The route wrapper is action() at six_server.py:159-170.
- Grading/coach response: pokerbot/web/six_server.py:91-122 Session.view — add a 'coach' key (per-decision feedback, grade band, glossary term ids) to the returned dict; the frontend render(v) in six.html:157-201 is the single redraw point to display it.
- Bot opponents / PRINCE wiring: pokerbot/web/six_server.py:36-38 (Session.__init__ bot assignment, SixMaxBot from pokerbot/arena/sixmax.py) and six_server.py:58-73 (advance loop calling bots[seat].decide(obs)) — swap in or wrap a PRINCE-profile agent here; PRINCE v2.2 itself = POKERB_PRINCE=1 profile over pokerbot/strategy/bot.py + pokerbot/strategy/gto_mode.py (exploit mode = same + exploit=True).
- Per-hand data layer: pokerbot/web/session_log.py:8-32 build_hand_record (extend fields) + six_server.py:40-47 _log_if_done (the single call site); append_record (session_log.py:35-38) is reusable for a parallel per-decision JSONL.
- Replay mode data: the session JSONL already stores all-seat hole cards, full board, and street-tagged action list per hand — sufficient to reconstruct a walkthrough; serve it via a new GET route reading SESSION.path (pattern: analyze() at six_server.py:180-185, which lazy-imports and reads the same file).
- UI attach points in pokerbot/web/static/six.html: right panel .panel CSS lines 100-109 / DOM lines 121-126 (coaching + collapsible glossary panel); action bar renderBar lines 227-247 (per-decision feedback moment); renderEnd line 224 advTimer auto-advance (must be suppressed in replay/walkthrough mode); fit() lines 138-140 (stage scaling for 2560x1440 — enlarge stage width to make room for a wider trainer panel).
- Analysis seam: POST /api/analyze -> pokerbot/analysis/session_analysis.analyze_session(SESSION.path) (six_server.py:180-185) — the existing end-of-session report the trainer's session summary can extend or replace.
- Server entry: six_server.main() at six_server.py:188-202 (argparse --host/--port/--open, uvicorn log_level='warning', port 8000; launcher 'PokerB 6max spielen.bat'). A trainer server can be a new module following this exact template.
**Gotchas:**
- Single module-global SESSION (six_server.py:125): one session per process, 'Neu' button replaces it wholesale; any trainer state stored on Session dies with it — persist to the JSONL/data layer, not in-memory only.
- six_server serves six.html FRESH per request (six_server.py:139) so HTML/JS edits need no restart — but the module-level _INDEX (line 24) is a dead read; the HU server.py and others CACHE HTML at import and DO need a restart (CLAUDE.md convention). Python code changes always need a restart (uvicorn runs without --reload).
- advance() swallows ALL bot exceptions into a silent check/call/fold (six_server.py:66-70) — a broken trainer-bot integration will not crash, it will quietly play fit-or-fold; verify bots via logs/tests, not by absence of errors.
- Client auto-advances to the next hand 2400ms after hand end (six.html:224) and human send() clears that timer (line 252) — replay walkthrough and post-hand coaching must cancel/gate advTimer or the hand disappears under the user.
- Amount semantics: raise 'amount' is the total commit-TO in chips (bb=100 chips), not an increment; legal_actions raise_min/raise_max are TO-totals; history stores raises as 'to' and calls as 'amount' (session_log.py:20 merges them via h.get('to') or h.get('amount')).
- build_hand_record drops non-player history events ('blinds', 'deal' at table.py:119/237): the record has no explicit per-street board reveal events — replay must reconstruct flop/turn/river from board order + action street tags.
- obs_for contains NO action history or opponent info — SixMaxBot builds context from observe() callbacks (six_server.py:49-53 broadcasts every public action). A PRINCE grader needs table.history / a range tracker fed separately.
- view() hole-card hygiene (seat_view, six_server.py:98-105): the API never leaks unrevealed bot holes to the client (?? backs), BUT the session JSONL logs ALL hole cards — a replay endpoint that streams raw records reveals bot holes; for the trainer that is intended for review, just be deliberate about DURING-hand vs post-hand exposure.
- A backgrounded server killed via TaskStop can hold port 8000: Get-NetTCPConnection -LocalPort 8000 -> Stop-Process -Id <pid> -Force (CLAUDE.md).
- Verify UI/server via fastapi.testclient.TestClient, not screenshots (CLAUDE.md; the screenshot tool hangs). All routes are trivially TestClient-drivable since state is pure request/response with no websockets.
- Frontend is a fixed 1200x680 stage uniformly scaled (six.html fit()) — at 2560x1440 everything just upscales ~2.1x; a genuinely wider Snowie-style layout (big right coaching column) requires changing stage dimensions + the absolute-positioned geometry (SC seat coords are %-of-felt so they survive; .panel/.bar/.hdr use absolute px).
- Session bots share NOTHING with the shipped PRINCE engine: today's opponents are heuristic league profiles (arena/sixmax.py Knobs); PRINCE env-flag profiles (POKERB_PRINCE=1) are process-global env config — running PRINCE-graded coaching AND league opponents in one process is fine (flags only affect strategy/bot.py paths), but be explicit about which component reads which flags.
**Reuse instead of rebuild:**
- session_log.append_record + the JSONL-per-session pattern (session_log.py:35-38) — reuse verbatim for the trainer's per-decision data layer (a parallel decisions_<sid>.jsonl next to session_<sid>.jsonl).
- build_hand_record (session_log.py:8-32) — already captures 90% of what replay needs (all holes, board, street-tagged actions with positions/amounts, result, per-seat net); extend, don't rewrite.
- Session.view() contract + six.html render(v) single-redraw architecture — add trainer payload keys to the same dict instead of inventing a second channel; no websockets/polling needed.
- The request-driven no-poll frontend pattern (api() helper six.html:249, POST returns full state) — keeps the trainer server dead simple and TestClient-verifiable.
- Table engine as-is (pokerbot/engine/table.py): legal_actions/obs_for/act/history/result are complete for trainer needs, including side pots and all-in run-outs — zero engine changes required for logging or replay.
- SixMaxBot league (pokerbot/arena/sixmax.py PROFILES tag/lag/nit/station/maniac + observe() opponent-model feed) — keep as the default opponent field; the PRINCE engine slots in only as the grader/GTO reference.
- POST /api/analyze -> analysis/session_analysis.analyze_session — the existing session-summary machinery (VPIP/PFR/3bet/AF/WTSD/net + German narrative) is a working base for the trainer's session report.
- six.html assets: card renderer card()/setCards, chipStack, seat layout SC coords, German strings + RANK_DE dict (a glossary micro-precedent), fit() scaling, action-bar builder renderBar with legal-action-driven buttons and pot-fraction quick sizes — all directly reusable in the trainer UI.
- main()/uvicorn/--open launcher template (six_server.py:188-202) + the .bat launcher pattern for the trainer's entry point.
- fastapi.testclient.TestClient verification pattern (CLAUDE.md) — the mandated autotest for every trainer route can drive new_session -> action -> hand -> analyze end-to-end headlessly.

### BOT/ORACLE
**Core APIs:**
- `PokerBot.decide` — `decide(self, state: dict) -> {"action": str, "amount": int|None, "rationale": dict}` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/bot.py:190). Raises ValueError unless state['legal']['to_act'] == self.hero_idx. action in {fold, check, call, bet, raise, allin}; amount is a TOTAL street-commitment target ('raise to'), None for fold/check/call/allin. REQUIRED state keys (the HU game.state() shape, NOT table.obs_for): 'legal' (to_act, to_call, can_check, can_call, can_raise, is_bet, raise_min, raise_max, pot; game.py:97-128), 'players' = list of EXACTLY 2 dicts each with hole/stack/committed_street/committed_total/all_in/folded (villain hole may be ['??','??'] — never read), 'street', 'board', 'pot', 'bb', 'button' (index of SB/BTN), 'current_bet', 'history' (entries {player, action, street[, to|amount]} plus deal markers {action:'deal', street, board}), optional 'hand_id' (keys the per-hand LINE_U draw — PRINCE ships POKERB_LINE_U=1, so pass a stable hand_id per hand). gtowizard.py:85-147 gtow_to_state is the canonical external state-builder precedent (hero forced to index 0, villain hole hidden).
- `PokerBot.__init__` — `PokerBot(hero_idx: int, seed: int | None = None, exploit: bool = True)` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/bot.py:134). seed=None -> nondeterministic rng. Line 143-144: if flag('POKERB_EXPLOIT')=='0' the constructor's exploit=True is FORCIBLY overridden to False — and PRINCE v2.2 (POKERB_PRINCE=1 implies POKERB_GTO_MODE=1 whose PROFILE sets POKERB_EXPLOIT='0'). So for the trainer's exploit mode you MUST set os.environ['POKERB_EXPLOIT']='1' explicitly (explicit env beats the profile in gto_mode.flag); exploit=True alone is silently ignored under PRINCE. Instance attrs settable after construction: use_resolver / use_turn_resolver (default False — bot.py does NOT read POKERB_RESOLVER env; wiring is external, see gtowizard harness), use_river_advisor, use_range_tracker, hero_idx (reassignable — gtowizard.py:208 does self.bot.hero_idx = 0 per call). Construction is cheap; advisor MLPs/blueprint/texture tables are lazily-loaded module-level caches shared read-only across instances.
- `rationale dict (decide() output)` — `rationale keys per path — always includes 'reasoning': str (added by _mk, bot.py:1242-1245)` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/bot.py:1242). PREFLOP base (line 259): phase='preflop', hand, hand_class, percentile, eff_stack_bb, position ('SB/BTN'|'BB'). Push/fold adds: mode='push/fold', optionally cfr={jam,call,...} or equity_vs_shove+required_equity. Blueprint path adds: blueprint={node,picked,pct,dist}, optionally blueprint_clamp/limp_clamp. bb_vs_open adds: vs_range='SB open', equity, required_equity, realization. vs_3bet adds: vs_range='BB 3bet', equity, required_equity. POSTFLOP base (line 560): phase='postflop', street, hand, made_hand, board, equity, villain_combos, position ('IP'|'OOP'), texture (list[str]), fold_model ('learned'|'prior'), opponent (opp.summary() dict, or None when exploit off). Facing-bet adds: required_equity, mdf, facing_bet, call_threshold, villain_aggr; optionals: defense_advisor=[pf,pc,pr]+size_faced, pair_defense, pair_defense_mdf, turn_defense, river_defense, barrel_discipline, raise_commit, river_blocker. Bet-side optionals: advisor_pbet / advisor_pbet_turn / advisor_pbet_river, turn_probe, river_thin_sel, cbet_damp, slowplay, exploit_river={size,mode,lam,vs_floor,model_n,ev}, river_probe={size,n,spent_bb}. RESOLVER paths (river line 1042, turn line 1069) return a SPARSE shape: phase, street, hand, made_hand, board, range_conf, resolver=True — NO equity/mdf/required_equity. DEEPCFR path (line 235): phase='deepcfr', hand, street, probs. Trainer template renderer MUST handle the sparse resolver and deepcfr shapes (P7 autotest criterion 'every rationale path renders').
- `PokerBot.observe_opponent / observe_hand_end` — `observe_opponent(street: str, action: str, facing_bet: bool) -> None; observe_hand_end(final_state: dict | None = None) -> None` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/bot.py:1248-1276). The ONLY cross-hand learning entry points. observe_opponent feeds self.opp (OpponentModel: fold_to_bet_freq/aggression/confidence). observe_hand_end: opp.end_hand() + pairs this hand's _river_keys with villain responses parsed from final_state['history'] into self.opp_model (Dirichlet OppModel) — the 'What the bot has learned about you' panel data source is game_bot.opp_model / game_bot.opp, read-only. NEVER call these on the oracle instance.
- `gto_mode.apply / flag / fingerprint` — `apply() -> bool (expand PRINCE/GTO profiles into env via setdefault); flag(name, default) -> str (env > PRINCE_PROFILE > PROFILE > default); fingerprint() -> dict` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/gto_mode.py:122,108,135). Set POKERB_PRINCE=1 (or POKERB_FINAL=1) and call gto_mode.apply() BEFORE importing pokerbot.strategy modules — postflop.py/advisor.py read flags at IMPORT time (module docstring line 6). PRINCE_PROFILE (line 42): GTO_MODE=1, TURN_DEFENSE=0.07, SLOWPLAY=0.25, LINE_U=1, RIVER_ECALL=1, SIZE_INJECT=1, TRACKER_AGGRO_FULL=1; the implied GTO PROFILE (line 12) adds EXPLOIT=0, RIVER_VALUE=0.75, RIVER_LA=1, ONTREE=1, tracker damping, census tree/size flags, RSV_CENSUS=1. Log fingerprint() with every graded session (trainer QA doctrine).
- `HeadsUpGame.state / legal_actions` — `state(hide: int | None = None) -> dict (the exact shape decide() consumes); legal_actions() -> dict` (C:/Users/hampe/Desktop/PokerB/pokerbot/engine/game.py:290,97). game.state() IS the decide() input contract (server.py:62-63 passes it directly). state(hide=BOT) redacts a seat's hole for UI/logging.
- `Table.obs_for — NOT decide()-compatible` — `obs_for(seat: int) -> flat dict {hole, board, to_call, pot, my_stack, bb, n_active, position, preflop_raises, cur_bet, my_committed_street, street, can_check, can_call, can_raise, raise_min, raise_max}` (C:/Users/hampe/Desktop/PokerB/pokerbot/engine/table.py:301). No 'legal', no 'players', no 'history', no 'button' — PokerBot.decide cannot consume it. Confirmed authoritatively at brain/solver_policy.py:29: 'PokerBot.decide() needs the HU game's state() dict, which a Table does not produce.' The 6-max Table's decision engine is SixMaxBot.decide(obs) / decide_6max(obs) (arena/sixmax.py:84 _decide accepts an injected rng).
- `pf_advisor.p_bet / p_defense (direct mix access for grade bands)` — `p_bet(hole, board, role, street='flop', pot_type=None) -> float|None; p_defense(hole, board, role, size_faced, street) -> (p_fold, p_call, p_raise)|None` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/advisor.py (imported as pf_advisor at bot.py:18; call sites bot.py:586, 759, 785, 815)). decide() returns ONE SAMPLED action — for the fairness doctrine's mixed-support band ('Advisor-Frequenz >= 15%') the trainer should read the mix directly: the facing-bet triple from p_defense and the bet/check split from p_bet, which decide() also surfaces in rationale (defense_advisor / advisor_pbet*). Grading by 'is the human action inside the mix' needs these frequencies, not repeated decide() sampling.
**Integration points:**
- Oracle instantiation (P0 Oracle-Diff): create a DEDICATED PokerBot per trainer session — oracle = PokerBot(hero_idx=0, seed=<fixed int>, exploit=False) — and build the human's spot as a HU game.state()-shaped dict with the human forced to index 0 (precedent: gtow_to_state, pokerbot/benchmark/gtowizard.py:85-147, which proves villain hole may be ['??','??'] and hero_idx may be reassigned per call at gtowizard.py:208). Never share the instance with any live game bot and never call observe_* on it.
- Existing proven pattern to copy: pokerbot/web/server.py:30-31 — the HU server already runs self.bot = PokerBot(BOT, exploit=True) for play and a SEPARATE self.advisor = PokerBot(HUMAN, exploit=False) called per human action at server.py:76 (reco stored in last_human for review). This is exactly the per-spot-oracle pattern the trainer needs; it has run in production.
- 6-max reality check (binding for the plan): the six_server game bots are SixMaxBot instances (pokerbot/web/six_server.py:38, arena/sixmax.py), NOT PokerBot — so there is no live PokerBot state to corrupt in the 6-max app at all. But PokerBot itself is HU-ONLY: villain is hardcoded as 1 - self.hero_idx (bot.py:316, 371, 612, 694, 929, 941 ...), _villain_barrels assumes strict HU actor alternation (bot.py:498-514), and state['players'] must have exactly 2 entries. For MULTIWAY spots the oracle must be SixMaxBot._decide/decide_6max (obs_for shape, injectable rng — sixmax.py:84) plus the math grades (pot-odds/MDF from engine formulas, PokerBot-independent); PRINCE-grade PokerBot verdicts are only available once the hand is heads-up, via a Table→HU-state adapter modeled on gtow_to_state (remap the two live seats to indices 0/1 INCLUDING every history 'player' field — _has_initiative bot.py:916 and _villain_barrels parse them).
- Bot config seam: trainer server startup must set POKERB_PRINCE=1 and call gto_mode.apply() BEFORE any pokerbot.strategy import (import-time flag reads, gto_mode.py:6). GTO-mode bot = PokerBot(seat, exploit=True-but-clobbered-to-False-by-profile); exploit-mode bot additionally needs os.environ['POKERB_EXPLOIT']='1' set explicitly, or the profile silently forces exploit=False (bot.py:143-145).
- Resolver knobs for the design doc's uncertainty hierarchy ('river = resolver = hard verdicts'): set oracle.use_resolver=True (and optionally use_turn_resolver=True) on the ORACLE instance only — they are plain attributes defaulting False (bot.py:175-176); bot.py never reads POKERB_RESOLVER env (the harness wires it). Resolver-ON changes the rationale to the sparse resolver shape (no equity/mdf) — renderer must branch.
- Per-decision data layer: the decision record's advisor_dist / equity / pot_odds / mdf fields map 1:1 onto the rationale keys enumerated above (equity, required_equity, mdf, call_threshold, defense_advisor, advisor_pbet*); spot_fp should include gto_mode.fingerprint() once per session.
- 'What the bot has learned' panel (P3): read the LIVE exploit-mode game bot's self.opp_model (Dirichlet per-node posteriors, opp_model.posterior(node_key(...)) — bot.py:1118-1127) and self.opp.summary() — read-only display, no writes.
**Gotchas:**
- decide() is NOT read-only. Per call it mutates: (1) self.rng — the Mersenne stream advances (1500-iter MC equity + every mixing draw), so calling decide() on a shared instance perturbs that bot's future mixed actions; (2) self._river_keys — RESET whenever street=='preflop' (bot.py:203) and APPENDED on exploit river bets/probes, so an interleaved oracle call on a live bot desyncs its observe_hand_end pairing (the count-match guard at bot.py:1271 then silently DROPS the learning rather than corrupting it); (3) self._hand_u / self._hand_u_by_id — under PRINCE (LINE_U=1) every preflop decide draws and stores the hand's line-draw, overwriting the live bot's barrel-plan coherence for that hand_id; (4) self._probe_spent_bb (exploit probe budget); (5) per-call scratch _cur_state/_cur_hole/_cur_board/_cur_street/_cur_committed/_ecall_exact_to (harmless single-threaded, but decide() is not re-entrant — never call the same instance from two threads).
- Cross-hand learned state lives ONLY in self.opp (OpponentModel) and self.opp_model (Dirichlet OppModel), fed exclusively by observe_opponent/observe_hand_end — decide() itself never writes them. RangeTracker is rebuilt from the state dict on every call (bot.py:969, RangeTracker().build(state)) — stateless across calls, no corruption vector.
- Exploit clobber trap: under PRINCE, PokerBot(..., exploit=True) yields an exploit=OFF bot because the profile's POKERB_EXPLOIT='0' wins at bot.py:143-145 (it only forces OFF, never ON). The trainer's exploit mode must set the env var explicitly. Conversely the ORACLE should stay exploit=False in BOTH modes — grading the human against the GTO baseline, not against the bot's Dirichlet read of the human.
- Reproducibility: seed the oracle (seed=None is nondeterministic) AND note that two decide() calls on the identical state still differ because the rng stream advances; for repeatable grading either derive a per-spot seed (e.g. random.Random(hash(spot_fp))) into a fresh rng, or accept stream order. Full process determinism additionally needs the PYTHONHASHSEED discipline already fixed in ranges.combos_for_classes (CLAUDE.md clean-code note). Also pass a stable state['hand_id'] — with LINE_U on, omitting it makes concurrent/interleaved hands share the scalar _hand_u fallback (the exact H1 bug, bot.py:137-141).
- amount semantics: bet/raise amount is the TOTAL street commitment ('raise to'), clamped by game.act to [raise_min, raise_max] (game.py:170); 'allin' passes amount=None. When diffing human sizing vs oracle sizing, compare street-commitment targets, not chips-added.
- Latency: decide() ~83 ms post-memoization (12.1x fix, commit 114e107) — fine for per-decision grading; use_resolver=True adds a real river solve on cold cache (POKERB_SOLVE_CACHE/POKERB_ISO_CACHE are fingerprint-relevant seams, gto_mode.py:83-86).
- The rationale shape is path-dependent (three families: preflop / postflop / sparse-resolver / deepcfr) — the glossary+template renderer must cover ALL enumerated keys or the P7 autotest criterion 'every rationale path renders text' fails. required_equity notably appears in BOTH preflop and postflop facing-bet shapes but is absent in first-in bet shapes and resolver shapes.
- PRINCE v2.2 is the validated live anchor (AIVAT -19.70 n=2393); the post-v2.2 levers (PAIR_DEFENSE, RIVER_DEFENSE, PURIFY, RAISE_NARROW, OVERBET_MENU, TURN_DEF_ADVISOR) are EXCLUDED default-OFF flags (gto_mode.py:56-66) — the trainer must not set them.
- Do NOT reproduce the six_server 'fake mixing' bug: sixmax.py:96-97 warns per-spot reseeding makes every frequency deterministic (no real mixing); if SixMaxBot is used as the multiway oracle, give it one persistent injected rng (solver_policy.py:32-33 pattern: bot.rng = random.Random(seed)).
**Reuse instead of rebuild:**
- pokerbot/web/server.py Session (lines 27-95): the complete HU trainer skeleton already exists — separate play-bot + advisor-oracle instances, last_human={state, action, amount, reco} capture, per-hand JSONL harvest, coach hook. The trainer's P0 oracle-diff is this pattern generalized.
- pokerbot/benchmark/gtowizard.py gtow_to_state (85-147) + its _selftest (222-275): the canonical recipe for synthesizing a decide()-compatible HU state dict from a foreign game representation, including the to_call committed-delta fix, hidden villain hole, hand_id passthrough, and locked asserts — the template for any Table→HU-state adapter.
- pokerbot/strategy/gto_mode.py fingerprint(): ready-made config provenance for every graded session record (QA doctrine: GTOW-Analyzer second opinion needs the exact flag set).
- pokerbot/arena/sixmax.py decide_6max/_decide + PROFILES (nit/tag/lag/station/maniac + held-out rock/whale/shark): the multiway oracle AND the difficulty-ladder opponent league (10-20% error band regulator) — both already built; _decide accepts an injected rng (sixmax.py:84-97).
- pokerbot/brain/solver_policy.py _default_fallback (26-34): the established SixMaxBot-as-policy wrapper with its own seeded rng — reuse for the autotest harness where league bots play the human seat.
- The rationale dict itself + pokerbot/brain/understanding.py strategic_read: the design doc's explanation payload — decide() already emits per-decision equity/pot-odds/MDF/advisor frequencies; no new math layer needed for P0 grades.
- pokerbot/web/session_log.py append_record/build_hand_record + data/sessions JSONL: the existing per-hand logging the per-DECISION records extend.
- pf_advisor.p_bet/p_defense direct calls: the mixed-strategy frequencies for the >=15%-support grade band — no need to Monte-Carlo decide() to estimate the mix.

### MATH/EXPLANATION
**Core APIs:**
- `strategic_read` — `strategic_read(spot) -> str` (C:/Users/hampe/Desktop/PokerB/pokerbot/brain/understanding.py:32). Input = the canonical Spot dataclass (duck-typed: needs .street, .board, .bb, .pot, .to_call, .hero_pos, .hero_seat, .hero_hole, .seats list of {seat,pos,stack,folded}, .line list of {street,action,hero}, .b(chips) method). Output = ONE English NL block: 'STRATEGIC READ ...' + bullet lines Geometry (SPR/position/required-equity/MDF), Hand vs board (api.hand_rank + texture), Initiative, Principle (street-specific measured heuristic). Deterministic, $0, sub-ms. For the trainer this is the ready-made explanation SKELETON — but it is ENGLISH; the German coaching text must be generated/mapped, not taken verbatim. Named tunable constants at understanding.py:21-26 (RIVER_BET_MEDIAN_X=0.33, RIVER_CHECK_RATE=0.58, SPR_COMMITTED=1.0, SPR_DEEP=6.0, STRONG_MADE=0.62, MEDIUM_MADE=0.40) are directly reusable as grade-band thresholds. Internal helpers _in_position(spot), _effective_stack_bb(spot) (understanding.py:41,49) are private but importable and exactly what a grader needs for position/eff-stack.
- `Spot / spot_from_table / format_spot` — `@dataclass Spot(street, board, bb, hero_seat, hero_pos, hero_hole, pot, to_call, n_active, seats, legal, line, villain_fold=0.5, villain_aggro=0.5); spot_from_table(table, seat) -> Spot; format_spot(spot) -> str; Spot.b(chips) -> float(bb)` (C:/Users/hampe/Desktop/PokerB/pokerbot/brain/format_spot.py:42,81,112). spot_from_table builds the canonical Spot from the live engine Table at a decision point — the trainer should capture ONE Spot per human decision (this is the per-decision data-layer record). All chip fields RAW chips; bb=100. legal dict = {can_fold,can_check,can_call,can_raise,raise_min,raise_max} in chips. line = ordered public actions with amount_bb and hero flag. Env gates POKERB_MADE_HAND (default ON), POKERB_SOLVER_FREQ (default OFF), POKERB_UNDERSTANDING (default OFF) alter format_spot output — irrelevant for grading (grader calls primitives directly), but Spot itself is the shared input shape. ACTION_RE at format_spot.py:15 parses 'ACTION: <verb> [N]' if ever needed.
- `advisor.p_bet` — `p_bet(hole: list[str], board: list[str], role: 'IP'|'OOP', street: 'flop'|'turn'|'river' = 'flop', pot_type: 'srp'|'3bet'|'4bet'|None = None) -> float | None` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/advisor.py:87). THE advisor-frequency check: solver P(bet) for hero's exact hand at that node (flop donk/c-bet, turn lead/barrel, river). None => advisor unavailable for that street -> grader must skip the frequency check (never treat None as 0). Memoized (120k-entry dict), CPU MLP (3x64 Linear) — microseconds after first torch load (~1-2s one-time). Gate with advisor.available(street) (advisor.py:74). pot_type only routes when POKERB_RIVER_LA=1 (default OFF). Convenience wrapper: api.solver_freq(hole, board, role, street) (api.py:161) = same with try/except+availability built in. Role convention: HU = BTN/SB is IP (see format_spot.py:127).
- `advisor.p_defense` — `p_defense(hole, board, role, size_faced: float, street: str = 'flop') -> tuple[float, float, float] | None  # (P_fold, P_call, P_raise)` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/advisor.py:144). The FACING-BET action distribution — this is the grader's tool for 'was fold/call/raise reasonable', complementing p_bet (which only covers the betting node). size_faced = bet as a fraction of the pot it was bet into (x pot). Softmax 3-tuple sums to 1. None => fall back. Memoized, same latency profile. Gate with advisor.defense_available() (advisor.py:140). No api.py wrapper exists — the trainer grader should import pokerbot.strategy.advisor directly or add a thin api wrapper.
- `api.required_equity / api.pot_odds / api.mdf / api.spr / api.outs_equity` — `required_equity(to_call, pot) -> float; pot_odds(to_call, pot) -> (ratio, required_equity); mdf(bet, pot) -> float; spr(effective_stack, pot) -> float; outs_equity(outs, cards_to_come=2) -> float` (C:/Users/hampe/Desktop/PokerB/pokerbot/brain/api.py:63-85). The pot-odds/MDF checks call these DIRECTLY — pure arithmetic, zero latency. CONVENTION TRAPS: (1) api.required_equity(to_call, pot) expects pot = the pot AS FACED (villain's bet already in it) -> req = to_call/(pot+to_call); the Spot.pot at a facing-bet decision point already includes the bet, so req = api.required_equity(spot.to_call, spot.pot) is correct as understanding._geometry does it (understanding.py:77). (2) api.mdf(bet, pot) argument ORDER is (bet, pot) but it forwards to formulas.minimum_defense_frequency(pot, bet) — pot here = PRE-bet pot: pot_pre = spot.pot - spot.to_call (understanding.py:79-81 is the reference usage). (3) api.pot_odds passes (pot, to_call, to_call) to compute_pot_odds treating pot as already containing the bet — same convention. Get these two conventions wrong and every grade is off by the bet size.
- `api.hand_rank / api.board_texture / api.hand_class_of` — `hand_rank(hole, board) -> (made_hand_name: str, strength: float 0..1) | ('preflop', None); board_texture(board) -> {'paired','monotone','twotone','connected','high','dynamic': bool}; hand_class_of(hole) -> str ('AKs')` (C:/Users/hampe/Desktop/PokerB/pokerbot/brain/api.py:89-103). Exact treys evaluation, deterministic, microseconds. strength = 1 - treys/7462; tier bands in understanding.py (>=0.62 strong, >=0.40 marginal). board_texture wraps classify_board (postflop.py:76) — returns all-False dict for board <3 cards. These feed the German explanation layer (hand category + Board-Textur in plain words).
- `api.equity` — `equity(hero: list, villain: list[str]|list[tuple]|dict, board=None, iters=None, seed=0) -> float` (C:/Users/hampe/Desktop/PokerB/pokerbot/brain/api.py:29). Villain polymorphic: 169-class strings, combo tuples, or weighted dict {combo: w}. iters=None reads modes.current().equity_iters (STANDARD=1000, FAST=300, DEEP=8000 — modes.py:36-39). seed=0 default => DETERMINISTIC (same spot always grades the same — important for reproducible feedback + autotest). LATENCY: river with a complete board is EXACT enumeration (zero MC cost, equity.py:23,50-60,92); flop/turn MC at 1000 iters ≈ tens of ms; 3000 iters (DEFAULT_ITERS in equity.py:12) ≈ ~0.9pp standard error. For live grading, iters=1000 (STANDARD) is comfortably within a UI budget; wrap grading in modes.using('standard') or pass iters explicitly.
- `equity_vs_hand / equity_vs_range / equity_vs_weighted_range / equity_vs_class_range` — `equity_vs_hand(hero, villain, board=None, iters=3000, rng=None) -> float; equity_vs_range(hero, villain_combos: list[tuple], board, iters, rng) -> float; equity_vs_weighted_range(hero, combo_weights: dict, board, iters, rng) -> float; equity_vs_class_range(hero, classes: list[str], board, iters, rng) -> float` (C:/Users/hampe/Desktop/PokerB/pokerbot/engine/equity.py:15,38,79,120). Raw engine layer; prefer api.equity on top (mode-aware iters + seeded rng). All return NaN on an empty/dead range — grader must guard. Pass rng=random.Random(seed) for determinism.
- `formulas (knowledge_base.math.formulas, imported in api.py as F)` — `compute_pot_odds(P, B, C) -> (ratio, required_equity); equity_needed_to_call(pot_before_call, call_amount, villain_risked=0) -> float; expected_value(probs, payoffs) -> float; minimum_defense_frequency(pot, bet) -> float; bluff_to_value_and_frequencies(P, B, n_value, n_total) -> (r, n_bluff, f_bluff, f_bluff_bet, p_value_given_bet); required_future_winnings_for_implied_odds(pot_size, call_cost, hit_probability) -> float; required_fold_equity(P, R, C, E) -> float; outs_to_equity_rule_2_and_4(outs, cards_to_come) -> float; compute_spr(effective_stack, pot_size) -> float; count_hand_combos_with_blockers(pattern, known_cards) -> int; breakeven_bluff_percentage(bet_size, pot_size) -> float; equity_realization(eq, pot, cost, ev=None) -> (r, r_required, percent_ev)` (C:/Users/hampe/Desktop/PokerB/knowledge_base/math/formulas.py:7,53,104,150,183,259,322,374,439,473,594,624). Everything the trainer's math checks + the German glossary formulas need exists here — pot odds, MDF, alpha (breakeven bluff %), bluff-to-value r=B/(P+B) (audited 2026-07-06, correct denominator), implied odds, fold equity, SPR, rule of 2&4, blocker combos, equity realization. All raise ValueError on bad inputs (grader should catch). Note compute_pot_odds convention differs from api.pot_odds's call: F.compute_pot_odds(P=pot-before-villain-bet, B=villain-bet, C=call); api.pot_odds treats Spot.pot as P with B=C=to_call — for grading from a Spot, use api.required_equity/api.mdf and the understanding.py:_geometry usage as the reference.
- `api.solve_node / api.preflop_solve / api.preflop_mix` — `solve_node(spot) -> {verb: prob, '_sizes_bb': {...}} | None; preflop_solve(spot) -> same | None; preflop_mix(spot) -> dict | None` (C:/Users/hampe/Desktop/PokerB/pokerbot/brain/api.py:392,123,107). The optional heavier truth layer. solve_node = live TexasSolver on the actual board — HU postflop only (n_active==2), ~6-30s+ per uncached solve (cached by board+pot-bucket+stack-bucket+range), timeout 150s -> NOT suitable for synchronous live grading; only for an async 'deep analysis' button or the replay walkthrough. preflop_mix/preflop_solve = HU-200bb blueprint only (returns None on 6-max preflop, api.py:130) -> the 6-max trainer canNOT use it for preflop grading; preflop grading must come from another source (advisor nets are postflop-only too).
- `snap_to_tree / classify_board (sizing check)` — `snap_to_tree(bet_chips: int, pot: int, street: str|None = None) -> int; classify_board(board) -> dict` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/postflop.py:49,76). The sizing check = compare the human's bet to snap_to_tree's nearest GTOW-tree pot-fraction (grid TREE_SIZES, or GTOW measured street grid with POKERB_GTOW_TREE=1); distance bet/pot vs snapped fraction gives a graded sizing error. snap_raise_to_tree(desired_to, call_level, pot, to_call, street) (postflop.py:60) covers raises (increment-over-call as fraction of post-call pot). Pure arithmetic, zero latency.
- `api.legalize` — `legalize(spot, action: str, size_bb: float|None = None) -> (action: str, amount_chips: int|None)` (C:/Users/hampe/Desktop/PokerB/pokerbot/brain/api.py:469). Coerces any (action, size) to a legal engine action with safe fallback check>call>fold. Trainer UI input validation can reuse this instead of re-implementing legality.
**Integration points:**
- Per-decision capture: at each human decision point in the 6-max app, build the record with spot_from_table(table, seat) (pokerbot/brain/format_spot.py:81) BEFORE the action is applied — this Spot is the single input every grader check and every explanation consumes.
- Explanation layer seed: strategic_read(spot) (pokerbot/brain/understanding.py:32) gives the full engine-computed frame (geometry/hand/initiative/principle) — the trainer's German feedback generator should call the same private helpers (_geometry math via api.*, _effective_stack_bb, _in_position) or re-derive from api.* with the exact conventions understanding.py:67-82 demonstrates, then render German text + glossary-tagged terms.
- Advisor-frequency check hooks: pokerbot/strategy/advisor.py:87 p_bet (betting node) + advisor.py:144 p_defense (facing-bet node, 3-tuple fold/call/raise) — postflop only, always None-guarded via available(street)/defense_available().
- Pot-odds/MDF check hooks: api.required_equity + api.mdf (pokerbot/brain/api.py:63,75) with the pot-convention exactly as understanding.py:76-81 (req uses spot.pot as-faced; MDF uses pot_pre = spot.pot - spot.to_call).
- Equity for the pot-odds comparison: api.equity(hero, villain_range, board, iters=1000, seed=fixed) (api.py:29) — villain range can come from the trainer's assumed range or the engine's range tracker; river is exact enumeration.
- Sizing check hook: pokerbot/strategy/postflop.py:49 snap_to_tree + :60 snap_raise_to_tree — grade |human_frac - snapped_frac|.
- Deep-analysis / replay walkthrough (async only): api.solve_node(spot) (api.py:392) for HU postflop exact GTO mixes; never call it synchronously in the live grading path (6-150s).
- Latency budget control: pokerbot/brain/modes.py — wrap live grading in modes.using('standard') (1000 MC iters, ~5s budget) or pass iters explicitly to api.equity.
**Gotchas:**
- POT CONVENTIONS ARE THE #1 FOOTGUN: Spot.pot at a facing-bet decision ALREADY CONTAINS villain's bet. required_equity uses spot.pot as-is; MDF needs the PRE-bet pot (spot.pot - spot.to_call). understanding.py:76-81 is the audited reference usage — copy it. F.compute_pot_odds has a THIRD convention (P = pot before villain's bet, B separate). Mixing these silently skews every grade by the bet size.
- Advisor coverage is POSTFLOP-ONLY and can be None (torch missing, model file missing, street uncovered). p_defense covers flop/turn/river facing-bet; p_bet covers the betting node. There is NO preflop advisor distribution — preflop grading needs the trainer's own source (the HU blueprint api.preflop_mix returns None on 6-max by design, api.py:130-131). The grader must render 'no reference available' rather than treating None as 0.
- Advisor 'role' is a binary IP/OOP string, and the nets were trained HU — in a 6-max multiway spot the IP/OOP mapping is a simplification (format_spot.py:127 uses BTN/SB=IP, a HU convention). ADVISOR_ROLE_POS (v3.5, default-OFF) exists precisely because advisors were queried by initiative but trained by position — the trainer should treat advisor frequencies as a soft reference band (the design doc's 10-20% error band), never a hard right/wrong.
- solve_node latency: 6-30s+ per uncached solve, 150s timeout, HU-only (n_active==2). Never in the synchronous grading path. p_bet/p_defense/api.* math are all sub-ms (after a one-time ~1-2s torch model load — pre-warm at server start by calling advisor.available('flop')/available('turn')/available('river')/defense_available() once).
- api.equity default seed=0 => deterministic — GOOD for reproducible grades/autotest, but note iters comes from the GLOBAL mode (modes.current()); another code path calling set_mode('fast') changes grading precision silently. Pass iters explicitly in the grader.
- equity_vs_range/equity_vs_weighted_range return float('nan') on an empty/blocked range — a naive threshold comparison (nan >= x is False) silently mis-grades; check math.isnan.
- strategic_read output is ENGLISH and hardcoded prose — the German trainer must NOT surface it verbatim; use its computations (same api calls + constants) and write the German rendering fresh. The named constants (STRONG_MADE 0.62 etc.) belong in the shared glossary/threshold module so UI text and grader agree.
- PRINCE v2.2 profile: flags flow through pokerbot/strategy/gto_mode.flag() (see advisor.py:27) — the trainer must run with POKERB_PRINCE=1 set BEFORE module imports, since several gates (RIVER_LA, INCLUDE_MADE_HAND, etc.) are read ONCE at import time, not per call.
- Import-time env reads: format_spot.py INCLUDE_* and advisor.py RIVER_LA are frozen at first import — toggling env vars at runtime does nothing; decide the trainer's env in the launcher.
- formulas.py functions raise ValueError on zero/negative inputs (e.g. compute_pot_odds with C=0, equity_realization with eq=0) — grader paths hitting a check-node (to_call=0) must branch before calling, as understanding.py does (spot.to_call > 0 guard).
**Reuse instead of rebuild:**
- understanding.py wholesale: its constants (RIVER_BET_MEDIAN_X, SPR_COMMITTED/DEEP, STRONG_MADE/MEDIUM_MADE), its _in_position/_effective_stack_bb helpers, and its _geometry pot-convention logic — the trainer's grader + German explainer should import/mirror these rather than re-derive (they encode the audited conventions).
- The Spot dataclass + spot_from_table as the per-decision record — do NOT invent a new spot schema for the trainer's data layer; serialize Spot (dataclasses.asdict) plus the human action and the grade.
- api.py as the single grading vocabulary: required_equity, mdf, spr, hand_rank, board_texture, equity, solver_freq, legalize — thin, tested, deterministic; the grader calls these, never raw engine internals (except advisor.p_defense which lacks an api wrapper — add one thin wrapper rather than importing the net logic).
- knowledge_base/math/formulas.py for every glossary formula (alpha, bluff-to-value, implied odds, fold equity, equity realization, blocker combos) — audited 2026-07-06 by the dual-blind re-derivation campaign and guarded by tests/test_math_suite.py; each function's docstring already cites the book/page, reusable for the glossary's 'Quelle' line.
- snap_to_tree/snap_raise_to_tree for the sizing check — the GTOW-tree grid already exists; do not hardcode a new size grid.
- modes.py for the latency budget — the FAST/STANDARD split is the existing accuracy-vs-time lever; the trainer's live grade = STANDARD-class iters, its replay deep-dive = DEEP or async solve_node.
- The advisor memoization pattern (advisor.py:82-98) — grading replays of the same hand hit the same (hole, board, role, street) keys for free; no extra caching layer needed.

### EXPLOIT/DIFFICULTY
**Core APIs:**
- `sixmax.OppModel (per-opponent counter dataclass)` — `@dataclass OppModel: hands, vpip, pfr, tb, tb_opp, faced_bet, fold_bet, agg, agg_opp; fold_to_bet()->float|None (gate faced_bet>=8), threebet()->float|None (gate tb_opp>=6), aggression()->float|None (gate agg_opp>=8)` (C:/Users/hampe/Desktop/PokerB/pokerbot/arena/sixmax.py:207-227). THE data source for the 'what the bot has learned about you' panel in the 6-max trainer. Raw counts are always readable even below the gates (the derived methods return None until min samples). Tracks per opponent seat: VPIP count, PFR count, 3-bet made/opportunities, postflop faced-bet/folded, postflop aggression acts/opportunities.
- `SixMaxBot` — `SixMaxBot(seat:int, knobs:Knobs); .new_hand(seats), .observe(actor,street,action,to_call,preflop_raises), .decide(obs)->{action,amount,rationale}, ._read(obs)->dict (exploit deltas), .opp: dict[int,OppModel]` (C:/Users/hampe/Desktop/PokerB/pokerbot/arena/sixmax.py:229-322). Stateful per-seat agent. .opp[0] = its model of the HUMAN (seat 0). Because six_server._observe_all feeds every public action to every bot identically, bots[1].opp[0] == bots[s].opp[0] for all s — the panel can read ONE bot's model of the human. ._read returns the LIVE exploit deltas currently applied vs the target, with human-readable tags: 'vs 3bet-happy' (threebet>0.11 -> cont_bonus up to +0.15), 'bluff-catch vs aggro' (aggression>0.55 -> call_delta -0.07), 'he only value-bets' (aggression<0.30 -> call_delta +0.07), 'he over-folds' (fold_to_bet>0.55 -> bluff_mult up to 2.2), 'station, no bluff' (fold_to_bet<0.30 -> bluff_mult 0.4). These exact trigger thresholds ARE the panel content: show the human's stat vs the threshold and whether the exploit is firing.
- `Knobs` — `@dataclass Knobs: name, open_mult, tb_pct, fb_pct, flat_hi, cont_lo, value_eq, raise_eq, bluff_mult, call_delta — defaults reproduce the neutral TAG core` (C:/Users/hampe/Desktop/PokerB/pokerbot/arena/sixmax.py:33-46). The continuous difficulty lever set. All floats — a difficulty controller can INTERPOLATE between profiles (e.g. lerp(nit,tag,x)) or construct custom Knobs directly, no code change needed. bluff_mult and call_delta are the biggest 'exploitability' dials (how punishable the bot is).
- `PROFILES` — `PROFILES: dict[str,Knobs] = {nit, tag, lag, station, maniac, rock, whale, shark}` (C:/Users/hampe/Desktop/PokerB/pokerbot/arena/sixmax.py:48-58). 8 named profiles. rock/whale/shark are HELD-OUT variants (never RL-trained-against) — perfect extra difficulty tiers: whale/rock = easy (very exploitable), shark = hard (near-TAG with mild exploit lean). Difficulty ladder by table composition alone: easy={station,whale,nit,rock,maniac} -> mid={tag,lag,nit,station,maniac} (current default) -> hard={tag,tag,shark,lag,shark}.
- `six_server profile assignment` — `_assign = {1:'tag',2:'lag',3:'nit',4:'station',5:'maniac'}; self.bots = {s: SixMaxBot(s, PROFILES[_assign.get(s,'tag')]) for s in range(1, n)}` (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:36-38). The ONLY place table composition is set — hardcoded in Session.__init__. The difficulty controller replaces this dict per session (e.g. from a ?difficulty= param or trainer config). Session also already tracks human_net and hands_done (six_server.py:34-35) — free inputs for adaptive difficulty.
- `strategy.opp_model.OppModel (Dirichlet, HU engine)` — `OppModel(kappa=3.0); .observe(key,resp,w=1.0); .posterior(key,prior=(0.5,0.4,0.1))->([P(fold),P(call),P(raise)], n_eff); .save(path=data/opp_model.json); .load(path)` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/opp_model.py:32-62). Per-NODE (not per-player) fold/call/raise posterior, keyed by node_key(street,role,line,board_class,size_bucket) (opp_model.py:28, size buckets q/half/twothird/pot/big/over). Wired ONLY into the HU PokerBot (bot.py), which learns live from responses to ITS OWN river bets (observe_hand_end, bot.py:1251-1276). Persistence exists (save/load JSON) but is not auto-called by any server. Panel-usable in a HU trainer mode: 'auf River nach Board-Typ X foldest du Y% (n=Z)'.
- `opponent.OpponentModel (HU aggregate tracker)` — `OpponentModel: record(street,action,facing_bet), end_hand(); fold_to_bet_freq(k=6), aggression_freq(k=6), vpip(k=6) — Bayesian-shrunk; confidence()->0..1 (faced_bet/20 + pf_actions/30); summary()->{hands,vpip,fold_to_bet,aggression,confidence}` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/opponent.py:9-68). The HU bot's player-level read. summary() is ALREADY exposed per decision in rationale['opponent'] when exploit=True (bot.py:565) — a HU trainer panel gets it for free from the decision payload. confidence() is the exploit-strength gate (full trust ~20 faced bets / ~30 preflop actions, i.e. ~15-25 hands).
- `PokerBot exploit switch` — `PokerBot(hero_idx:int, seed:int|None=None, exploit:bool=True); env kill-switch POKERB_EXPLOIT=0 forces exploit=False (bot.py:143-145)` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/bot.py:134-151). exploit=True gates: (1) conf>0 -> preflop 3bet-bluff freq scales with villain fold_to_bet (bot.py:431 bluff_p=0.30+conf*(fold_to_bet-0.5)); (2) postflop bluff_base likewise (bot.py:559) + PriorFoldModel(fold_to_bet,conf) fold-equity model (bot.py:558); (3) MDF shade vs villain aggression, capped +/-0.12 (bot.py:621-623); (4) the EXPLOIT-PRIMARY river engine _river_exploit (bot.py:743, 1104-1140): LCB-gated deviation from the floor's river action to the max-EV size vs the Dirichlet posterior, incl. the 2x-pot overbet cliff, plus the prediction-gated off-tree river probe (_river_probe, 1142-1179, session risk budget); (5) rationale['opponent']=summary(). exploit=False zeroes conf -> pure PRINCE floor. NOTE: bot.py is HU-only (villain hardcoded as 1-hero_idx, bot.py:1260) — it does NOT run in the 6-max app.
- `PokerBot learning hooks` — `observe_opponent(street,action,facing_bet); observe_hand_end(final_state) — pairs hero river bets with villain responses and feeds opp_model.observe` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/bot.py:1248-1276). How the HU bot would learn the TRAINEE live: the caller (server/game loop) must invoke observe_opponent per human action and observe_hand_end with the final state. The Dirichlet model then sharpens per river node during the session.
- `seed_from_fold_curve` — `seed_from_fold_curve(model, curve_path=None, street='river', n_eff=30.0)->int — seeds Dirichlet river buckets from a measured fold curve` (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/opp_model.py:65-83). Warm-start mechanism; a trainer could seed the bot's model of the human from the human's OWN prior sessions (session logs -> fold curve) so the exploit panel is non-trivial from hand 1.
**Integration points:**
- six_server.py Session.__init__ (C:/Users/hampe/Desktop/PokerB/pokerbot/web/six_server.py:36-38) — the difficulty controller's single hook: replace the hardcoded _assign profile dict + optionally pass interpolated custom Knobs per seat; Session already owns human_net/hands_done (lines 34-35) for adaptive re-composition between hands.
- sixmax.py SixMaxBot.decide (C:/Users/hampe/Desktop/PokerB/pokerbot/arena/sixmax.py:320-322) — the exploit-gain hook: decide() calls self._read(obs); there is currently NO off/scale switch for the bots' exploit layer. Add one field to Knobs (e.g. exploit_gain: float = 1.0) and multiply/zero the read deltas in _read (lines 290-318) — this is the cleanest per-bot difficulty dial beyond composition.
- sixmax.py _read caps (C:/Users/hampe/Desktop/PokerB/pokerbot/arena/sixmax.py:299,306-308,315-317) — the numeric bounds a difficulty controller can widen/narrow: cont_bonus cap 0.15, call_delta +/-0.07, bluff_mult 0.4..2.2, plus the firing thresholds (threebet>0.11, aggression 0.30/0.55, fold_to_bet 0.30/0.55).
- Panel data source (6-max): session.bots[1].opp[0] (any bot's model of seat 0 — identical across bots because six_server._observe_all at six_server.py:49-53 feeds every public action to all bots) — read raw counters + the three gated rates + evaluate the _read trigger thresholds against them to show which exploits are LIVE vs the trainee.
- bot.py PokerBot(hero_idx, seed, exploit=True) + POKERB_EXPLOIT env (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/bot.py:134-145) — only relevant if the trainer adds a HU mode or a PRINCE-strength hero-replacement seat; the PRINCE v2.2 profile (POKERB_PRINCE=1 via pokerbot/strategy/gto_mode.py) rides on this HU bot, not on SixMaxBot.
- bot.py rationale payload (C:/Users/hampe/Desktop/PokerB/pokerbot/strategy/bot.py:560-565) — in exploit mode every decision already carries rationale['opponent']=self.opp.summary() plus exploit_river/river_probe sub-dicts (1133-1134, 1175) — free per-decision explanation data for the HU trainer feedback layer.
- PokerBot.observe_opponent/observe_hand_end (bot.py:1248-1276) — the wiring a HU trainer server must call per human action / per hand end so the Dirichlet model learns the trainee live; opp_model.save/load (opp_model.py:51-62, data/opp_model.json) gives cross-session persistence of what the bot learned about YOU.
**Gotchas:**
- NAME COLLISION: two unrelated classes are both called OppModel — pokerbot/strategy/opp_model.py (Dirichlet per-NODE, HU engine) and pokerbot/arena/sixmax.py:207 (per-OPPONENT counter dataclass). Trainer code must import explicitly (from pokerbot.arena.sixmax import OppModel as SeatOppModel or similar) or a future one-run agent will wire the wrong one.
- bot.py (PRINCE v2.2 + exploit) is HEADS-UP ONLY: villain is hardcoded as 1-hero_idx (bot.py:1260) and state parsing assumes 2 players. It cannot sit at the 6-max table without adaptation. The 6-max trainer's opponents are SixMaxBot; 'PRINCE + exploit=True' as bot basis implies either a HU trainer mode or accepting SixMaxBot(tag)+_read as the 6-max exploit approximation.
- SixMaxBot state is in-memory per Session only — the bots' model of the human resets every session; no save/load exists on the sixmax OppModel (only the Dirichlet one persists, and only for the HU bot, and no server currently calls .save()). A cross-session 'what the bot learned' panel needs a small serializer for the 9 counter fields.
- Sample gates mean the panel is EMPTY early: fold_to_bet needs >=8 postflop faced-bet decisions, threebet >=6 opportunities, aggression >=8 postflop actions — typically 10-25 hands. The panel must render a 'noch nicht genug Daten (n/N)' state from the raw counters instead of None.
- SixMaxBot uses an UNSEEDED random.Random() per seat (sixmax.py:235) and _decide falls back to a module-level _RNG for stateless callers (sixmax.py:81) — real mixing, good for play, but the mandatory autotest/replay walkthrough needs injectable seeds; note the in-code warning that per-spot reseeding creates FAKE deterministic mixing (sixmax.py:96-97), so seed per-session, not per-spot.
- The sixmax exploit layer (_read) has NO off switch and its deltas are hard-capped — difficulty via 'exploit intensity' requires the small Knobs.exploit_gain addition; do NOT try to control it via POKERB_EXPLOIT (that flag only touches bot.py).
- six_server._observe_all maps 'allin' -> 'raise' before feeding bots (six_server.py:51) — a trainer data layer replicating the pipeline must do the same or counts drift.
- Difficulty is NOT linear in profile 'looseness': station/whale punish over-bluffing trainees hard while being trivially value-bet — an error-band controller should compose tables per LEAK TYPE (the trainee's measured leaks), not per a scalar difficulty; the trigger thresholds in _read give the leak taxonomy for free.
- The bots' exploits are convergent-observation by design (INTEGRITY block, sixmax.py:4-8): all 5 bots independently form the same read of the human — the panel should present ONE read, not 5, to match reality.
- PRINCE v2.2 = POKERB_PRINCE=1 (20-flag profile in pokerbot/strategy/gto_mode.py); several bot.py exploit-adjacent paths are additionally flag-gated (_PAIR_DEFENSE, _TURN_DEFENSE, POKERB_TURN_DEF_ADVISOR, _AUDIT_FIX, _SLOWPLAY, _PURIFY2 — bot.py:583-638, 432, 452) — per CLAUDE.md the v3+ levers are default-OFF re-test arms; the trainer must pin the v2.2 profile and not flip these.
**Reuse instead of rebuild:**
- Table composition ladder: reuse PROFILES as-is (8 profiles incl. held-out rock/whale/shark) — the 10-20% error-band controller's coarse lever is just a per-session _assign dict; zero new bot code for tier 1.
- Continuous difficulty: reuse the Knobs dataclass — interpolate numeric fields between two named profiles for a smooth dial; the neutral defaults are documented to reproduce the plain TAG core exactly (sixmax.py:35).
- 'What the bot has learned about you' panel: reuse SixMaxBot.opp[0] counters + the _read trigger thresholds verbatim (threebet 0.11, aggression 0.30/0.55, fold_to_bet 0.30/0.55) — the panel is a pure READ of existing state plus threshold comparisons; the German tag strings ('he over-folds' etc.) map 1:1 to glossary entries.
- Per-decision explanation data: every SixMaxBot/PokerBot decision already returns rationale dicts (hand class, percentile, equity, pot-odds/required_equity, made hand, texture, exploit tags, exploit_river details) — the trainer's data layer and glossary-linkable feedback should consume these, not recompute.
- Live-learning of the trainee (HU mode): reuse PokerBot.observe_opponent/observe_hand_end + opp_model.save/load + seed_from_fold_curve — the whole learn-persist-warmstart loop exists; only the server-side call wiring and a session->fold-curve extractor are new.
- Session plumbing: six_server.Session already logs every hand (session_log.append_record), tracks human_net/hands_done, and has the /api/analyze end-of-session breakdown — the difficulty controller and the data layer should extend Session, not build a parallel loop.
- OpponentModel.summary() (opponent.py:61) is a ready-made compact JSON for a HU 'bot's read of you' widget — already emitted in rationale when exploit=True.

### REPORT/EXPORT
**Core APIs:**
- `session_analysis.compute_stats` — `compute_stats(recs: list[dict]) -> dict` (C:/Users/hampe/Desktop/PokerB/pokerbot/analysis/session_analysis.py:21). ALREADY operates on our OWN session JSONL logs (data/sessions/, written by six_server) — schema per record: {human_seat, bb, net:{seat:chips}, actions:[{is_human, street, action, amount}], result:{reason}}. Returns hands, vpip_pct, pfr_pct, threebet_pct, postflop AF + action counts, WTSD%, net_bb, bb_per_100, avg_postflop_bet_bb. This is the zero-work baseline the trainer's session report should start from (it is what /api/analyze already serves); extend with prince_protocol's richer metrics via the adapter.
- `session_analysis.narrative` — `narrative(stats: dict) -> str` (C:/Users/hampe/Desktop/PokerB/pokerbot/analysis/session_analysis.py:80). The existing German Claude coach-narrative pattern: anthropic client from config.ANTHROPIC_API_KEY + config.CLAUDE_MODEL, German system prompt, graceful '' / error-string fallback when no key. Copy this exact pattern for the trainer's language layer (interface-first: stats dict in, German text out).
- `session_analysis.analyze_session` — `analyze_session(path) -> {stats, narrative}` (C:/Users/hampe/Desktop/PokerB/pokerbot/analysis/session_analysis.py:100). One-call session report over a JSONL log path. The trainer endpoint can call it directly.
- `pokerstars_export.format_hand (HU)` — `format_hand(hand_id, dt, names, button, holes, history, result, hero_seat) -> str` (C:/Users/hampe/Desktop/PokerB/research/pokerstars_export.py:84). One engine hand -> a PokerStars HH block. Input = exactly what the engine already yields: holes=[[c1,c2],...], history=HeadsUpGame.history events ({action: deal|fold|check|call|bet|raise, street, player, amount|to, board}), result={winner, board, reason, hands:[{rank}]}, plus button. Handles uncalled-bet refund, pot accounting, silent all-in run-out board sections (post-2026-07-05 fix), SUMMARY section. HU 2-max only; SB/BB/STACK are module constants (50/100/20000).
- `sixmax_export.format_hand (6-max)` — `format_hand(hand_id, dt, h: {names, button, holes, history, result}) -> str` (C:/Users/hampe/Desktop/PokerB/research/sixmax_export.py:82). THE exporter the 6-max trainer needs for GTOW-Analyzer validation: takes the dict play_hand() returns straight from Table (holes=[s.hole], history=table.history, result=table.result, button, names). For trainer sessions, serialize exactly {names, button, holes, history, result} per hand into the session log and this function converts to uploadable HH with zero replay. Geometry: SB/BB/STACK = 50/100/10000 (100bb standard GTOW 6-max).
- `sixmax_export._snap_hero` — `_snap_hero(table: Table, decision: dict) -> (action, amount)` (C:/Users/hampe/Desktop/PokerB/research/sixmax_export.py:34). Snaps sizes onto GTOW-tree conventions (preflop open 2.5bb / reraise 3x, postflop pf.snap_to_tree) — off-tree sizes are UNGRADEABLE by the Analyzer. If the trainer wants the USER's played hands GTOW-gradable, either snap the user's bets at export or accept degraded grading; document the choice.
- `sixmax_export.play_hand` — `play_hand(table: Table, bots: dict) -> {holes, history, result, button, names}` (C:/Users/hampe/Desktop/PokerB/research/sixmax_export.py:59). Shows the guard-loop + illegal-action fallback (check->call->fold) idiom and the exact per-hand record shape the exporter consumes — the trainer's hand-record schema should match this dict.
- `test_math_suite check registry` — `@check(name) decorator -> CHECKS list; fn returns None (pass) or failure-detail str; main: exit 1 on FAIL, --deep (20x fuzz), --json PATH, seed 42` (C:/Users/hampe/Desktop/PokerB/tests/test_math_suite.py:23-40). THE pattern for pokerbot/coach/autotest.py: a module-level CHECKS registry via decorator, deterministic seed, fast default + --deep flag, machine-readable --json output, exit code 1 on any failure. This is the repo's most disciplined test harness.
**Integration points:**
- Trainer session report endpoint: follow six_server's existing /api/analyze -> pokerbot/analysis/session_analysis.py:100 analyze_session(path) flow; the trainer server should log per-hand JSONL to data/sessions/ (or data/trainer/) in the SAME record schema {human_seat, bb, net, actions:[{is_human,street,action,amount}], result} so compute_stats works unchanged, PLUS the exporter fields {names, button, holes, history, result} per hand for HH export.
- GTOW export of trainer hands: pokerbot/coach (new export module or CLI flag) calls research/sixmax_export.py:82 format_hand(hand_id, dt, h) over the logged hand records; write with open(out, 'w', encoding='utf-8', newline='\r\n') exactly as research/pokerstars_export.py:246 does; user uploads via GTO Wizard -> Analyze -> Upload hands.
- German language layer: clone the pattern at pokerbot/analysis/session_analysis.py:80 narrative() — config.ANTHROPIC_API_KEY gate, config.CLAUDE_MODEL, German system prompt, graceful fallback string when the key/API is absent (trainer must work offline).
- Glossary data: a {begriff, erklaerung} item schema; store as a static JSON the collapsible UI panel loads; feedback-text term matching happens client-side against the begriff keys.
- Autotest harness: pokerbot/coach/autotest.py structured like tests/test_math_suite.py — @check registry, deterministic seeds, exit 1 on FAIL, --json; invoked as python -m pokerbot.coach.autotest (repo convention: every test is a __main__ module run from project root, NO pytest anywhere); server/UI end-to-end checks follow tests/test_six_client.py (urllib against the running server, wait_up() poll loop, scripted policy, guard counters) or FastAPI TestClient per CLAUDE.md ('verify UIs via fastapi.testclient.TestClient, not the screenshot tool').
**Gotchas:**
- CRLF is MANDATORY for GTOW-Analyzer uploads: open(out, 'w', encoding='utf-8', newline='\r\n') — LF-only files silently fail to analyze (pokerstars_export.py:243-246).
- Hand-ID dedup law: the Analyzer burns hand identities on FIRST CONTACT (even failed uploads) and dedups across ALL prior uploads. Never reuse idbase/dayoffset; legacy range 299-1822+ is burned; new families start at idbase >= 50000 with >= 2000 spacing per export (pokerstars_export.py:210-224 + STATE.md ledger). The trainer's exporter MUST generate fresh id blocks (e.g. timestamp-derived idbase) automatically.
- Off-tree bet sizes are ungradeable by GTOW: sixmax_export snaps hero sizes to tree conventions (_snap_hero, line 34). A trainer exporting the USER's raw sizes will get degraded/absent grading on off-tree bets — either snap at export or warn in the report.
- gto_mode.apply() must run BEFORE any strategy import (flags are read at import time) — pokerstars_export.py:16-21. The trainer server must set POKERB_PRINCE=1 / exploit toggles before importing pokerbot.strategy.bot, not after.
- All-in run-out streets are dealt silent=True by the engine (no deal events) — any HH formatter must emit the missing FLOP/TURN/RIVER sections post-hoc (pokerstars_export.py:151-162; sixmax_export has the same handling). If a new formatter is written instead of reused, this bug WILL recur.
- session_analysis.compute_stats threebet detection is crude (any human preflop raise when >=2 total preflop raises exist — counts 4bets too and misses opportunity denominators); an opportunity-based 3bet/4bet logic is preferable for graded stats.
- No pytest in this repo: every test is python -m tests.<name> with a main() and asserts/exit-codes; the six_server test needs the server already running (wait_up polls, doesn't launch). six_server reads six.html fresh per request but OTHER servers cache HTML at import -> restart after UI edits.
- HU exporter geometry is 200bb (STACK=20000), 6-max exporter is 100bb (STACK=10000) — the trainer's table config must match the exporter constants or the HH stack lines will lie; both exporters state stacks as the reset value each hand (cash-game reset).
**Reuse instead of rebuild:**
- session_analysis.compute_stats + analyze_session (pokerbot/analysis/session_analysis.py) — the working stat+narrative pipeline over our OWN JSONL decision logs, already wired to six_server /api/analyze; extend, don't rebuild.
- sixmax_export.format_hand + _snap_hero + play_hand record shape (research/sixmax_export.py) — the complete 6-max PokerStars-HH exporter for GTOW-Analyzer validation of trainer hands; pokerstars_export.format_hand for any HU trainer mode.
- narrative() Claude-call pattern (session_analysis.py:80) — German coaching text with offline fallback; same config-key plumbing (pokerbot/config.py, keys from Secret keys dir, never hardcoded).
- Glossary item schema {begriff, erklaerung} — the German plain-language glossary content pattern for the collapsible UI panel.
- test_math_suite.py check-registry harness (decorator CHECKS, seed 42, --deep, --json, exit 1) — the structural template for pokerbot/coach/autotest.py; test_six_client.py wait_up/post/policy/guard loop — the end-to-end server test template.
