# QUANTPLAY — PARASITIC ARCHITECTURE + ROADMAP OF THE NEXT BASE VERSION (2026-07-06)

## The guiding principle: the reduced-genome parasite
We do not build a solver — we build the ORGANISM that uses solvers, equilibrium engines and papers as
HOSTS. Selection rule for EVERY building block:
1. **Outsource (reduced genome):** can a host do it cheaper? Solve→TexasSolver,
   equilibrium→GTOW, architecture→papers. Our genome stays tiny (125KB, consumer PC, ~$50).
2. **Transfer (horizontal gene transfer):** is it best-in-domain elsewhere? Build it in — DeepStack
   gadget, Johanson iso, Jackson uint8, GTOW frequencies. A mosaic genome made of nothing but peaks.
3. **Harvest (klepto-/brood parasitism):** appropriate the expensive rearing (solves, revealed cards).
4. **Keep distance (anti-mimicry):** too-perfect imitation kills our own edge (v8 PURIFY break).
5. **Red Queen:** static hosts (GTOW) are safe prey; adaptive opponents need regime-aware exploit.

## What we are (honestly): a TexasSolver with perception, body, reflexes, memory
Borrowed: resolver=TexasSolver, advisors=distilled, gold=solver output. OURS (non-solver):
range reconstruction from the line · whole-hand orchestration · real-time routing · measurement loop.
CEILING (honestly): we never undercut TexasSolver's own exploitability → we inherit its abstraction
leaks. That is why better leaf values (patch nets) become relevant at some point — but LATE.
EDGE DECOMPOSITION (diagnosis, buildable): vs "solver+TRUE ranges" = range error; vs "solver+tracked ranges"
= orchestration error. These two numbers tell where the −20 really sits.

## THE TWO CORPORA (nervous system of the iteration; unify under dataset/registry.py)
- **Corpus A — GOLD:** every GTO-validated (spot→action/frequency). From solve cache + GTOW reveals +
  census. Positive teacher: advisor training, range calibration, flop library.
- **Corpus B — SIN REGISTER:** every measured error (spot + deviation + EV cost). From money_mine +
  GTOW grades + danger sets. Negative teacher: scheduler priorities, patch queue.

## ROADMAP — the next base version (every phase against anchor tag v2 = −19.70, precision doctrine)
**Phase 0 — Unify corpora ($0, days).** Extend registry.py by A/B; money_mine + grades + solve cache
+ reveals into one indexed structure (state key: position/stack/pot/line/texture). Deliverable: queryable
A (gold) + B (errors) with coverage statistics. The IS-building-block of everything after.

**Phase 1 — RANGE TRUTH (highest lever, $0, deterministic).** GTOW reveals as ground truth:
state key → empirical f_K vs tracker R̂ → logit correction δ_K, smoothed, fall back at n<30. Two-stage
tracker (Bayes → δ_K → renormalize). Gate: cross-entropy/calibration on held-out keys BETTER; then
paired Analyzer arm vs v2 flags. Multiplies EVERY organ.

**Phase 2 — FLOP LIBRARY (the flop-bleed fix, offline, pod scale).** Board clusters (paired/mono/twotone/
rainbow × high/mid/low × connect) × pot/SPR × bet menu; solve representative flops offline (hours okay),
online = lookup + interpolation. Falls back to advisors on miss (graceful degradation). Gate: close to
TexasSolver on held-out flops; own Analyzer arm. The ../catalogs/FLOP_LIBRARY.md conditions apply.

**Phase 3 — SCHEDULER + OFF-TREE TRANSLATOR (coordination logic, no net).** Importance scheduler:
per (pot, SPR, texture, line) → which organ + which bet menu + how much solve budget (no iter budget
on $1 pots). Off-tree translator: real villain actions mapped EV-impact-aware onto on-tree proxies + range
adjustment. Both address measured −20→−10 leaks. Gate: danger-paired + live smoke.

**Phase 4 — RESOLVER PRECISION (pure compute).** First close the matrix gap (GRADE the resolver, bounded
solves) → where it beats the advisor, raise iters/exact enumeration. Changes no behavior.

**Phase 5 — PATCH NETS (last, narrow, ONLY if Gate 0 is passable).** Turn-boundary leaf (enable flop
resolve) / river cut / range-residual denoiser. Never the central engine (Gate-0 proof + consult).

## Definition of the final base version
Not "perfect", but the SKELETON wired: blueprint + calibrated tracker + scheduler + off-tree +
flop library + live resolver + advisors + regime-aware exploit, with the A/B corpora as nervous system and the
patch nets as late, narrow organs. Every later step compounds cumulatively on this skeleton.
