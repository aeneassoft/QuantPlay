# PRECISION DOCTRINE — optimize decisions, do not change them (User, 2026-07-06)

> Born from the empirical finding of the night of 07-05/06: EVERY behavioral lever carried
> interaction risk (family E: winners ate each other; live break: lever x resolver
> −58 instead of −20). NO precision step ever backfired (memoization byte-identical, exact
> river enumeration, determinism, census trees). Consequence: the class of improvement changes.

## The law
A change is permissible if it computes THE SAME decision-theoretic quantity MORE PRECISELY
(convergence to the same fixed point; cost = compute only). Impermissible as a default is any
change of the objective per class (thresholds/discounts/menu extensions/modality knobs) —
such levers only as an exception with class-dense paired evidence AND live confirmation, never
stacked.

## Permissible steps (the precision queue)
1. Resolver convergence: iters/accuracy per solve up (no latency limit vs GTOW); the achieved
   exploitability per solve is logged (Q3 fix active) — the convergence auditor is the gate.
2. Exact turn enumeration instead of MC at decision boundaries (eliminates ~0.9pp MC-SE).
3. Range truth instead of range levers: tracker changes (incl. v3.3 narrowing) validate themselves
   exclusively as range-error reduction vs GTOW's REVEALED cards (13.5k hands,
   check_range_l1 principle). Only truer ranges may feed the resolver.
4. Flop library (precompute; the ../catalogs/FLOP_LIBRARY.md conditions apply).
5. PATCH NETS (user directive): NO primary net. Small CFV nets as components that make
   a SOLVE deeper/more precise — first patch: a turn-boundary CFV net unlocks the
   flop resolver (depth limit at the turn, the net evaluates the leaf). Gate 0 (Leduc, exact
   exploitability) validates the machinery; then Gate 1 = small river/turn net
   (../plans/VALUE_NET_SPECS.md synthesis, pilot ladder, suit aliasing = blocking gate).

## The anchor (the only validated benchmark)
Tag v2 = −19.70 ± 4.37 live (n=2,393), rank 11. EVERY step measures against it: paired exports
always with a v2.2-flags arm; live runs always read against −19.70. The Analyzer levers of the night
(v3.3/OBM/PURIFY/TDA) remain archived facts, but touch the live bot again only once
they pass the precision check (range truth / convergence / live pair).
